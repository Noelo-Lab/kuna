# 06 — Variables & merge

```yaml
Anchors:
  - decompiler/crates/kuna-decomp/src/p6_variables
```

> **STATUS: stub.** Section list is normative; prose lands chapter by chapter
> (draft -> adversarial verify -> commit). See `docs/spec/README.md` for the rules.

## 6.1 HighVariables & Cover

*Scope:* forced vs speculative merge.

## 6.2 The stack frame

*Scope:* varmap, spacebase, restructure/restrict-local.

## 6.3 Dynamic hashes

*Scope:* identity for storage-less values; (kuna) dynamichashmax.

## 6.4 kuna extensions

*Scope:* (angr) foldcallret; (kuna) stackalias; (kuna) inputvarnodeadjust.

## 6.5 Cleanup

*Scope:* the post-fixpoint cleanup passes.
