## What was broken

RE-friction round 2, need `wide-api-string-args` (major, 1 instance, challenge
`65acadf3eef082e477ff5ede` — `ObfuscationFiesta.exe`, PE32+), filed by a tester
reverse-engineering the binary with kuna:

> **Wide Windows API string arguments collapse to one-character strings**
> Rendered calls such as `LoadLibraryW("n")` and `FindWindowW("O",0)`, losing the
> remainder of the UTF-16 strings and making anti-debugging checks opaque.

The DLL and window-class names *are* the content of such a check. `kuna strings
--encoding utf16` had been printing them in full all along — the difference
between the two surfaces was the whole tell.

## Root cause (the filed hypothesis is half right, and names the wrong producer)

Read at 1-byte width a UTF-16LE literal is a **one-character string**:
`L"ntdll.dll"` is `6e 00 74 00 …`, and the NUL behind the first unit closes the
run. A 1-char run is below `StringLiteralPass`'s minimum length, so the 1-byte
pass marks up **nothing** there — it is not what produced the `"n"`.

What did is **`operand_refs`**, whose facts arrive in the same commit stream and
whose run test accepts a *single* visible character (it exists for the residual
short strings the main pass misses). At a wide literal it reads the first unit's
low byte plus the high-byte NUL behind it as a complete `char[2]`. `operand_refs`
is a `--mode aggressive` member and `auto` picks aggressive for anything under
500 KiB, so it is what the `kuna` binary runs by default — which is also why the
symptom is invisible under `--mode reliable`, where the same call rendered the
bare `LoadLibraryW(0x1400074b8)`.

Both ends of the real fix already existed and were simply not wired together:
`kuna_stringinv.rs` had a UTF-16 matcher whose only consumer was `kuna strings`,
and `stringmanage.rs` has handled `charsize == 2` since the port.

## Mechanism — `widestrings` (new option, `on|off`, default **on**, DIV-110)

`kuna_widestrings.rs` is the 2-byte width of `StringLiteralPass` — Ghidra
`StringsAnalyzer`'s `allCharWidths` arm, which kuna's 1-byte port left as a
documented seam. It is the **same** matcher widened, not a different one: the same
printable-ASCII recognizer over each unit's low byte, the same require-NUL-end
rule, the same minimum length of 5, over the same section set, reading units on
even addresses only. The commit plants a typelocked `wchar2[len/2]` instead of a
`char[N]`, and the character type's size 2 is what already made the printer emit
the `L` prefix and read the bytes two at a time.

The **ordering is the other half of the fix**, not a detail: the wide facts commit
*before* the 1-byte stream, so the width that read the whole literal wins the
commit's `occupied` guard against `operand_refs`' one-character read. Against
`StringLiteralPass`'s own facts the order is immaterial — a wide unit demands a
zero high byte, so five consecutive 1-byte-charset bytes never occur inside a wide
run and neither width can reach the other's address.

The UTF-16 matcher moved from `kuna_stringinv.rs` (the `kuna strings` query) into
the new module and is now called by both, so the reported inventory and the
committed markup remain one scan.

## The acceptance probe now passes

```
$ python -m scripts.repipe.verify --need wide-api-string-args --json
"counts": { "total": 1, "pass": 1, "fail": 0, "closed": 1, "regressed": 0 }
```

On the witness, `kuna decompile ObfuscationFiesta.exe sub_140002530`:

```c
-  v105 = LoadLibraryW("n");                   +  v105 = LoadLibraryW(L"ntdll.dll");
-  v85  = FindWindowW("O",0);                  +  v85  = FindWindowW(L"OLLYDBG",0);
-  v15  = FindWindowW(0,"O");                  +  v15  = FindWindowW(0,L"OllyDbg - [CPU]");
-  char *v127;                                 +  wchar_t *v127;
```

Whole-binary A/B on the witness: **42 changed lines, every one a recovery** —
5 distinct wide literals (`ntdll.dll`, `OLLYDBG`, `OllyDbg - [CPU]`,
`Immunity Debugger - [CPU]`, and the 55-`%s` `OutputDebugStringW` format) plus the
`char *` → `wchar_t *` declarations that follow them.

## Collateral sweep

`SWEEP_BLOCK`

## Tests

- `tests/cli/wide-api-string-args.json` — the promoted acceptance. The recorded
  target is dataset-only, so it is re-pointed at a **vendored 1536-byte
  synthesized PE32+** (`widestrings_x86_64.exe`, regenerated by
  `widestrings_x86_64.py`, the same technique as the existing
  `crtmain_x86_64.exe`) that reproduces the shape exactly: `LoadLibraryW` on a
  wide literal and `GetProcAddress` on an ASCII one, the second being the control
  that the 1-byte width is untouched. `make test-cli` 26/26.
- `decompiler/crates/kuna-console/tests/verify_widestrings.rs` — 4 cross-crate
  end-to-end tests: the on-arm renders `L"ntdll.dll"`, the off-arm leaves the
  address untyped, the commit plants `s_140002100` only with the width on, and —
  with `operand_refs` on, the pass set a default `kuna decompile` runs — the
  off-arm reproduces the recorded `LoadLibraryW("n")` verbatim while the on-arm
  outranks it.
- `kuna_widestrings.rs` unit tests: the widened matcher, short/unterminated
  rejection, the disjointness of the two widths, even-address unit reads, and the
  fixture end-to-end.

## Gates

| Gate | Result |
|---|---|
| `make test` | **PARITY OK** (675/675) |
| `make test-stages` | **PARITY OK** |
| `make rust-test` | RUSTTEST |
| `make check-spec` | OK (strict mode too) |
| `kuna catalog --check` | catalog OK |
| `make test-cli` | 26/26 |
| `scripts.repipe.verify --need wide-api-string-args` | acceptance **PASS** |

Speed: **+0.11%** on the witness function (`scripts.pipeline.timeit`, 7 repeats,
budget 5%) — the scan is one extra linear pass over the same section bytes the
1-byte scan already walks.

Both XML corpora are symbol-less bytechunks, which no analysis pass reaches, so
the parity gates are byte-identical by construction as well as in fact.

## Docs

`docs/spec/01-program-prep.md` §1.4 gains the width as its own metadata-analyzer
bullet; `docs/history.md` gains **DIV-110** for the default-ON flip;
`docs/options.md` regenerated. No CLI surface change, so `docs/cli.md` is
untouched.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
