{
  "note": "The 11 changed functions corpus-diff.py leaves as REVIEW (types vs the default cycles, 15 binaries), each read by hand from the full diff.",
  "labels": [
    {"binary": "O0-tar-tar", "function": "sub_6a7a9", "label": "member-reach", "detail": "void *a0 -> struct_85 *a0; *(int8 *)(a0 + 0x10) -> a0->field_0x10; the indexed load a0[0x30] + v4*0x18 + 8 has its two addends printed in the other order"},
    {"binary": "O0-tar-tar", "function": "sub_6a8e2", "label": "member-reach", "detail": "struct_87 *a1 -> struct_85 *a1 (the same record under the ledger's surviving name); every a2*0x18 + a1->field_0x28 sum printed base-first"},
    {"binary": "O2-bash-bash", "function": "sub_466f0", "label": "store-width", "detail": "int4 *v4 -> struct pointer: the two 4-byte stores v4[4] = 0; v4[5] = 0; become one v4->field_0x10 = NULL, which is what the machine does (movq $0x0,0x10(%rax)); the rest is local renumbering"},
    {"binary": "O2-bash-bash", "function": "named_function_string", "label": "store-width", "detail": "same shape as sub_466f0: one movq $0x0,0x10(%rax) printed as one 8-byte store instead of two int4 stores; a1 unsigned long -> unsigned long *"},
    {"binary": "O2-bash-bash", "function": "sub_4f720 (execute_coproc)", "label": "regroup", "detail": "v6 = a0[1]; ... v6 &= 4; ... v11 = v6 != 0 becomes v1 = a0[1]; ... v11 = (v1 & 4) != 0: the mask folded into its two uses; a0/a3 retyped"},
    {"binary": "O2-bash-bash", "function": "sub_50b10", "label": "regroup", "detail": "a local holding one load compared twice (cmp $0x120 / cmp $0x121 on one mov 0x18(%rdx),%edx) is printed inline at both compares; nothing is written between them, so both read the same value; the rest is member-reach through a0 retyped int4 *"},
    {"binary": "O2-bash-bash", "function": "sub_7b800", "label": "regroup", "detail": "a temporary pointer (*v + -1 + v) used for one load and one store is printed inline at both as v->field_0x0[v + -1]; struct pointer member-reach elsewhere; local renumbering"},
    {"binary": "O2-bash-bash", "function": "assign_compound_array_list", "label": "member-reach", "detail": "struct_55 -> struct_47 (ledger name), *v -> v->field_0x0, *&v[1] -> v->field_0x8, 0 -> NULL for a pointer-typed local"},
    {"binary": "O2-coreutils-sort", "function": "sub_b220 (sequential_sort's parallel driver)", "label": "pointer-arith", "detail": "v24 + (v28 >> 1) * -0x20 -> &v23[(v28 >> 1) * -4] for a local retyped unsigned long *; locals renumbered"},
    {"binary": "O2-grep-grep", "function": "sub_cec0", "label": "member-reach", "detail": "v[1]/v[2] -> v->field_0x8/v->field_0x10 through a struct pointer; one indexed address held in a temporary instead of computed at both uses; a local split differently (v16/v32)"},
    {"binary": "O2-nuttx-nuttx", "function": "sub_801295c", "label": "wrong-callee-vote", "detail": "the call sub_8014484(&a1[3],v2,4,...) renders the literal 4 as &UndefinedInstruction: sub_8014484 is nuttx's self-recursive qsort and its OWN recovery types the element size char * (both arms print void sub_8014484(unsigned int *a0,unsigned int a1,char *a2,void *a3)); under cycles it states that type and the constant takes the pointer vote, which resolves to the vector-table symbol at 0x4. The value is unchanged. This is the documented cost of any recovered vote (a callee's wrong recovery with nothing at the call site to contradict it)."}
  ]
}
