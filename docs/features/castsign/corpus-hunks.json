{
 "classifier": "docs/features/castsign/hunkclass.py (decl = `unsigned T vN;` -> `T vN;`; sign-cast = `(T)vN` dropped on a re-declared vN; def-widen = an integer cast dropped at the top of `vN = ...;` where vN is declared an integer of the cast's width; anything else is OTHER; a flip none of whose sign-casts appear is counted as a flip that removed no cast)",
 "castbench_full": {
  "arms": "castbench main arm at 5458b7ab5 (== castsign off) vs the final default-on build",
  "binaries": "45: coreutils fmt ls sort du cp tail wc, grep, gzip, diffutils cmp diff diff3 sdiff, tar, find x O0/O2/O2-noinline",
  "files": 45,
  "functions_changed": 114,
  "by_class": {
   "decl": 95,
   "sign-cast": 171,
   "def-widen": 94
  },
  "other_lines": 0,
  "flips_removing_no_cast": 0,
  "before_the_arithmetic_rule": {
   "functions_changed": 203,
   "by_class": {
    "decl": 234,
    "sign-cast": 371,
    "def-widen": 94
   },
   "flips_removing_no_cast": 23,
   "flips_now_left_alone": 139,
   "of_which_print_arithmetic_on_the_variable": 138,
   "of_which_self_update": 69,
   "flips_added_since": 0,
   "note": "all 95 final flips are among the first version's 234",
   "of_which_removed_no_cast": 1
  }
 },
 "extra_corpus": {
  "arms": "--option castsign off vs the default, same final build",
  "binaries": [
   "O2 bash",
   "O2-noinline dash",
   "O2-noinline cf2.elf (ARM Cortex-M)",
   "O2-noinline kmod",
   "O0 bzip2",
   "O0 crontab",
   "O0 od",
   "O0 numfmt",
   "O0 pr",
   "O2-noinline csplit",
   "O2 minigzip",
   "O2 xmlwf",
   "O2 dpkg-query",
   "O0 last",
   "O0 chage"
  ],
  "functions": 8841,
  "files": 15,
  "functions_changed": 41,
  "by_class": {
   "decl": 15,
   "sign-cast": 50,
   "def-widen": 102
  },
  "other_lines": 0,
  "flips_removing_no_cast": 0,
  "variables_json_identical": "15/15 binaries byte-identical"
 },
 "assignment_recheck": "docs/features/castsign/assigncheck.py: every `lhs = (T)rhs;` that became `lhs = rhs;` has lhs declared an integer of T's width and rhs declared an integer: 27/27 (castbench), 16/16 (extra), 4/4 (DWARF), 0 flagged",
 "dwarf_corpus": {
  "arms": "--option castsign off vs the default, same final build",
  "script": "docs/features/castsign/corpus-dwarf.py (unstripped decbench 'compiled/' binaries: every DWARF stack local is mapped typelock|namelock)",
  "binaries": [
   "O0 wc",
   "O0 fmt",
   "O0 sort",
   "O0 gzip",
   "O0 find",
   "O0 bzip2",
   "O0 diff",
   "O0 grep",
   "O2 sort",
   "O2-noinline ls"
  ],
  "functions": 4254,
  "files": 10,
  "functions_changed": 23,
  "by_class": {
   "decl": 8,
   "sign-cast": 8,
   "def-widen": 18
  },
  "other_lines": 1,
  "hand_classified": "gzip O0 gen_codes @0xb76a `len = (unsigned int)tree[n].dl.freq;` -> `len = tree[n].dl.freq;`: def-widen on a register local named `len` (hunkclass.py only recognizes vN names). `len` is not type-locked (not in variables[]); `signedness` already declares it `int` in both arms, and an unsigned short converts to the same int either way",
  "locked_declarations_re_signed": 0,
  "variables_json_identical": "10/10 binaries byte-identical off vs on",
  "assignment_recheck": "assigncheck.py: 4 `lhs = (T)rhs;` became `lhs = rhs;`, 0 flagged",
  "flips_removing_no_cast": 0,
  "history": "the build before the type-lock check re-signed 3 DWARF-locked declarations (wc `chars`, `words`; bzip2 fallbackQSort3 `med`); the lock check restores exactly those"
 },
 "arithmetic_scan": "every flipped variable's function was scanned for + - * << on the variable's name: the only matches (17 castbench, 3 extra) are arithmetic on its sign extension or truncation, `(long)v4 + v5 * 0x1f` where main printed `(long)(int)v4 + v5 * 0x1f` at the same place, which computes the same value in both arms"
}