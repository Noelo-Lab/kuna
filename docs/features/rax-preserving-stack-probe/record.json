{
  "need_id": "rax-preserving-stack-probe",
  "track": "quality",
  "scope": "small",
  "option": "calleescratchbody",
  "div": "DIV-149",
  "status": "implemented-partial",
  "acceptance": {
    "id": "a-ccd85db0e5f2",
    "passed": false,
    "clauses": {
      "exit_code == 0": "PASS",
      "stdout_absent Stack<hex>)[v<n>]": "PASS (the filed defect; 8 hits before, 0 after)",
      "functions[0].error == null": "PASS",
      "functions[0].line_mappings len_gt 7": "FAIL (2)"
    },
    "why_the_last_clause_cannot_pass": "The probe decompiles under --define-function 0x140001000-0x140001043, an extent that cuts the function's own flow (0x140001041 JMP 0x140001053 leaves it). Once the frame index folds to a constant, the eight register-save stores are unread locals and dead-store elimination removes them, leaving `void sub_140001000(void) { sub_1408d0000(); return; }`. Independent control: a byte overlay that replaces the whole calling sequence with a literal `sub rsp,0x5e028` and no call at all (--assert 'bytes 0x14000100c 4881ec28e00500909090909090') produces the SAME empty body, so the collapse is not caused by this change. Keeping a clipped function's frame live-out is a second, independent change in P3 dead code; a prototype seeding every address-tied stack Varnode as consumed at the top of ActionDeadCode did NOT restore them, so the removal is upstream of that seam and is real work, not a one-line guard. On a function whose flow is not cut, the fix is complete."
  },
  "decisions": [
    "The filed hypothesis (callee preservation loses RAX) is UPHELD; the round-5 refutation's MECHANISM is overturned. It read `MOV R11,qword ptr GS:[0x10]` as a CALLOTHER that makes the callee-body walk incomplete. x86-64 SLEIGH lowers a segment override to `GS_OFFSET + disp` (ia.sinc:1117 and 1143), not to the `segment` userop, and the walk was measured complete at 19 instructions with proves_untouched(RAX) true.",
    "The real blocker is calleepreserves' positive-evidence gate body_departs_from_convention: it requires a write to a register the model marks <unaffected>, and __chkstk writes only RSP/R10/R11, which x86-64-win.cspec names in neither list.",
    "Shipped as a separate option rather than a relaxation of calleepreserves so the two evidence clauses stay independently ablatable, matching how calleeretpreserves was split out.",
    "The anti-vacuity mark is the callee's own memory write (its save slot), not an instruction-count threshold: `ret`, `endbr64; ret`, a placeholder and a wrong-address decode all store nothing, which is exactly the set the shipped gate exists to reject."
  ],
  "tests": {
    "stages": "tests/stages/kuna-calleescratchbody.xml (two-pass, 5 assertions, __chkstk verbatim + an endbr64/ret negative control)",
    "unit": "decompiler/crates/kuna-decomp/src/p4_calls/kuna_calleescratchbody/tests.rs (5 tests)",
    "cli_probe": "NOT promoted: `verify --promote` refuses because the need is still open (the shape clause above) and because the probe's target is binary_source=dataset, which CI has no access to. The stages case is the permanent regression test."
  },
  "gates": {
    "make test": "PARITY OK 675/675",
    "make test-stages": "PARITY OK 730/730",
    "make test-cli": "108/108",
    "make check-spec": "green",
    "kuna catalog --check": "catalog OK"
  },
  "sweep": {
    "corpus": "10 x86-64 PEs from the arena, 4,220 comparable functions, decompile-all --json both arms",
    "changed_binaries": 4,
    "changed_functions": 26,
    "functions_losing_a_call": 0,
    "functions_gaining_a_call": 3,
    "lines": "+81 / -2592, where the whole -2592 is one function whose 10 s per-function watchdog landed in one arm only and which is byte-identical without a budget"
  },
  "speed": "nikos_crack_me.exe sub_140023350 (2,537 lines, the corpus's slowest witness): 10.21 s off / 10.11 s on, medians of 3, byte-identical output."
}
