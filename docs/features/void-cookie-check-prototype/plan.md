# Plan

1. Reproduce the faithful PE and prove where the existing exact `/GS` matcher
   loses the saved cookie.
2. Repair nested-phi fixed-point propagation while keeping seedless, unknown,
   and conflicting paths as refusals.
3. Reuse the exact-site locked-void ABI preservation and keep the checker call;
   do not relax generic call modeling.
4. Fold only the exact adjacent literal/checker/return presentation triplet,
   binding the checker line's markup `opref` to the exact P7-marked call,
   requiring a complete standalone call statement, and moving the literal
   assignment's provenance onto the synthesized return.
5. Promote the exact-SHA witness, add the option-off control, and run focused,
   parity, stage, workspace, specification, catalog, and CLI gates.
