{
  "need_id": "arm-neon-zero-initialization",
  "slug": "constspaceload",
  "track": "quality",
  "scope": "small",
  "option": "constspaceload",
  "default": "on",
  "div": "DIV-158",
  "phase": "P3",
  "subphase": "simplification-quiescence",
  "module": "decompiler/crates/kuna-decomp/src/p3_dataflow/kuna_constspaceload.rs",
  "symptom": "ARM NEON zero initialization becomes near-null memory reads (major, 1 instance, crackmes.one 68d40081224c0ec5dcedc2d2 ARM32 ELF trap, _start_main @0x4d4)",
  "witness": {
    "binary": "crackmes.one 68d40081224c0ec5dcedc2d2 bin/trap",
    "function": "_start_main @ 0x4d4",
    "before": "v2 = *(unsigned int *)(0 + 4); v3 = *(unsigned int *)(0 + 8); v4 = *(unsigned int *)(0 + 0xc); v6 = 0; v1 = 0; v5 = 0;",
    "after": "v4 = 0; v1 = 0; v3 = 0; v2 = 0;  (v1/v2 are the two 8-byte halves the vst1 writes)"
  },
  "decisions": [
    "The filed hypothesis ('vector-store expansion may treat a constant vector as a source pointer') was overturned by the round-12 refuter and is confirmed wrong here. The vst1 lowering is innocent: ARMneon.sinc:6026 buildVst1DdList is two clean 8-byte stores through mult_addr with no pointer-typed read. The refuter's own diagnosis -- the pointer is born in the vmov's dynamic const-space export -- is confirmed by the raw p-code: `u0x00024200:16 = #0x0:16` then `u0x00024300:16 = *(const,u0x00024200:16)`.",
    "The refuter named RuleLoadVarnode as the wrong-fix trap and that is exactly right, but the reason it cannot fire is one level earlier than 'size 16 is unrepresentable': RuleLoadVarnode is registered in OPPOOL2 (post-blockstructure), not oppool1, so ActionLaneDivide runs first and there is no 16-byte LOAD left by the time RuleLoadVarnode sees the function. What it sees are the four lane LOADs, and it folds only the one at offset 0 -- which is the `v1 = 0` control the refuter identified.",
    "The lane split is the defect, not the missing fold. In the constant space an address IS the value, so `LOAD(const, v+4)` is the constant 4, not the second word of v. Folding the three offset lanes where they stand (the naive fix) would have printed 4, 8 and 0xc and the acceptance probe's stdout_absent clause would have gone green on wrong output.",
    "Shipped as the general identity instead: `out:N = LOAD(const, p:S)` -> COPY p / SUBPIECE(p,0) / INT_ZEXT p, applied in oppool1 so it precedes ActionLaneDivide and each lane is cut from the value. This also fixes the NON-ZERO case for free, which the widening fix would have corrupted.",
    "Regression evidence against wrongness, as the refuter demanded, is measured and not argued: the vendored fixture and the stage test both carry `vmov.i32 q8,#1` beside `vmov.i32 q9,#0`. With the option on the four lanes are 1,1,1,1; with it off they are the `v1._0_16_` pointer arithmetic. The witness binary cannot detect a truncating fix because its immediate is zero.",
    "A constant pointer is deliberately left to RuleLoadVarnode rather than handled here: its apply_op also clears the spacebase-placeholder trigger a call sets up and calls resolve_spacebase_relative, which a bare COPY would drop. A LOAD whose output is that placeholder is declined for the same reason.",
    "No new Action: this is a Rule in the existing oppool1, so the universal schedule's pass adjacencies (verify_w8x_allowlist) are untouched. It does move the byte-exact list_action_decompile_oracle fixture by one line (the rule renders as a leaf), and the w8 leaf count 279 -> 280.",
    "Corpus sweep hunk classification. The single binary that changes is an AVX-512 static x86-64 keygen; its 2258-line diff is (a) a variable-renumbering cascade and (b) the real change, which is that an AVX-512 mask-register value is now rendered at its true width. `vpcmpd ymm,ymm -> k` compares 8 dwords, so the mask IS 8 bits, and the option-on output says so with an explicit `& 0xff` where the option-off output had an opaque cast off an unfoldable const-space LOAD. Checked against the disassembly (0x445d7d `vpcmpeqb (%rdi),%ymm16,%k0` / `kmovd %k0,%eax` / `test %eax,%eax` is byte-comparison, 32 lanes, and correctly keeps a bare `if (v)`). No statement is deleted -- the file grows by 62 lines, all declarations. Recorded as a rendering change on AVX-512 code, not a no-op."
  ],
  "tests": {
    "stage": "tests/stages/ghdec-constspaceload.xml (2 passes, 5 assertions)",
    "unit": "decompiler/crates/kuna-decomp/src/p3_dataflow/kuna_constspaceload/tests.rs (6 tests)",
    "fixture": "decompiler/crates/kuna-analysis/tests/fixtures/neonimm_arm_le32 (+ .py generator)",
    "promoted_probe": {
      "path": "tests/cli/arm-neon-zero-initialization.json",
      "retargeted_from": "crackmes.one 68d40081224c0ec5dcedc2d2 bin/trap _start_main",
      "retargeted_to": "decompiler/crates/kuna-analysis/tests/fixtures/neonimm_arm_le32 @0x10004",
      "why": "the need's target is binary_source dataset and CI has no dataset; the need record keeps the dataset acceptance so every later B_VERIFY still runs the real witness",
      "verified_on_witness": true
    }
  },
  "gates": {},
  "ablation": {
    "method": "whole-binary `kuna decompile-all`, option off vs default, unified diff of the emitted C",
    "targets": "5 linked binaries from tests/bug-repro (betaflight_STM32F405.elf, faillog, grep, libselinux.so.1, sort) + RE-dataset challenge images",
    "compared": 120,
    "unchanged": 115,
    "changed": 5,
    "skipped": 10,
    "changed_class": "all 3 are the same AVX-512 static glibc strlen/memchr family: a k mask-register value is rendered at its true width (& 0xff for the 8-lane vpcmpd, & 0xffffffff for the 32-lane vpcmpub) instead of an opaque cast off an unfoldable const-space LOAD; no statement deleted",
    "datatests": "0/675, byte-identical"
  }
}