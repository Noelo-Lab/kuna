{
 "opportunity": "gzip -O2 gzip_base_name (0xd290) is `endbr64; jmp last_component`: it forwards its own rdi and returns the callee's rax, and kuna renders `void sub_d290(void) { sub_dfd0(); }` while recovering the callee as `char *sub_dfd0(char *a0)` in the same run. DWARF says `char *gzip_base_name(char const *fname)`. A call's argument trials come from Heritage::guardCalls, one per HERITAGED range, and a register the function only forwards is heritaged nowhere, so the call gets no trial and the function no parameter. df dir_name 0x7550 and tar *_diag are the same shape. The O2 census counts 25 register-forward arity-short functions.",
 "test_name": "tests/cli/passthrough-gives-a-forwarding-function-its-parameter.json + passthrough-off-leaves-the-forwarded-register-unread.json; tests/stages/kuna-passthrough.xml; unit tests in kuna_passthrough/tests.rs",
 "binary": "decompiler/crates/kuna-analysis/tests/fixtures/passthrough_x86_64",
 "selector": "sub_401019",
 "func_addr": "0x401019",
 "option": "passthrough",
 "flag": "pass_through",
 "element_id": 4177,
 "change_kind": "correctness-fix",
 "tier": "core",
 "source_decompiler": "ghidra",
 "inspiration": "gzip -O2 gzip_base_name 0xd290, df -O2 dir_name 0x7550, tar -O2 *_diag (decbench full_run_address_2026-09-11); the locked branch of ActionFuncLink::funcLinkInput (coreaction.cc:1500) materializes a declared parameter's read at the call, which is the seam this reuses for a recovered one; AncestorRealistic::execute (funcdata_varnode.cc:2159) refuses an input Varnode at a call slot",
 "default_on": true,
 "default_value": "on",
 "values": "on|off",
 "destructive": false,
 "rule_change": {
  "adds_argument": "a register R at a direct, unlocked, non-variadic call is an argument when the callee's recovered prototype (protoorder) has a parameter starting at R and the list is an arity claim (RecoveredTypes::arity_sound: all in registers, body does not read the next arg register, does not saturate the register file, canonical storage), the callee's body proves_input(R), the caller did not set the call up as variadic and is not variadic itself, and the value at the call is the function's own input Varnode. Width is the narrowest of the trial, the parameter and the callee's low-justified body read.",
  "adds_return": "when every live RETURN is reached from a direct call with nothing between (<=4 single-pred blocks), every such callee states a non-void return in the same register, the function's output is not locked and no op touches that register, the RETURN reads it and the tail call's result is the function's return value, typed by the callee's recovered return. A callee states a return only where its own body computed one: every live RETURN must hand back a value produced in every byte and on every path (kuna_returnuncomputed::every_return_computes), which is the strict form of the question the uncomputed-half pair repair asks.",
  "mechanism": "claim_untouched_registers (end of ActionFuncLink) registers a trial + a CALL read for an untouched register only at a call no other CALL/CALLIND can precede (first_calls); heritage registers the range at every other call as usual and only guardReturns is kept off it (suppresses_return_trial); check_input_trial_use never scores a trial on a claimed range (claimed_range -> inactive, never no-use); capture (first in build_input_from_trials) keeps unused trials standing on the function's own input with the evidence and retires unused claimed-range trials; extend_pending (last in ActionActiveParam, after calleearityfwd/calleearitylive/calleearitybody) appends the stated registers that follow a final list that is a leading run of the stated one; returns_tail_result scores the RETURN trial; tail_return_type feeds call_output_type_local.",
  "inert_where": [
   "kuna decompile (one function per forked process)",
   "decompile-all narrowed by --addr/--functions",
   "--jobs N",
   "callee in a recursive component under --option protoorder types",
   "an import",
   "--option protoorder off"
  ],
  "only_adds": "the rule runs after every other argument rule and only appends: no call loses an argument it has with the option off (lostargs.py over 26 binaries: call_lost_args=0 param_lost=0; callsite_metrics deleted=0)."
 },
 "benchmark": {
  "metric": "type_match, pinned decbench 625e892, DECBENCH_NO_CACHE=1, KUNA_BIN the same build both arms, --option passthrough on vs off",
  "slices": 444,
  "functions": 10748,
  "perfect_off": 1569,
  "perfect_on": 1593,
  "perfect_delta": 24,
  "moved_onto_perfect": 24,
  "moved_off_perfect": 0,
  "aggregate_off": 3853.89,
  "aggregate_on": 3905.63,
  "improved": 83,
  "worsened": 3,
  "control_byte_identical": "10489 functions, 0 scored differently",
  "worsened_detail": "three rows, all type votes on a parameter that already existed, none an arity move: print_group_list (groups and id O2, .50->.417) username char* -> long and print_stuff (id O2, .071->0) pw_name char* -> long -- the forwarded parameter also reaches the mgetgroups copy, recovered as `long a0`, whose integer beats the char* the caller's other uses give it. The copy_reg trio (cp/ginstall/mv O2-noinline) that was worse before the tightening no longer is.",
  "build": "the tightened head (vararg-tail + no-hole refusals), same binary both arms",
  "history": "before the tightening the same sweep on the rebased tree gave the same +24 with 104 improved and 6 worse; earlier, pre-rebase, 1,524 -> 1,548."
 },
 "arity_counters": {
  "why": "type_match scores variables[] (args+stack+framelayout), never the return type, and a fabricated register arg on a function with no DWARF twin is never a false positive -- so arity/return fabrication is measured against the DWARF twins, off vs on, over every function that gains something.",
  "build": "the tightened head",
  "corpus_a": "444 slices, 8 projects (coreutils grep gzip diffutils bzip2 findutils tar shadow) x O0/O2/O2-noinline -- the decbench measurement corpus",
  "corpus_b": "130 slices, 17 projects DISJOINT from A (openssh-portable e2fsprogs dpkg kmod dash cronie sysvinit base-passwd libacl libbsd libselinux zlib libexpat libedit rsyslog iproute2 gnutls) x O2/O2-noinline",
  "params": {
   "a_rows": 2851,
   "a_confirmed": 2728,
   "a_contradicted": 0,
   "a_no_twin": 123,
   "b_rows": 1495,
   "b_confirmed": 1379,
   "b_contradicted": 0,
   "b_no_twin": 116,
   "both_rows": 4346,
   "both_confirmed": 4107,
   "both_contradicted": 0,
   "both_no_twin": 239,
   "functions_gaining": 2225,
   "params_lost": 0,
   "callsite_args_lost": 0,
   "class_agrees_with_dwarf": 3782
  },
  "params_before_the_tightening": {
   "a_rows": 2910,
   "a_confirmed": 2783,
   "a_contradicted": 4,
   "b_rows": 1813,
   "b_confirmed": 1532,
   "b_contradicted": 159,
   "b_no_twin": 122,
   "b_rate": "9.4% of checkable -- every one openssh, every one the xcalloc/open_safer vararg-tail shape",
   "cost_of_the_tightening": "208 confirmed parameters (55 on A, 153 on B), 5% of the gain"
  },
  "returns": {
   "a_rows": 4087,
   "a_confirmed": 4012,
   "a_contradicted": 6,
   "a_no_twin": 69,
   "b_rows": 1659,
   "b_confirmed": 1446,
   "b_contradicted": 151,
   "b_no_twin": 62,
   "both_confirmed": 5458,
   "both_contradicted": 157,
   "before_the_gate_on_a": "212 contradicted of 4,267",
   "shape_of_the_misses": "one shape and undecidable from the machine code: a source-`void` wrapper that tail-calls a value-returning function compiles to the same `jmp` as a wrapper that returns what it calls (e2fsprogs ext2fs_fast_mark_block_bitmap, openssh blf_key; gzip's char *gzip_base_name is the same code and is right). Concentrated in projects that write many one-line void wrappers: openssh 123, e2fsprogs 24, kmod 2, iproute2 2, every other project 0."
  },
  "control_off_arm": "over 444 slices and 100,198 functions the recovered argument list and return spelling under `--option passthrough off` are identical to the pristine origin/main build's default output",
  "verdict": "the parameter arm is 0 contradicted of 4,107 checkable over 574 slices in 25 projects with nothing ever lost; the return arm is 97.2% confirmed and its 2.8% is the undecidable void-wrapper shape, named in use_when. Default ON.",
  "drivers": ".scratch/armsweep.py + dwarfjoin.py + dwarfproto.py (both arms cached per slice, joined by low_pc following DW_AT_abstract_origin)"
 },
 "corpus_diff": {
  "binaries": 8,
  "build": "the tightened head",
  "which": "O2 coreutils df/fmt/ls, O2 gzip, O2-noinline coreutils sort, O2-noinline grep, O2 shadow useradd, O2 tar",
  "functions_changed": 276,
  "line_classification": {
   "a type or spelling changed on a line": 528,
   "a call gained arguments": 269,
   "a RETURN now hands back the tail call's result": 172,
   "a declaration for the captured result": 35,
   "a void `return;` became `return <tail call>;`": 30,
   "the tail call's result is captured": 26,
   "a declaration moved with the renumbered variables": 25,
   "comment/blank": 8,
   "brace": 1
  },
  "unexplained": "2 lines in 2 functions, both hand-read and both a documented effect the textual pairing misses: grep O2-noinline 0x1c0c0 `*a0 = *a0 + 1;` became `a0->field_0x0 = a0->field_0x0 + 1;` (the gained argument typed the pointer struct_2 *), and tar O2 0x255c0 `sub_45230(a0,a3); return;` became `return sub_45230(a0,a3);`",
  "every_class_is_the_documented_effect": "a function gains a parameter or a tail-call return, calls gain (never lose) arguments, and types propagate from those.",
  "history": "before the tightening: 290 functions over the same 8 binaries, every line explained; and over 26 binaries at commit 7da4afecf, 1,179 functions with lostargs call_lost_args=0 param_lost=0"
 },
 "gates": {
  "tested_commit": "this branch's head (feat/passthrough-on), rebased on origin/main 9e07ab931",
  "make_test": "PARITY OK, datatests: 675/675 assertions passed",
  "make_test_stages": "PARITY OK, datatests: 1311/1311 assertions passed",
  "make_test_cli": "tests/cli: 235/235 passed",
  "kuna_catalog_check": "catalog OK: documents exactly the registered kuna options",
  "counters_check": "no drift: every hard-coded site agrees with the derived truth",
  "options_md_fresh": true,
  "phase_catalog_fresh": true,
  "check_spec": "OK lenient + strict",
  "make_rust_test": "pending re-run after the tightening (lane script)",
  "full_ci_label": true
 },
 "catalog_counts": {
  "settables": 226,
  "core_transform_analysis": [
   71,
   91,
   64
  ],
  "live_value_present_for": 94,
  "stage_corpus_files": 330,
  "element_id": 4177
 },
 "preset_decision": "ON by default; the UNEVALUATED entry in modes.rs is removed and nothing is added to AGGRESSIVE_OVERRIDES (a default-on option needs no override).",
 "files": [
  "decompiler/crates/kuna-decomp/src/p4_calls/kuna_passthrough.rs (+tests)",
  "decompiler/crates/kuna-decomp/src/p4_calls/kuna_protoorder.rs (RecoveredTypes::arity_sound + output)",
  "decompiler/crates/kuna-decomp/src/p4_calls/kuna_calleedeadarg.rs (live_input_width)",
  "decompiler/crates/kuna-decomp/src/p4_calls/coreaction_protos.rs (ActionFuncLink claim, ActionActiveParam extend_pending, ActionReturnRecovery hooks)",
  "decompiler/crates/kuna-decomp/src/p4_calls/funcdata_callsite.rs (claimed_range arm in check_input_trial_use; capture in build_input_from_trials)",
  "decompiler/crates/kuna-decomp/src/p5_types/coreaction_infertypes.rs (tail_return_type)",
  "decompiler/crates/kuna-decomp/src/substrate/funcdata.rs (claim/vararg/variadic state)",
  "tests/stages/kuna-passthrough.xml, tests/cli/passthrough-*.json",
  "decompiler/crates/kuna-analysis/tests/fixtures/passthrough_x86_64(.s)",
  "decompiler/crates/kuna-decomp/src/p3_dataflow/heritage.rs (guardReturns skips a claimed range)",
  "docs/features/passthrough/lostargs.py (corpus lost-argument detector)",
  "decompiler/crates/kuna-decomp/src/p4_calls/kuna_returnuncomputed.rs (every_return_computes + computes_everywhere, +4 unit tests)",
  "decompiler/crates/kuna-console/src/project.rs (park_recovered takes the callee's Funcdata)",
  "docs/features/passthrough/dwarf-confirmation.md",
  "decompiler/crates/kuna-decomp/src/p4_calls/kuna_varargtail.rs (+tests) -- the vararg-tail refusal",
  "decompiler/crates/kuna-decomp/src/p4_calls/kuna_passthrough.rs (no_hole_before; set_up_as_variadic reads the writing instruction's own ops as its own)"
 ],
 "speed": {
  "method": "interleaved min-of-15, SAME build, whole-binary `kuna decompile-all --json --max-fn-seconds 120`, `--option passthrough off` against the new default; the option is inert under single-function `kuna decompile`",
  "measured": "2026-09-23, contended (another campaign lane was running its own min-of-15 sweep); min-of-15 takes the least-contended sample",
  "O2": {
   "coreutils": {
    "fmt": {
     "off_s": 4.494,
     "on_s": 4.378,
     "delta_pct": -2.59
    },
    "ls": {
     "off_s": 15.713,
     "on_s": 14.668,
     "delta_pct": -6.65
    },
    "sort": {
     "off_s": 16.288,
     "on_s": 15.803,
     "delta_pct": -2.98
    }
   },
   "bash": {
    "bash": {
     "off_s": 101.103,
     "on_s": 103.218,
     "delta_pct": 2.09
    }
   }
  },
  "worst_delta_pct": 2.09,
  "budget_pct": 5.0,
  "within_budget": true,
  "samples": 15
 },
 "default_flip": {
  "round": "G (second pass, after an adversarial review on a corpus outside the 8 projects)",
  "base": "origin/main 9e07ab931",
  "what_changed": "default off -> on; recovered_output gates a stated callee return on every_return_computes; passthrough declines a vararg-tail parameter (kuna_varargtail) and a claim that would fill a hole (no_hole_before); set_up_as_variadic no longer counts the writing instruction's own reads",
  "a_make_test": "PARITY OK 675/675",
  "b_make_test_stages": "PARITY OK 1311/1311 (tests/stages/kuna-passthrough.xml already pins `option passthrough off` in pass 1; no assertion moved, no baseline re-recorded)",
  "c_make_test_cli": "235/235; ONE probe re-pinned: protoorder-types-keeps-a-float-pointee-bitwise expects `double s3(...)`, which the fixture's own source says s3 returns. protoorder-types-never-moves-an-arity is back on the DEFAULT command -- the rule was tightened so `overrec` keeps its one parameter",
  "d_typesweep": "444 slices / 10,748 functions, same build, new default vs `--option passthrough off`: PERFECT 1,569 -> 1,593 (+24), aggregate 3,853.89 -> 3,905.63, 24 moved onto perfect, 0 off, 83 improved, 3 worse; control 10,489 byte-identical-variables functions, 0 scored differently",
  "d_worse_rows": "three, all a parameter the function already had being retyped by the callee that now receives it, none an arity move: print_group_list (groups O2, id O2) a0 char* -> long, print_stuff (id O2). The copy_reg trio that was worse before the tightening no longer is.",
  "f_corpus": "8 binaries (fmt/ls/df O2, sort O2-noinline, gzip O2, grep O2-noinline, tar O2, shadow useradd O2): 276 functions change; every changed LINE is a documented effect bar two, both hand-read (grep 0x1c0c0 `*a0` -> `a0->field_0x0` on a newly typed pointer, tar 0x255c0 `sub_45230(a0,a3); return;` -> `return sub_45230(a0,a3);`)",
  "g_modes": "the UNEVALUATED entry is removed -- a default-on option needs no aggressive override",
  "dwarf": "docs/features/passthrough/dwarf-confirmation.md",
  "dwarf_summary": "TWO corpora, 574 slices, 25 projects. Parameters 4,346 rows / 4,107 confirmed / 0 contradicted / 239 no twin, 0 lost. Returns 5,746 rows / 5,458 confirmed / 157 contradicted / 131 no twin (6 contradicted on the 8-project corpus, 151 on the disjoint one, all the undecidable void-wrapper shape). Before the tightening the disjoint corpus contradicted 159 of 1,691 checkable parameters.",
  "d_typesweep_rebased": "the +24 is unchanged by the tightening and by #711 calleevote landing under it: 1,524 -> 1,548 pre-rebase, 1,569 -> 1,593 rebased, 1,569 -> 1,593 rebased and tightened",
  "rebase": "rebased onto 9e07ab931; one conflict, in docs/spec/04-calls-and-prototypes.md, where this PR's `**Default.** On.` paragraph and #711's new `calleevote` section land at the same anchor -- resolved by keeping both"
 }
}