{
  "opportunity": "test_decompiling_missing_function_call::main",
  "test_name": "test_decompiling_missing_function_call",
  "binary": "/home/mahaloz/github/angr-dev/binaries/tests/x86_64/decompiler/adams",
  "selector": "main",
  "func_addr": "0x8e40",
  "angr_version": "9.2.213",
  "option": "switchguardbound",
  "flag": "switch_guard_bound",
  "element_id": 4022,
  "change_kind": "opt-in-tool",
  "source_decompiler": "angr",
  "inspiration": "test_decompiling_missing_function_call; angr jump-table / VSA guard bounding; main",
  "default_on": false,
  "ablation_changed": 0,
  "parity": "OK",
  "speed_off_ms": 192.9,
  "speed_on_ms": 259.06,
  "speed_delta_pct": 34.3,
  "speed_samples": 5,
  "speed_budget_pct": 5.0,
  "speed_within_budget": false,
  "speed_forced_off": true,
  "decisions": [
    {
      "question": "Scope (small vs large) + hook point + default + whether to fix analyzeGuards",
      "by": "decider subagent (Plan)",
      "decision": "scope: small. It is one gated JumpBasic model-extension method (kuna_try_guard_bound_table) + one new module owning the option/arch-flag, structurally identical to the switchmodbound (GH-9191) precedent: no new pass TYPE, no S7/structuring touch (the BRANCHIND->CALLIND collapse happens in truncate_indirect_jump BEFORE structuring; bound the table and the switch survives), 3 ported-core anchor files (jumptable.rs, architecture.rs, seams.rs) + option-registry files + 1 new module. Hook point CONFIRMED: recover_model_basic's `jrange.size > maxtablesize` failure branch, immediately after the kuna_try_modulo_bound_table check (path_meld/jrange/varnodeIndex already populated; independent `if` branches so neither gate shadows the other). Default-OFF opt-in REGARDLESS of ablation: the guard-to-index correspondence is a control-flow heuristic asserted across a memory round-trip it cannot prove in dataflow, whose worst case is WRONG output (collapsed/spurious cases on an unrelated jump), not just missing output. Do NOT touch analyze_guards: it is verbatim-ported core (deliberately conservative on partial functions via usenzmask = !is_partial); the gated downstream rebound keeps `gate off => upstream byte-identical` (preserved here: 675/675 unchanged) and avoids 675-datatest parity risk. MANDATORY: also wire the ArchSeam flag (seams.rs + build_arch_handle copy) — recover_model_basic runs off the seam handle, so setting the flag only on the real Architecture would never fire (the documented archseam-flags trap; switchmodbound proves it is required)."
    },
    {
      "question": "How to extract the table bound N when the guard is in unsimplified flag form, register-reused (idx and m at the same storage), and the simplifier rewrote `idx > 0x36` into `idx > 0x35 && m != 0x75`",
      "by": "implementer (after instrumenting the failing recovery)",
      "decision": "Constant-aggregation of comparison operands is insufficient (it yields N=0x36, missing the boundary 'u' case at idx 0x36 carried by the `m != 0x75` term). Instead EVALUATE the guard boolean as a function of a candidate index varnode v: pick the meld index var, build a linear-offset map for its register-reused siblings (m = v + 0x3f, from idx's INT_ADD def), concretely evaluate the boolean for v = 0,1,2,... and take N = the first v whose routing flips from v=0's. GCC normalizes the table to start at index 0 (sub LOW), so the switch values form a contiguous prefix [0, N); this correctly composes the `idx > 0x35` and `m != 0x75` terms to N = 0x37 (55). Result on the target: 6 cases + default recovered, fileno present once, no computed call."
    }
  ]
}
