# w10-proto-recovery
verdict: ACCEPT-WITH-LOSSES
verifier: claude-opus-4-8 (independent verifier, round 1)
date: 2026-06-13
item: rport/w10-proto-recovery (Funcdata.funcp -> fspec::FuncProto + proto-recovery actions: ActionPrototypeTypes, Heritage::guardReturns(+Overlapping), ActionReturnRecovery + AncestorRealistic/ancestorOpUse/onlyOpUse; closes the LOSS-042(d) param-trial sliver)

## Gate
- `cd /tmp/kuna-proto/rust && cargo test --workspace` -> **3157 passed, 0 failed** (was 3155 pre-review; +2 verifier adversarial tests).
- B4 gate `tests/proto_b4.rs::boolless_return_recovers_accumulator` -> **ok** (RETURN in0 stripped to `#0x0`; in1 = `ACC:1` recovered; alive ops 9->7; no CPUI_PIECE survives).
- Updated `tests/verify_w6_s4_coreaction_protos.rs::proto_action_apply_change_signal_contract` -> **ok**.
- `cargo clippy -p kuna-decomp` and `-p kuna-console` -> **0 warnings**. New code has no `todo!`/`unimplemented!`/`HashMap`/`HashSet`/`sort_unstable`/`partial_cmp`.
- C++ oracle UNTOUCHED: `git diff rust-port...rport/w10-proto-recovery` touches ZERO files under decompiler/ or specs/ (only rust/). `python -m kuna.run_tests --all --baseline docs/baseline.json` -> **207/207 unit, 675/675 datatest, PARITY OK**.

## Funcdata.funcp type swap (FOCUS 1) — correct, consumers faithful
- `Funcdata::funcp` went `seams::FuncProto` (empty placeholder) -> `fspec::FuncProto::new()`; `activeoutput: Option<ParamActive>` added; ctor updated. `get_func_proto`/`get_func_proto_mut` borrow it. `init_active_output`/`clear_active_output`/`take_active_output`/`restore_active_output`/`get_active_output(_mut)` are a faithful port of `Funcdata::initActiveOutput` (funcdata_varnode.cc:603: `ParamActive(false)`, maxdelay=`getMaxOutputDelay()` capped to 3 if >0) + `clearActiveOutput`, with a defensive model-less guard (maxdelay 0). `op_heritage`'s reset now calls `clear_active_output` (matches funcdata.cc clearActiveOutput). The take/restore pair is a borrow-checker accommodation (C++ holds `activeoutput` as a member and mutates it concurrently with the IR); it is sound — see F3 for the one observable consequence, which is dead on the realized path.
- `FuncProto::has_store()` + the `is_output_locked` no-store guard are faithful degradations for the W4 `setScope` seam (no ProtoStoreSymbol yet): no store -> output is void/unlocked, exactly the un-recovered default. `derive_output_map` forwards to `model->deriveOutputMap` (fspec.hh:1501).

## Faithfulness of each un-seamed body (FOCUS 2) — line-by-line vs C++
- **ActionPrototypeTypes::apply** (coreaction.cc:4843) -> coreaction_protos.rs: model-select (`evalfp_current ?: defaultfp`, set if !modelLocked && !hasMatchingModel) EXACT; RETURN-in0 strip to `newConstant(getIn(0)->getSize(),0)` EXACT; output-locked-vs-`initActiveOutput` branch EXACT (unlocked path on the recovery). Locked-input/truncated-space/`prepareThisPointer` branches correctly seamed (no locked proto / no truncated stack / no this-ptr on the 8051 default path). DEVIATIONS: bumps `count` where C++ never does (F1); the strip loop omits the C++ `if(op->isDead())continue` (F2). Both observationally inert (proven).
- **Heritage::guardReturns** (heritage.cc:1653) -> heritage.rs: `active != NULL` gate (+ a defensive `has_model()`), `characterizeAsOutput` -> contained_by->guardReturnsOverlapping / else-if !no_containment->registerTrial + append-invn(setActiveHeritage)-to-each-RETURN(skip dead/halt, insert at numInput). EXACT. The persist branch correctly returns 0 (fl&persist==0 for an unrecovered register).
- **Heritage::guardReturnsOverlapping** (heritage.cc:1610) -> heritage.rs: getBiggestContainedOutput gate, registerTrial(truncAddr,vData.size), `offset = (int4)(vData.offset - addr.getOffset())` with big-endian flip, SUBPIECE(invn, const offset) before each RETURN (skip dead/halt), newVarnodeOut(vData.size,truncAddr), setActiveHeritage, opInsertInput. EXACT, including op construction order. Wrapping handled: `wrapping_sub(...) as i64 as int4` == C++ `(int4)(uintb-uintb)` low-32-as-signed.
- **ActionReturnRecovery::apply** (coreaction.cc:1954) -> coreaction_protos.rs: getActiveOutput gate; per-RETURN(skip dead/halt) per-trial(skip checked): `execute(op,slot,trial,false) && ancestorOpUse(maxancestor,vn,op,trial,0,0) -> markActive`, `count += 1` per trial (incl. the vn-None case); finishPass; numPasses>maxPass->markFullyChecked; fully-checked -> deriveOutputMap + buildReturnOutput per RETURN + clearActiveOutput + count. EXACT. The split of trial flag-mutation into `apply_trial` (C++ `execute` mutates `trial` in place; Rust collects ind_create_formed/cond_exe_effect/ancestor_realistic/ancestor_solid and applies after) is faithful — the flags land regardless of ancestorOpUse, as in C++.
- **buildReturnOutput** (coreaction.cc:1880) -> `build_return_output`: in0 kept first, used trials in slot order (break on !isUsed / slot>=numInput), (kuna)GH-6990 return_single resize(2), easy <=2 case opSetAllInput. EXACT. The 2-piece-PIECE and many-piece concat branches are seamed (need `constructJoinAddress`/translate-on-handle) and truncated to 2 — unreached on the single-register recovery path (no PIECE survives, asserted).
- **ancestorOpUse / onlyOpUse** (funcdata_varnode.cc:1963/1851) -> faithful transcription: every opcode arm, the INDIRECT(indirect+1)/MULTIEQUAL(mark-trim, recurse-all)/COPY(internal/incidental)/PIECE(offset 0 vs in1.size)/SUBPIECE(remFormed on INT_REM/SREM at newOff==0, overlap==newOff) recursion; onlyOpUse's varlist-mark BFS, the BRANCH/LOAD/STORE/CALL(double-use)/RETURN(same-slot-skip, active-output alt-path)/PIECE(concat_high)/SUBPIECE(lsb_truncated) classification, persist->false, mark set/clear. Signedness/loop bounds clean (usize varlist index, int4 offset matches C++).
- **AncestorRealistic** (funcdata.hh:672, funcdata_varnode.cc:2043) -> faithful: State/from_subpiece(offset accumulate), checkConditionalExe(sizeIn==2 && solid.sizeOut==1), enterNode (mark/written/input(unaffected/persist/directwrite) + INDIRECT(indirectCreation->indCreateFormed, indirectZero->failkill; non-store->retaddr-fail), SUBPIECE/COPY internal-vs-minimal-traversal, MULTIEQUAL multiDepth, PIECE size>trial truncation), uponPop (MULTIEQUAL markSolid/markKill, seenSolid/seenKill/allowFailingPath/condExe), execute (input-not-realistic-unless-condExe short-circuit, mark cleanup, pop_success=(true,false)/pop_solid=(true,true)). The one transcription gap is the killedbycall->pop_fail branch (F3).

## RETURN recovery is REAL (FOCUS 4) — proven, not a boolless shortcut
- The recovered output is gated on the trial-machinery: `fillinMapFallback` (fspec.cc:1706 "Only actives are ever marked used") marks a trial `used` ONLY if `isActive()`, and `isActive()` is set ONLY by `markActive()` in ActionReturnRecovery's `execute && ancestorOpUse` test. Verifier adversarial test T3 confirms: `derive_output_map` on a registered-but-not-active trial marks NO trial used. So the ACC that appears as RETURN in1 came from real ancestor-realism + the cspec `<output><pentry>` decode, not a fabricated value. The direction matches the C++ oracle (datatest boolless green; B4 snapshot adds ACC).
- **killedbycall SEAM is genuinely inert, not load-bearing**: the ACC output trial IS killedbycall (register space). The verifier patched `enter_node` to restore the C++ `if(trial->isKilledByCall()) pop_fail` (threading the flag through `execute`) and re-ran the gate: ACC is STILL recovered and the patched branch never fires — the ACC ancestry never reaches an INDIRECT-non-store node. Reverted. The recovery does not depend on dropping the check.
- **count bumps are not load-bearing**: removing both `self.base.count += 1` from ActionPrototypeTypes keeps the gate green (ACC still recovered). They are gratuitous (C++ has none) but inert.

## NO SPECIAL-CASING (FOCUS 3) — clean
Grepped the full src diff for `boolless`/`ACC`/hardcoded address/`get_name()==`/`name()==`/`=="main"`/`funcaddr==`/`entry==`. The ONLY matches are: `c.get_name() == nm` (XML child lookup in the cspec decoder — element-name, general over any processor's `<default_proto>`), and "8051 ACC" in comments/doc. The decode path is fully spec-driven (any cspec); recovery dispatches on `op->code()`, `characterizeAsOutput`, and trial activity — never on a function name/address/fixture. The cspec ACC `<pentry>` is what makes the 8051 default model recover ACC; the same machinery fires for every function under that model. REAL recovery.

## hunt list
- Signedness: clean. `(size - vData.size) - offset` (big-endian) and `(int4)getOffset()` casts match C++ explicit `(int4)`; no int4-vs-size loop hazard (loops collect `iter_code`/`iter_alive` vecs then index).
- Integer widths: clean. uintb->u64, int4->i32, VarnodeData.size(u32)->int4 — sizes small, no promotion divergence.
- Wrapping: clean. `vdata.offset.wrapping_sub(addr.get_offset())` is the one wrap-prone site (C++ unsigned subtract then `(int4)`); ported with `wrapping_sub`. `parse_int` hex/dec uses checked `from_str_radix`/`parse` (Option) — no wrap. The big-endian `size - vData.size as int4` cannot underflow (getBiggestContainedOutput guarantees vData ⊆ range).
- Comparator totality: n/a (no new comparators; ParamActive::sort_trials reuses the existing ParamTrial order).
- Iteration-order provenance: clean. RETURN ops via `iter_code(CPUI_RETURN)` = the intrusive `returnlist` insertion order = C++ `beginOp(CPUI_RETURN)` per-opcode list; alive via `iter_alive` insertion order; cspec `<pentry>`/`<input>`/`<output>` children walked in document order (== C++ decoder). NO HashMap/HashSet in ported logic. proto_models is a BTreeMap.
- Off-by-one/do-while/reverse: clean. buildReturnOutput break-on-!isUsed/slot>=numInput matches C++; the AncestorRealistic enterNode minimal-traversal `do/while` (C++ 2115-2122) is ported as a `while let`+initial step keeping the at-least-once visit; numgroup advance `getAllGroups().back()+1` matches parsePentry.
- Erase-while-iterating: clean. No erase-during-traversal in the new bodies; return-op loops collect-then-act. onlyOpUse's varlist grows during the walk (index loop, like C++ `for(i=0;i<varlist.size();++i)`), faithful.
- Exception->Result partial-state: clean. `decode_default_proto` failure -> name-only fallback (graceful, recorded LOSS-136e); guard/recovery bodies use `let _ =` on op-builder Results (the merged-tree convention) with no mid-mutation early-return that would strand state differently from C++.

## findings
- F1 (minor / accepted, LOSS-136a): `ActionPrototypeTypes::apply` bumps `self.base.count` on the RETURN strip and on `init_active_output`; C++ `ActionPrototypeTypes::apply` (coreaction.cc:4843-4939) contains NO `count += 1` (always returns count 0). Inert: onceperfunc action + restart-group keys on `hasRestartPending()` not count + the pipeline driver does not loop on the returned count. Proven by removing both bumps (gate stays green). The prior test was retargeted to allow prototypetypes==1 (honest documentation, other 8 actions still pinned at 0) — NOT a weakening that hides a wrong rewrite.
       cpp: decompiler/cpp/coreaction.cc:4843
       rust: rust/crates/kuna-decomp/src/coreaction_protos.rs (ActionPrototypeTypes::apply count bumps)
- F2 (low, latent, LOSS-136d): the RETURN-strip loop omits the C++ `if(op->isDead()) continue` (coreaction.cc:4864). No dead RETURN ops exist when ActionPrototypeTypes runs (pre-heritage/pre-deadcode), so unreachable on the live path.
       cpp: decompiler/cpp/coreaction.cc:4862-4869
       rust: rust/crates/kuna-decomp/src/coreaction_protos.rs (RETURN-strip loop)
- F3 (low, accepted, LOSS-136b/c): (b) `AncestorRealistic::enter_node` omits the INDIRECT-non-store `if(trial->isKilledByCall()) pop_fail` (funcdata_varnode.cc:2100); the ACC trial IS killedbycall but its ancestry never reaches that node (verifier patch experiment: restoring the branch leaves recovery unchanged). (c) `only_op_use` reads `self.get_active_output().is_some()`, which is `None` during ActionReturnRecovery (container moved out by `take_active_output`); dead because opmatch is always a RETURN there, so the `else if activeoutput` arm is never reached. Both inert on the realized path; could matter for a different return register or a non-RETURN-opmatch (ActionActiveParam) caller.
       cpp: decompiler/cpp/funcdata_varnode.cc:2100, 1900
       rust: rust/crates/kuna-decomp/src/funcdata_varnode.rs (enter_node INDIRECT arm; only_op_use active_output read)
- F4 (nit, LOSS-136e): `decode_default_proto`/`decode_pentry_list` port a SUBSET of `ParamListStandard::decode` (pentry lists + numgroup + finish_decode); no `<group>`/`<modelrule>`/pointermax/splitFloat-resource-section/spacebase/autoKilledByCall-effect. Sufficient for the 8051 plain-pentry default model; other cspecs would decode incompletely and fall back to the name-only model.
       cpp: decompiler/cpp/fspec.cc:1453-1512
       rust: rust/crates/kuna-decomp/src/architecture.rs (decode_default_proto / decode_pentry_list)
- F5 (nit): the new `as int4`/`as i64`/`as u64` casts in heritage.rs/architecture.rs lack the `// cast:` justification comment the protocol requests. Each is provably faithful to an explicit C++ `(int4)`/promotion (traced above). Consistent with the crate's existing inconsistent cast-comment usage. Not a blocker.

## adversarial tests (added, land with the verdict, all green)
rust/crates/kuna-decomp/tests/verify_w10_proto_recovery_adversarial.rs (marked w10-proto-recovery):
- `w10_boolless_return_recovery_is_real_and_seam_inert` — independent op-walk: T4a RETURN in0 == `#0x0` constant; T4b in1 resolves via the translate register table to `ACC` (the cspec output pentry, from the trial machinery); T1 the recovered storage is IPTR_PROCESSOR (so the registerTrial killedbycall predicate is live — the SEAM's inertness is the reason it is safe, not a dropped check); T4c no CPUI_PIECE survives (single-register, multi-piece branch not taken).
- `w10_register_trial_killedbycall_boundary_and_derive_needs_active` — T2 `register_trial` marks a register trial killedbycall but a spacebase (stack) trial NOT (fspec.cc:1965 SPACEBASE guard); T3 `ProtoModel::derive_output_map` over a registered-but-NOT-active trial marks NO trial `used` (proves recovery is gated on the ancestor-realism `markActive`, not fabricated).
Plus the verifier's transient patch experiments (reverted): restoring the killedbycall `pop_fail` and removing the ActionPrototypeTypes count bumps — both keep tests/proto_b4.rs green, proving neither the SEAM nor the count divergence is load-bearing.

## losses
- LOSS-042 (pre-existing): family (d) — `checkCallDoubleUse`/`onlyOpUse`/`ancestorOpUse`/`initActiveOutput`/the whole `AncestorRealistic` class — is CLOSED by this wave (ported + adversarially tested; faithful per the body review above). `check_call_double_use` itself stays a `false` stub (no FuncCallSpecs / num_calls==0 on the recovery path) — the only sliver of (d) still seamed, folded into LOSS-136.
- LOSS-136 (appended): the residual seams + the change-signal divergence — F1 (count, a/inert), F2 (dead-op skip, d/latent), F3 (killedbycall b + active-output-during-take c, inert), F4 (cspec-decode subset, e). All non-output-affecting on the live path; none special-cased.
