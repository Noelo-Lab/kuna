# w7-s6-merge
verdict: ACCEPT-WITH-LOSSES
verifier: claude-opus-4-8 (independent verifier, round 1)
date: 2026-06-13
gate: `cd rust && cargo test -p kuna-decomp` -> lib 1254/1254 pass (incl. 18 in-module merge tests); `verify_w7_s6_merge` 6/6 pass; `catalog_*`/`corpus_bootstrap` 4/5 — the ONLY failure is `corpus_bootstrap::ldefs_pspec_cspec_parse_matrix` ("Could not find .sla file for x86:LE:64:default"), a missing gitignored build artifact (`make specs`), pre-existing and wholly off the merge/cover path (identical to the w7-s6-variable-cover gate). Protocol clippy `cargo clippy -p kuna-decomp -- -D warnings` clean; the `--tests` profile surfaces pre-existing lint debt in unrelated files (constseq/double/heritage/prefersplit/typeop/unionresolve + several older verify_* tests), none in merge.rs or verify_w7_s6_merge.rs.

blob-sha drift: NONE — `merge.cc c9023e8efb8f74813514ab0ee136bf4fddd54a0e` and `merge.hh 4ff5c248020e96c23701301f2f049e56e2273924` both match the checklist `cpp_blob_sha` exactly (`git hash-object`). (`cargo run -p port-audit` unavailable: the `port-audit` crate is absent in this worktree — environment gap, not a finding; same as the dependency review.)

Scope note: the item's modules are merge.cc/merge.hh only. The branch (`worktree-wf_f5333dc7-250-19`) also carries the already-verified dependency `w7-s6-variable-cover` (commits 9ba454d + 833ecb9: cover.rs/variable.rs net-new); the merge item itself is exactly commit `c4659a5` = merge.rs (3132 lines) + a one-line `pub mod merge;` in lib.rs. The `MergeIntersect` cover-intersection driver (variable.cc lineage) is net-new in *this* commit because it is owned by `Merge::testCache`, so it is in scope as the wave's named concern; the underlying Cover primitives (intersect/intersect_list/compareTo/BTreeMap order) were verified clean in the dependency review.

## hunt list
- **Signedness**: clean. `find_front`'s `max = list.len() as int4 - 1` reproduces the C++ unsigned `list.size()-1` -> `int4` truncation to `-1` on an empty list (merge.cc:47; merge.rs:103); the `min<max`/`min>max` signed compares match. `mark_redundant_copies`' `for(int4 j=i-1;j>=0;--j)` is ported with signed `int4 j` so `j -= 1` to `-1` exits cleanly, no usize underflow (merge.cc:1255; merge.rs:1952-1964). All container-index loops use `int4` against `int4`-cast lengths matching the C++. Pinned by `w7_s6_merge_find_front_boundaries`.
- **Integer widths**: clean. `int4`/`uintb`/`uintm` mappings preserved. The SUBPIECE constant `val = op->getIn(1)->getOffset()` (uintb) truncated to `int4` (merge.cc:1516) is `... as int4` (merge.rs:2170). `op_order` is `uintm` (=u32), matching `SeqNum::getOrder()`. No `int4*int4`-into-`intb` promotions exist in merge.cc.
- **Wrapping**: clean (with documented debug-only divergence class). The two `(int4)(vn->getOffset() - vn2->getOffset())` partial-overlap offsets (merge.cc:524, :559) are `wrapping_sub(...) as int4` with explicit `// cast:` comments (merge.rs:1726-1728, 1775-1777) — the truncating wrap is the spec. The within-group `int4 - int4` piece-offset subtractions (`piece_offset(inter)-base_off` etc., merge.rs:850/863/873/1174/1383) mirror the C++ `int4` arithmetic exactly; on a *non-physical* negative result they would debug-panic where C++ wraps (UB-but-wraps) — the established LOSS-085/086/087 debug-panic-vs-C-wrap class, unreachable for physically-ordered overlap-group pieces; not separately blocked.
- **Comparator totality**: clean. `compare_high_by_block` (merge.hh:152-174 -> merge.rs:717-742) is a strict-weak order: cover `compareTo`, then first-instance addr, then def-op addr, with the null-def asymmetry (`def1==0 -> def2!=0`; `def1!=0,def2==0 -> false`) ported field-for-field; `Address::Ord` is total. `compare_copy_by_in_varnode` (merge.cc:1045 -> merge.rs:746-758) chains createIndex -> block index -> unique `SeqNum::order` (no ties). No float comparators anywhere. `BlockVarnode::less` is a single-key `index<index` (stable-sorted). Pinned by `w7_s6_merge_compare_high_by_block_tiebreaks` (incl. the tie that is `<` in neither direction) and `w7_s6_merge_compare_copy_by_in_varnode_chain`. See F2 for the sort-stability note.
- **Iteration-order provenance**: clean. `MergeIntersect::intersection`/`block_intersection` walk `blockisect` and instances in the C++ order (intersect_list ascending block order via `Cover`'s `BTreeMap`; instance index order). `mergeOpcode` walks `bblocks` in linear block index order then `block_ops` (merge.cc:335-337 -> merge.rs:1196-1199). `mergeMarker`/`mergeAdjacent`/`markInternalCopies` walk `ops_alive()` (the C++ `beginOpAlive..endOpAlive` dead-code list order — a Funcdata seam returning a Vec; the order provenance is the Funcdata impl's responsibility, recorded under LOSS-112). No `HashMap`/`HashSet` in merge.rs (grep clean). `merge_by_datatype` groups by `Rc::ptr_eq` type identity preserving discovery order (see Erase-while-iterating). Pinned by `w7_s6_merge_intersection_*`.
- **Off-by-one / do-while / reverse iterators**: clean. `find_front`'s binary search returns the FIRST of a run of equal block indices (`>=` keeps `max=cur`), verified at the run boundary. `collect_inputs`' `for(;;){ ...; op=op->previousOp(); if(op==0||code!=INDIRECT) break; }` do-forever-with-tail-break is ported as `loop { ...; match op_previous { Some(prev) if INDIRECT => op=prev, _ => break } }` keeping the at-least-once body (merge.cc:789-801 -> merge.rs:1546-1569). `mergeRangeMust`'s `vn=*startiter++` first-then-loop is ported as `iter.next()` then `for &vn in iter` (merge.cc:307-310 -> merge.rs:1643-1647). `merge_op`'s `nexttrim` trim-and-retest loop preserves the `nexttrim==max -> trimOpOutput` terminal (merge.cc:749-760 -> merge.rs:1429-1448). `mark_redundant_copies`' reverse `i=size-1..1`, `j=i-1..0` ported with signed int4 (merge.cc:1252-1264 -> merge.rs:1945-1966). Pinned by `w7_s6_merge_find_front_boundaries`.
- **Erase-while-iterating**: clean. `mergeByDatatype`'s C++ `list<HighVariable*>` dance — pull the first element, then scan the REST in order erasing same-type elements (`highlist.erase(hiter++)`) while advancing past non-matching (`++hiter`), leaving them in place — is ported with a `VecDeque`: `pop_front` the head, drain the remainder splitting matches into `highvec` (push order) and non-matches into `rest` (push_back order), then `remaining = rest` (merge.cc:383-400 -> merge.rs:1258-1273). Both the matched type-group AND the leftover list keep discovery order; groups are processed one-whole-type-at-a-time. `hideShadows`' in-place `singlelist[j]=(Varnode*)0` is ported as `Option<VarnodeId>` set to `None` with index-based traversal (merge.cc:1080-1097 -> merge.rs:1842-1877). Pinned by `w7_s6_merge_by_datatype_preserves_discovery_order`.
- **Exception -> Result partial-state parity**: clean. Every C++ `throw LowlevelError` is a `return Err(KunaError::lowlevel(...))` at the same point, leaving the same partial mutation: `mergeTestMust` (merge.cc:246), `mergeRangeMust` "Forced merge caused intersection" (merge.cc:315 -> merge.rs:1654), `mergeOp` "Non-cover related..."/"Unable to force merge" (merge.cc:765/769 -> merge.rs:1456/1461), `mergeIndirect` "Unable to merge address forced indirect" (merge.cc:881 -> merge.rs:1500). The `?`-propagation in `merge`/`trim*`/`snip*` preserves the C++ mid-mutation state (e.g. `mergeOp` having already inserted trim COPYs before a throw). `merge`'s back-out — `intersection` true -> `Ok(false)` with NO mutation, else `bank_merge_highs` then `bank_update_cover` — mirrors `Merge::merge` exactly (merge.cc:1565-1575 -> merge.rs:1056-1073).

## findings
- F1 (major / loss LOSS-112): the WHOLE `Merge` driver is ported over a 119-method `MergeContext` trait whose ONLY impl is the in-module test `Mock` — there is no `impl MergeContext for Funcdata`, so the merge pass is standalone-transcribed and NOT wired into the live pipeline. The merge ALGORITHM (the wave's named HighVariable-merge / Cover-intersection ordering, plus all control flow) is faithfully ported and exercised by 18 in-module + 6 verifier tests; the deferred half is every IR-mutating / cross-arena primitive (`overlapLoc` via `addr_tied_ranges`, `copyShadow`/`characterizeOverlap`, `newOp`/`opInsert*`/`totalReplace` via the `*_op`/`build_dominant_copy` seams, the W4 multi-entry scope walk, `gatherPieces`, the `compareByHigh` raw-`HighVariable*` sort). This is the established W7/W8 convention (cf. LOSS-094 S4 Actions, LOSS-104 blockaction shells); not a correctness defect in the transcription, but the item does not run on real IR yet.
     cpp: decompiler/cpp/merge.cc (all of `class Merge`)
     rust: rust/crates/kuna-decomp/src/merge.rs:142-439 (trait), :2451 (only impl, in `#[cfg(test)]`)
- F2 (minor / loss LOSS-113): `mergeLinear`'s `compareHighByBlock` sort is a STABLE `sort_by` (merge.rs:1111-1121) where C++ uses unstable `std::sort` (merge.cc:282). For two distinct highs that compare fully-equal under the cover/instance-addr/def-addr chain, the Rust keeps input order while `std::sort` may reorder, which can change the `mergeLinear` stack-representative pick. Uncommon (highs entering a type-group usually have distinct earliest-cover blocks), and a deterministic refinement (LOSS-097/111 class). The sibling `compareCopyByInVarnode` sort (merge.cc:1308) is unaffected — its tie-break runs to the unique `SeqNum::order`.
     cpp: decompiler/cpp/merge.cc:282; decompiler/cpp/merge.hh:152-174
     rust: rust/crates/kuna-decomp/src/merge.rs:1111-1121
- F3 (minor / style): several `as` index/len casts in merge.rs carry no `// cast:` justification (e.g. `list.len() as int4 - 1` :103, `slot as usize` :1707-1711, `pos as int4`/`sz as int4` :2031/:2058, `... as int4` :2170, the test-mock casts). All are benign non-negative index/len conversions or the documented SUBPIECE-offset truncation; the two load-bearing offset-truncation casts (:1728, :1777) DO carry `// cast:`. Consistent with the codebase laxity the dependency review noted (its F3); not a correctness finding.
     cpp: n/a
     rust: rust/crates/kuna-decomp/src/merge.rs:103, :1707, :2170
- F4 (info, not a divergence): `register_proto_partial_root` only pushes when `vn->getDef()` is `Some` (merge.rs:1031-1035) where C++ pushes `vn->getDef()` unconditionally (merge.cc:1552) — defensive, equivalent on valid input (a proto-partial root is always a written PIECE op); C++ would null-deref at `op->isDead()` on the impossible null. `snip_output_interference`'s `cur_high: Option = None` first-iteration compare and `snip_out.expect(...)` reproduce the C++ `curHigh=(HighVariable*)0` / `snipop->getOut()` null-deref-on-impossible-input identically. Both safe.
     cpp: decompiler/cpp/merge.cc:1552, :816-837
     rust: rust/crates/kuna-decomp/src/merge.rs:1031-1035, :1516-1531

## adversarial tests
(rust/crates/kuna-decomp/tests/verify_w7_s6_merge.rs — 6 tests, all green; committed on the branch as `0c93809`)
- `w7_s6_merge_find_front_boundaries` — `BlockVarnode::find_front` over empty / single / a run of equal block indices (must return the FIRST of the run) / gaps / below-all / above-all (merge.cc:43-61); witnesses the unsigned-`size()-1`->`int4`-`-1` empty path and the binary-search first-of-run.
- `w7_s6_merge_compare_high_by_block_tiebreaks` — the full `compareHighByBlock` chain: cover decides; equal cover + different instance addr; equal cover+addr + different def-op addr; the null-def asymmetry (null-def `<` defined, defined NOT `<` null-def); and the fully-equal strict-weak-order tie (`<` in neither direction) (merge.hh:152-174).
- `w7_s6_merge_compare_copy_by_in_varnode_chain` — the createIndex -> block-index -> SeqNum-order key chain incl. the identical-triple tie (merge.cc:1045).
- `w7_s6_merge_by_datatype_preserves_discovery_order` — interleaved types A,B,A,B,A across five disjoint-cover highs; asserts the type-A group merges in discovery order (h3 then h5 into h1) ENTIRELY before the type-B merge (h4 into h2), witnessing the C++ `list::erase(hiter++)` group-at-a-time erase-while-iterating port and that `mergeLinear` passes `isspeculative=true` (merge.cc:383-400).
- `w7_s6_merge_intersection_self_and_disjoint_and_overlap` — `MergeIntersect::intersection`: self never intersects; disjoint-block covers don't (symmetric); same-block overlapping instance covers do (symmetric) — drives the real two-cursor Cover block walk + `block_intersection`/`test_block_intersection`.
- `w7_s6_merge_intersection_cache_reused_when_clean` — mutating a cover WITHOUT marking `cover_dirty` yields the STALE cached result on re-query (both (a,b) and (b,a) edges), then `cover_dirty=true` forces a re-walk that sees the new overlap — pins the `updateHigh` clean/dirty cache short-circuit (variable.cc intersection driver hosted in merge.rs:924-981).

## losses
- LOSS-112 (F1): whole `Merge` driver unwired — no `impl MergeContext for Funcdata`; standalone transcription tested against a mock.
- LOSS-113 (F2): `mergeLinear`'s `compareHighByBlock` sort is stable `sort_by` vs C++ unstable `std::sort`; tie-only divergence, deterministic refinement.
