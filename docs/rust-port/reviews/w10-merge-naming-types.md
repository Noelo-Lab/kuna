# w10-unseam-merge-types
verdict: ACCEPT-WITH-LOSSES
verifier: claude-opus-4-8[1m] (independent verifier, round 1)
date: 2026-06-13
gate: per-un-seam B5 golden `cargo test -p kuna-decomp --test print_b5_boolless` -> 8/8 pass (1 ignored dump); the item's stated `KUNA_ENGINE=rust run_tests --datatests (count rises)` is the LAGGING signal (full Rust console still 18/285 with 43 ERROR — the dual-AddrSpaceManager M3 gap, LOSS-131/132), so per the un-seam convention (`w10-unseam-heritage-deadcode` note) the authoritative gate is the stage-boundary golden, which is met. C++ oracle untouched: `python -m kuna.run_tests --all --baseline docs/baseline.json` -> 207/207 unit + 675/675 datatest, PARITY OK. `cargo test --workspace` -> all green (141 `test result: ok`, 0 failed). `cargo clippy -p kuna-decomp -- -D warnings` clean (lib + the test target after one verifier test-name fix).

## scope
Un-seam item (modules empty). C++ in scope: merge.cc (allocateCopyTrim/trimOpOutput/trimOpInput/mergeOp; Varnode::copyShadow/characterizeOverlap from varnode.cc), printc.cc (pushSymbol/pushVnExplicit/emitLocalVarDecls/emitVarDeclStatement), coreaction.cc/.hh (ActionMergeRequired/Adjacent/Copy/MultiEntry/Type, ActionNameVars, ActionOutputPrototype), database.cc Scope::buildDefaultName (kunaAngrNaming branch), kuna_naming.cc kunaStorageComment, funcdata_varnode.cc inScope, fspec.cc updateOutputTypes/ProtoStoreInternal. The standalone `Merge` engine itself (merge.rs, 3132 lines) was ported+verified in w7-s6-merge (LOSS-112: "unwired, no `impl MergeContext for Funcdata`"); THIS item provides that impl (funcdata_merge.rs, net-new) and wires the three actions, plus the printer naming/decl/output-type rendering. The branch is one commit (`63393c1`) + the verifier's adversarial-test commit (`344fa49`).

## faithfulness verdict (the named concern)
REAL, not a shortcut to render boolless's string. The live analyzed IR (dumped via `zz_dump_boolless_cfg`) carries a genuine Merge artifact: `u0x10000002:1 = CPUI_COPY(i0x52:1)` (a trim COPY into a FRESH internal unique) feeding `ACC = CPUI_MULTIEQUAL(u0x10000002, ACC)`. That is `Merge::mergeOp`/`trimOpInput`/`allocateCopyTrim` over real IR — `ActionMergeRequired` ran mergeAddrTied->groupPartials->mergeMarker. The printer then renders it as `undefined1 v1; // acc` / `v1 = dat_52;` / `if (dat_52 <= 10) { v1 = 1; }` / `return v1;` — byte-identical to the C++ B5 oracle except the type name `undefined1` vs `uint1` (the W8 ActionInferTypes residual). copyShadow/characterizeOverlap/allocateCopyTrim/trimOpOutput are field-for-field transcriptions of their C++ (varnode.cc:996/155, merge.cc:411/656). The three actions match coreaction.hh:370/382/393/404/416 and coreaction.cc:4999. updateOutputTypes' unlocked arm (`pieces.addr=triallist[0]->getAddr(); pieces.type=triallist[0]->getHigh()->getType()`) is ported exactly.

## no-special-casing verdict
CLEAN. Grep of the diff for `boolless`/`dat_52`/`"v1"`/`uint1`/`0x52`/`acc` finds them ONLY in comments and test code — zero in engine logic. Empirically confirmed CONDITIONAL: a different function (`condconst`, run through the identical pipeline) gets NO `vN` name, no `dat_52`, no `// acc`, and a `void` return (its slice recovers no addr-tied local) — proving the naming/type recovery is engine logic gated on the merge/addrtied chain, not a boolless hardcode. The `v1` name comes from `name_local_highs_angr`'s running `v{base}` counter (base=1), the `// acc` from `kunaStorageComment`'s register-name-lowercased arm (ACC->acc) read off the addr-tied storage member, the `dat_52` from `kunaGlobalDataName` on the unnamed persist global.

## datatest-pass reality
The branch flips NO datatest assertion: boolless's only stringmatch (`if (dat_52 <= 10)`, min=1) was ALREADY green on rust-port (achieved by w10-structure-printbody) and does NOT depend on the merge (the CBRANCH condition is `INT_LESSEQUAL(i0x52,#0xa)` over the global directly, not the trim COPY). The merge/naming/output-type win is the full BODY parity (`v1` local, trim COPY, decl, return), which the boolless datatest does not assert. So the "count rises" gate is not advanced via the datatest harness by this item — but the per-un-seam B5 golden (full body) is, and that is the right gate for an un-seam (the datatest count is the M3 lagging signal blocked on LOSS-131). No vacuous/fake pass.

## hunt list
- **Signedness**: clean. `name_local_highs_angr`/`emit_local_var_decls` use `int4 base` and `BTreeSet<HighVariableId>` (no signed/unsigned loop-vs-size compare). `characterize_overlap` ports the C++ `loc.getOffset() < op.loc.getOffset()` unsigned-offset compares exactly (varnode.cc:162; funcdata.rs). Space identity is compared via `get_index()` (Rust) vs pointer (C++) — equivalent (indices are unique per space).
- **Integer widths**: clean. `varnode_characterize_overlap` casts `get_size() as u64` to match the C++ `uintb` offset arithmetic (`oa + (za-1)`); `vn_create_index() as u64`; `merge_group` carried as `int2` through the deferred setHigh log (matches C++ `int2 mergeGroup`). No int4*int4->intb promotion in the touched code.
- **Wrapping**: clean for the slice. `characterize_overlap`'s `za - 1`/`zb - 1` would debug-panic if a varnode size were 0 (varnodes are always >=1) and `oa + (za-1)` could overflow u64 only past the address space (matches the C++ uintb wrap domain) — the established debug-panic-vs-C-wrap class (LOSS-085/086), unreachable here. No `wrapping_*` needed on the merge/naming/decl path (additions are bounded indices/offsets).
- **Comparator totality**: clean. `decls.sort_by(|a,b| a.1.cmp(&b.1))` sorts owned `String` (total `Ord`). `BTreeSet<HighVariableId>`/`BTreeMap` keys are total. `sort_op_nodes_by_high` sorts by `get_create_index()` (a unique u64 — total, no ties). No float comparators, no `partial_cmp().unwrap()`.
- **Iteration-order provenance**: clean (with one ordering loss, F2). `name_local_highs_angr`/`emit_local_var_decls`/`bank_symbol`-context walks use `vbank.iter_loc()` = `loc_tree.values()` where `loc_tree: BTreeMap<LocKey,_>` (the C++ `VarnodeLocSet` std::set location order = `beginLoc()..endLoc()`). `ops_alive()` = `obank.iter_alive()` (the C++ dead-code-list order, LOSS-112 provenance). The `seen`/`mark_set` are `BTreeSet` (deterministic). NO HashMap/HashSet anywhere in the diff (grep clean). DECL EMISSION sorts by name not storage-address (F2/LOSS-137) — a divergence from the C++ scope walk for >=2 locals.
- **Off-by-one / do-while / reverse iterators**: clean. `name_local_highs_angr`'s `base` starts at 1 and post-increments (`format!("v{base}"); base += 1`) matching `buildDefaultName`'s `'v' << base++`. The `find(addr_tied).or_else(instance 0)` storage-rep pick mirrors `getFirstWholeMap` with the instance-0 fallback. No do-while in scope.
- **Erase-while-iterating**: n/a — the bridge methods don't erase during traversal; the Merge engine's erase dances were verified in w7-s6-merge.
- **Exception -> Result partial-state parity**: clean. `ActionMergeRequired::apply` runs the three passes with `if ...is_err() { return 0; }` (C++ returns 0 from apply; a thrown LowlevelError mid-pass propagates up to the action restart machinery — here the early-return-0 leaves the same partial IR mutation as the C++ catch site, which restarts). `bank_merge_highs` defers `vn->setHigh` writes into a log replayed after the read-borrow ends — behaviorally identical (the merge never reads `vn->high`), and the mark side-set is cleared before return (matches moveIntersectTests).

## mechanical pass
- `cargo run -p port-audit -- --item w10-unseam-merge-types`: N/A — item has empty `modules`/`cpp_blob_sha` (un-seam item), so there is no blob-sha to drift-check; port-audit has nothing to assert. (Not a finding; same as prior un-seam reviews.)
- grep: `HashMap`/`HashSet`/`todo!`/`unimplemented!`/`sort_unstable` — ZERO in the diff. Bare `as` casts without `// cast:`: two, both benign non-negative widenings (`get_size() as u64`, `get_create_index() as u64`) — F3.
- `cargo clippy -p kuna-decomp -- -D warnings` clean.

## findings
- F1 (major / loss LOSS-137): the W4 ScopeLocal/Symbol layer is replaced by two `(kuna)` stand-ins, not transcriptions. `mark_output_storage_addr_tied` marks the first-return-op value input `mapped|addrtied` as a heuristic for `ScopeLocal::inScope` (funcdata_varnode.cc:992-997); `name_local_highs_angr` reduces `linkSymbols`+`buildDefaultName`'s `vN` arm (coreaction.cc:3028/database.cc:1783) to `is_addr_tied && !is_free && !is_input && !is_persist`, excluding the C++ `in`/extraout/unaff/stack locals the namerec set also routes to `vN`. EXACT on boolless; under-names + uses a heuristic addrtied marking off-slice. Faithful as a documented stand-in (the merged tree has no ScopeLocal), not a fake — confirmed conditional by `w10_naming_conditional_other_function_gets_no_vn_name`.
     cpp: decompiler/cpp/coreaction.cc:3028-3098; decompiler/cpp/database.cc:1761-1786; decompiler/cpp/funcdata_varnode.cc:992-997
     rust: rust/crates/kuna-decomp/src/coreaction_cleanup.rs (`mark_output_storage_addr_tied`, `name_local_highs_angr`)
- F2 (minor / loss LOSS-137): `emit_local_var_decls` orders decls by NAME (`decls.sort_by(|a,b| a.1.cmp(&b.1))`) where C++ `emitScopeVarDecls` walks the scope's `SymbolEntry` map in STORAGE-ADDRESS order. For >=2 locals the decl block order diverges, and the name sort is lexicographic so `v10` would precede `v2`. Invisible on boolless (single local).
     cpp: decompiler/cpp/printc.cc:2667-2684
     rust: rust/crates/kuna-decomp/src/printc.rs (`emit_local_var_decls`)
- F3 (info / loss LOSS-137): `ActionOutputPrototype::apply` ports only the high-on / unlocked / triallist[0] arm of `updateOutputTypes` (skips `updateOutputNoTypes` for high-off and the size-type-locked override), and approximates `getTypeVoid` with a size-0 `TYPE_VOID` base. Faithful to the C++ unlocked arm (which also reads only triallist[0]); the skipped arms are unreachable on the high-on merge path. The output TYPE NAME renders `undefined1` vs the oracle's `uint1` — the W8 ActionInferTypes residual (the byte-parity test substitutes `undefined1->uint1`).
     cpp: decompiler/cpp/coreaction.cc:4999-5016; decompiler/cpp/fspec.cc (FuncProto::updateOutputTypes)
     rust: rust/crates/kuna-decomp/src/coreaction_protos.rs (`ActionOutputPrototype::apply`); rust/crates/kuna-decomp/src/printc.rs (`type_name_for_decl`)
- F4 (info / loss LOSS-137): the conservative-default `MergeContext for Funcdata` arms — `high_group_info`/`high_piece`/`piece_*`->empty, `bank_symbol`/`multi_entry_symbols`/`symbol_*`->none, `addr_tied_ranges`->empty, `build_dominant_copy`->Err, `vn_partial_copy_shadow`/`populate_affecting_ops`/`gather_pieces`->empty. Exact on the boolless MULTIEQUAL/COPY merge path (no CONCAT overlap groups, no addrtied overlap ranges, no W4 multi-entry symbols, no hideShadows dominant-copy); divergent for functions with overlap-group highs. Each is documented inline with its restoration surface.
     cpp: decompiler/cpp/merge.cc (overlapLoc/gatherPieces/buildDominantCopy/mergeMultiEntry)
     rust: rust/crates/kuna-decomp/src/funcdata_merge.rs (the empty/zero arms)
- F5 (info, not a divergence): `do_trim_op_output`'s INDIRECT `getOpFromConst` reconstructs an `OpId` from the raw u64 in the IOP-space input (`OpId::from(KeyData::from_ffi(addr))`) — fragile raw-key reconstruction, but matches the C++ `getOpFromConst` pattern and is reached only for INDIRECT trims (off the boolless path). `assign_high` un-seamed to actually call `assign_high_var` when high is on (so the trim COPY's unique gets a high) — faithful to `Funcdata::assignHigh`. `mark_mapped_addr_tied` sets exactly `mapped|addrtied` (funcdata_varnode.cc:997). All sound.
     cpp: decompiler/cpp/merge.cc:656-690; decompiler/cpp/funcdata_varnode.cc:48,997
     rust: rust/crates/kuna-decomp/src/funcdata.rs (`do_trim_op_output`); rust/crates/kuna-decomp/src/funcdata_varnode.rs (`assign_high`); rust/crates/kuna-decomp/src/varnode.rs (`mark_mapped_addr_tied`)

## adversarial tests
(rust/crates/kuna-decomp/tests/print_b5_boolless.rs — committed on the branch as `344fa49`; 3 new, all green; reuse the existing `run_full` harness)
- `w10_merge_trim_copy_is_real_ir_artifact` — the `v1 = dat_52` trim COPY is a REAL Merge-engine artifact: the analyzed IR holds a `COPY` whose output is a FRESH internal/unique varnode that feeds the ACC `MULTIEQUAL`; asserts the structural op exists (a printer hardcode would leave the IR without it) AND that it renders as `v1 = dat_52;`. Anti-special-casing witness that mergeAddrTied/trimOpInput/allocateCopyTrim ran over live IR.
- `w10_merged_high_has_multiple_instances_and_acc_storage` — ACC's `v1` high carries >1 instance (a real merge, not a rename), contains BOTH the addr-tied ACC register member AND the merged trim-COPY unique, and the `// acc` storage comment derives from the ACC register (not the unique, which has no register name). Guards against no-op-merge and wrong-representative storage reads.
- `w10_naming_conditional_other_function_gets_no_vn_name` — `condconst` (a different function through the identical pipeline) gets ZERO `vN` names, no `dat_52`, no `// acc`: the angr naming is engine logic conditional on the merge/addrtied chain, not a boolless-specific hardcode, and no boolless string leaks across functions.

## losses
- LOSS-137 (F1-F4): the MergeContext bridge wires the real Merge engine over live IR (boolless B5 body byte-parity modulo undefined1/uint1), but the W4 ScopeLocal/Symbol naming + addrtied-marking are angr stand-ins, the proto-output recovery is the high-on/trial[0] subset, local-decl order is name-lexicographic not storage-address, and the unpieced/symbol/multi-entry/dominant-copy arms take the merged-tree default.
- (context) LOSS-112 (w7-s6-merge): the Merge engine was standalone/unwired; THIS item closes the "no `impl MergeContext for Funcdata`" half of LOSS-112 for the MULTIEQUAL/COPY merge path.
- (context) LOSS-131/132: the full Rust datatest count lags (dual AddrSpaceManager / analysis-body M3 gap), which is why the gate is the per-un-seam B5 golden, not the datatest count.
