# kuna history

The condensed project history. This file replaces four retired doc trees —
`docs/history/` (frozen design docs + campaign logs), `docs/rust-port/` (port plan, ADRs,
losses ledger, verification protocol), `docs/divergences.md` (DIV registry), and
`docs/UPSTREAM.md` (vendored-tree provenance) — all deleted 2026-07-28; their full text
lives in git history before that date. Code comments citing `decompiler/cpp/<file>.{cc,hh}`
refer to the retired vendored C++ tree (recoverable from the pinned upstream commit below);
citations of `LOSS-NNN`, `UB-N`, or paths under `docs/rust-port/` and `docs/history/`
(in code comments or `phases.toml` prose) refer to the retired
ledgers and design docs summarized here.

## Timeline

| Date | Commit | Event |
|---|---|---|
| 2026-06-05 | `17b3c5eb` | **Started with Ghidra in C++**: vendored the Ghidra C++ decompiler (~196k LOC: `cpp/`, `unittests/`, the 83-file/675-assertion XML datatest corpus) and all 39 SLEIGH processor modules (`48169e0c`). kuna begins as the byte-identical C++ tree plus Python glue. |
| 2026-06-06 | `ed1ae4d8` | **C++ rewrite era begins** — the stage-model campaign: the P0 + S1–S9 phase model (today's P0–P9) derived from a structured Ghidra/angr/Reko study; GH-558 prototype (`64bb80aa`) is the first intentional divergence from upstream (`compareform`/`arraynotation`, the first settable options, and the `tests/stages/` family). |
| 2026-06-08 | `ca1d2a14` | C++ feature/fix wave complete: **37 public Ghidra issues fixed** (17 always-on bug-fixes, 16 of them SLEIGH spec corrections; 20 option-gated decision points) out of 58 reproduced; stage registry, `kassert`, restart log, per-stage console commands. |
| 2026-06-08 | `1b84f89e` | **First feature ported from angr**: LoweredSwitchSimplifier (`loweredswitch`, default-on next day as DIV-4). |
| 2026-06-09 | `1cf56a68` | The **autonomous angr-inspired feature pipeline** (`kuna.pipeline`) starts; its first autonomously-authored PR merges the same day (`638bb4b4`, PR #1: `stackguard`, angr's StackCanarySimplifier). angr-style naming (`namestyle`, DIV-5) and RegionIdentifier (`87016be4`) follow. |
| 2026-06-10 | `6a095f8a` | **Rust port begins**: 6-crate cargo workspace, 7 ADRs, `--engine {cpp,rust}` differential harness (`36b7862f`), 200-item checklist. |
| 2026-06-11→13 | `2942b5ec`…`f3df8f61` | Wave gates W1–W9 land in four days (foundations, SLEIGH runtime, core IR, framework, simplification rules, types/fspec, storage+structuring, print, console). M1 = 207/207 unit-test parity (`2c4a6bc4`); M2 = datatests run end-to-end under `--engine rust` (3/675). |
| 2026-06-19 | `bca4ae8d` | **Initial Rust port completed** — M3: full datatest parity **675/675** against the untouched C++ oracle, after the W10 parity grind (3 → 21 → 47 → 315 → 476 → 675). |
| 2026-06-20 | `b3838e1b` | SLEIGH compiler ported (`slacomp`): 148/148 specs compile to content-identical `.sla`; full suite green on Rust-built specs, both engines. |
| 2026-06-20 | `9346a1a5` | **C++ tree removed** — the Rust port is the engine. Python CLI becomes the single Rust `kuna` binary (`3b29635a`). |
| 2026-06-22 | `aa765712` | **Analysis-tier port begins**: Ghidra's Java analyzer suite recreated as `kuna-analysis` — 62 increments over ~5 weeks (no-return, demangling, strings, DWARF, entry discovery, ARM/Thumb painting, the Listing/xref tier, PE/Mach-O/COFF loaders, Go pclntab, FID, RTTI/Obj-C/PDB metadata), every increment holding 675/675. |
| 2026-07-04 | `6b270a3e` | decbench campaign: whole-binary benchmark loop (mine/triage/rescore) driving feature work from real-binary regressions vs Ghidra/angr. |
| 2026-07-05 | `b255b3d4` | Ghidra integration Phase 1: kuna as stock Ghidra's decompiler core (wire protocol + extension). |
| 2026-07-16→28 | `cdaa35bc`, `9d308212` | `kuna decompile-project` whole-binary export; WASM browser build; the project site + in-browser decompiler at kuna.noelo.org. |

## The Rust port: how it was verified

The port's central rule: the vendored C++ tree stayed **byte-for-byte untouched as the
oracle** the entire time — every claim of correctness was differential against an unmoved
reference, never self-graded. The Rust side reproduced the exact `decomp_dbg` /
`decomp_test_dbg` console command surface, so the existing XML datatests and harness
verified the port *unchanged* via an `--engine {cpp,rust}` switch.

**Structure.** 91 port items + 91 paired verify items + 18 infra items (182,926 LOC of
scope), each pinned to the C++ source with `git hash-object` blob shas. Items were grouped
into waves W0–W11, each wave fanned out to parallel agents in isolated git worktrees and
serially integrated under a wave gate; a gate passed only when every item's verify was
ACCEPTed. Bodies were line-faithful transliterations (same algorithms, iteration order,
comparators, registration order); only the shell (ownership, `Result`, crate layout) was
idiomatic. Determinism was enforced structurally: `BTreeMap`/`BTreeSet` only (`HashMap`
banned workspace-wide via clippy `disallowed_types`), stable sorts, mandatory wrapping
helpers, explicit widths.

**The porter/verifier split.** Every port item got an adversarial verifier that was
structurally independent: it received only the C++ source at the pinned blob sha, the Rust
diff, fresh gate output, and the checklist entry — never the porter's reasoning. Each
verdict had to check off a mandatory hunt list (signedness, integer widths/promotions,
wrapping, comparator totality, iteration-order provenance for every loop, do-while/
`lower_bound` boundaries, erase-while-iterating equivalence, exception→`Result`
partial-state parity) and write **≥3 new adversarial tests** targeting the fragile spots —
tests landed regardless of verdict. Verdicts were ACCEPT / ACCEPT-WITH-LOSSES (every
divergence cited in the losses ledger) / REJECT (required a failing test or a concrete
divergence trace against the C++ oracle; three REJECTs blocked the item for human
decision).

**How bugs were found.** Layered differential evidence, finest to coarsest:
golden vectors for opbehavior/float/comparators/XML; per-instruction SLEIGH lift-diff over
the corpus; stage-boundary snapshots B0–B5 (pipeline registration, lift, post-flow CFG,
post-heritage SSA, final IR, printed C) pinned byte-equal to C++; the 207 upstream unit
tests transcribed 1:1; and the 675 datatest assertions with **monotonicity** enforced —
a per-assertion passing-set diff at every wave meant no previously-passing assertion ever
regressed. The W10 "parity grind" root-caused each remaining divergence with dual-engine
traces; the ledger records wrong theses corrected by tracing (the dual-AddrSpaceManager
keystone, LOSS-132, made every analysis pass see zero varnodes) and one **vacuous parity
claim REJECTED** by a verifier (19/675 where all 19 were empty-negative assertions). The
SLEIGH compiler had its own oracle: `sleigh_opt` is deterministic, so 148/148 `.sla`
outputs were compared as decompressed element streams, then all specs were rebuilt with
the Rust compiler and the full suite re-run. Only after all gates held was the C++ tree
deleted.

**Cost.** The port-and-verification campaign ran roughly **two weeks of near-constant LLM
time** (2026-06-05 vendoring → 06-20 C++ removal, port proper 06-10 → 06-19) and cost
approximately **$8k** in API spend.

**Ledgers.** `losses.md` was the append-only accountability spine: ~250 numbered entries
(LOSS-001–252) recording every deliberate departure at decision time — deferrals (81),
faithful-to-C++-UB divergences (58), seams (24), dependency substitutions (9: zlib→flate2,
libbfd→`object`, bison/flex→hand parsers, std::regex→`regex`), and diagnosis journals.
Every parity-blocking entry was closed en route to 675/675; what stayed open is permanent
by design (deterministic iteration order replacing C++ heap-pointer order; debug-panics
where C++ relies on x86 shift-masking UB; content-equality rather than byte-equality for
deflate). The port also surfaced **six latent bugs in upstream Ghidra** (UB-1..UB-5):
`opcode_name[]` OOB read at `CPUI_MAX`, SIGFPE on `INT64_MIN / -1` in IntSdiv/IntSrem,
signed overflow in XML `convertCharRef`, a dangling-record read in `rangemap::erase`, a
`MemoryBank` page-copy overrun, and a sort-order violation in the pcode-snippet keyword
table making `||`/`abs` unlexable — all reproduced faithfully or fail-closed in Rust,
pinned by tests.

**Architecture decisions (ADR one-liners).** 0001: IR in slotmap generational arenas
owned by `Funcdata` (no `Rc<RefCell>`). 0002: ordered containers only, C++ comparators
transcribed field-by-field. 0003: `uintb→u64`/`intb→i64` with mandatory wrapping helpers.
0004: `Result<KunaError>` mirroring `LowlevelError`, caught at the same frames. 0005:
`universalAction` as a declarative SchedNode table, observably identical to C++ `clone()`.
0006: stage tables in `phases.toml` with build.rs codegen, byte-compatible catalog JSON.
0007: typed P0 store + per-stage artifact versioning for rerunability
(`GhidraExact` restart semantics by default).

## Divergences from upstream Ghidra (DIV registry)

A DIV entry records every **intentional change to default behavior** vs upstream (bug
fixes or evidence-backed default flips), with its measured effect on the 675 upstream
assertions. Byte-identical option-gated exposures need no DIV. Entries are append-only;
new default flips add a row here (full original entries with evidence: git history of
`docs/divergences.md`). Pre-rename entries say "stage"/S-codes for phase/P-codes.

| DIV | Option/area | What changed | Evidence/effect |
|---|---|---|---|
| DIV-1 | parenthesization (no flag) | `-(-x)` not `--x` (re-parses as pre-decrement) | 0/675 |
| DIV-2 | `compareform original`, `arraynotation`, `thumbfuncptr`, `inferfuncentry`, `booleanmask`, `addcarrychain`, `ovlesssimplify`, `memsetrecover` | 8 issue-fix options default-on (GH-558/8471/6930/1282/7190/8913/9230/1537) | 22/675 re-pinned (15 files) |
| DIV-3 | `flagcompare`, `stackprobeloop`, `dynamichashmax`, `arraystride`, `condexeplace`, `inputvarnodeadjust` | 6 more issue-fix options default-on | 0/675 |
| DIV-4 | `loweredswitch` | angr LoweredSwitchSimplifier: comparison cascades → `switch` | 0/675 (binary-search guard) |
| DIV-5 | `namestyle angr` | angr-style naming (`v1`/`a0`/`dat_`/`sub_`/`label_`) + storage decl comments | 185/675 re-pinned; `namestyle ghidra` restores |
| DIV-6 | `realtypes` | `xunknownN`/`undefinedN` render as real C types | print-only; 0/675 |
| DIV-7 | `dedupvardecls` | duplicate scalar local decls collapsed | 0/675; +0.14% speed |
| DIV-8 | `relocobjects` | ELF `ET_REL` `.o` loading (relocations + extern binding) | new capability; 0/675 |
| DIV-9 | `i386_pie_plt` | i386-PIE PLT stub decode → named imports, no-return works | 0/675; `usage` −69% time |
| DIV-10 | `loopbreak_recovery` | loop-exit gotos become `break;` (converges toward upstream) | 0/675 |
| DIV-11 | libc proto table (no flag) | `setlocale` prototype added | 0/675 |
| DIV-12 | `regionstructure` | angr Phoenix/SAILR region structurer is the default structuring path | 0/675 (CollapseStructure fallback) |
| DIV-13 | `noreturn_externmatch` | name-matched no-return externs in `ET_REL` | 0/675; ~23% faster on chcon.o |
| DIV-14 | `gotoreduce`, `crossjumprevert`, `taildup`, `dedupitetail`, `regionlooprefine`, `ifelseflatten`, `switchmultipred`, `tailcalljump`, `foldcallret`, `branchflip`, `stackguard`, `noreturn_extern`, `noreturn_propagate`, `switchsharedcase` | 14-flag angr SAILR structuring/readability sweep default-on (3 flags remove code) | 0/675 via per-test opt-outs in 12 files |
| DIV-15 | `decompile-all`: `listing on` | whole-binary driver builds the Listing by default | fixes function swallowing; 0/675 |
| DIV-16 | `noreturn_error` | `error(nonzero,…)` wrappers concluded no-return per call site | Listing-gated; 0/675 |
| DIV-17 | `iteregion` | assignment diamonds render as `?:` ternaries (angr ITERegionConverter) | print-only; 0/675; −2.6% speed |
| DIV-18 | `returndup` — **REVERTED** 2026-07-05, **SUPERSEDED by DIV-54** | early-return duplication was default-on; decbench measured −976 aggregate GED regression | opt-in runtime choice until DIV-54; **the −976 is superseded** — it was measured on the *unselective* pass, #137's const-return gate reversed it, and a 2026-08-02 isolating ablation over 52,862 optimized functions measures +417 GED-perfect / −7,756 aggregate GED (`docs/decbench/returndup-regression-triage.md`) |
| DIV-19 | `noreturn_reach` | CFG-reachability no-return discovery (+ 2026-07-08 hot/cold-split fix) | Listing-gated |
| DIV-20 | `decompile-all`: `funcstart_patterns on` (non-x86-64) | prologue-pattern function discovery on stripped ARM/AArch64/MIPS/PPC/RISC-V | betaflight 469→1470 fns; x86-64 identical |
| DIV-21 | no-return name list (data) | BSD `err`/`errx` family, `quick_exit`, glibc fatal internals added | 0/675 |
| DIV-22 | `noreturn_disc` | Ghidra's ≥3-site-evidence discovered-no-return analyzer default-on (as in Ghidra) | Listing-gated; removes code |
| DIV-23 | `earlyreturn` | per-edge const-guard early-return peeling | decbench +47 perfect / −576 GED; 1 opt-out |
| DIV-24 | DWARF global naming (bug fix) | globals mapped at real byte size so wide accesses get their name | 0/675 |
| DIV-25 | `switchreturn` | per-case const returns for wide switches (≤256 cases) | +2 perfect / −107 GED; 1 opt-out |
| DIV-26 | ELF data-symbol naming (no flag) | `.symtab`/`.dynsym` `STT_OBJECT` entries named as globals (`dat_20a098` → `optind`) | 0/675; commits last, DWARF+strings keep precedence |
| DIV-27 | own-prototype seeding (bug fix, no flag) | a function's parked (DWARF/libproto) signature is applied to its OWN decompile, not just to its callers | 0/675; also kills the bogus `undefined16` RAX:RDX return |
| DIV-28 | mid-string literals (bug fix, no flag) | a constant pointing INTO a readonly char array is typed `char *` (`bindtextdomain(0x68d8,…)` → `"coreutils"`) | 0/675; interior hits only, exact-hit path untouched |
| DIV-29 | DWARF enums (no flag) | `DW_TAG_enumeration_type` builds a real enum type (`quotearg_style(4,…)` → `…(shell_escape_always_quoting_style,…)`) | 0/675; falls back to the underlying int when anonymous/memberless |
| DIV-30 | uncomputed return half (no flag) | a recovered return PAIR whose half is a callee-saved restore / callee clobber is narrowed to the real half (kills `undefined16 main` + `v[8] = <uninit>`) | 0/675; subsumes `returnpair` on GH-6990 (3 stage assertions re-worded, 261/261) |
| DIV-31 | x86 `DF` unaffected (no flag) | the ABI's direction-flag guarantee is stated where the cspec is silent, folding `(uint8)df * -2 + 1` strides to `+1` | 0/675; x86-only, spec-silent models only |
| DIV-32 | whole-binary entry dedup (bug fix, no flag) | `decompile-all`/`functions`/`decompile-project`/wasm report each entry ADDRESS once — extra names move to `aliases[]`, and ARM/Thumb `entry\|1` addresses fold onto the real entry (symbol seeds, the enumeration key, and `--addr`) | 0/675 (analysis tier is parity-isolated); `arm_thumb_linked_le32` 6→2 entries, `fmt_arm` 32→14, x86-64 unchanged; 713/713 surviving functions byte-identical |
| DIV-33 | executable batch targets (bug fix, no flag) | unfiltered `decompile-all`/`decompile-project`/wasm runs skip callable symbols in data sections while preserving them for naming, inventory, and explicit address selection | 0/675 (analysis tier is parity-isolated); private PE `bc4c15d8…3ae1b` 693→351 targets and 63.58→7.78 s; all 351 executable artifacts byte-identical |
| DIV-34 | `braceformat function next` (upstream option, new default) | no blank line between a function prototype and its `{` (upstream skip_line renders `)\n\n{`); `option braceformat function skip` restores | 0/675; print-only |
| DIV-35 | `nullprinting on` (upstream option, new default) | zero pointer constants render as `NULL` instead of `(type *)0x0` (upstream option_NULL default off); `option nullprinting off` restores | 675/675 via 1 datatest opt-out (forloop_loaditer); 2 stage asserts re-worded; print-only |
| DIV-36 | `inplaceops on` (upstream option, new default + ported consumer) | `out = out OP y` statements render as `out OP= y` (the upstream `emitInplaceOp` consumer of option_inplace_ops was never wired; kuna ports it), `x += -c` folds to `x -= c`; comma contexts (for-headers) exempt; `option inplaceops off` restores | 675/675 via 9 datatest opt-outs; 1 stage assert re-worded; print-only |
| DIV-37 | `truthycond` (new kuna option, default-on) | boolean-context zero comparisons render truthy (`if (x != 0)` → `if (x)`, `if (p == NULL)` → `if (!p)`) in if/while/for/ternary conditions and `&&`/`\|\|`/`!` operands; float/enum/equate zeros and value uses keep the explicit form; `option truthycond off` restores | 675/675 via 7 datatest opt-outs; 9 stage asserts re-worded (1 opt-out: branchflip polarity test); print-only |
| DIV-38 | `braceelide` (new kuna option, default-on) | a single-statement if body drops its braces (statement indented on the next line); Copy-leaf bodies only (labels/comments/multi-statement/else arms keep braces, dangling-else impossible); `option braceelide off` restores | 675/675 via 4 datatest opt-outs (copytrim, ifswitch + boolless/condconst oracle pins); 1 stage assert re-worded; print-only |
| DIV-39 | `warnstyle` (new kuna option, default `inline`) | analysis warnings render as terse `// slug` end-of-line comments on the line they describe (`usage(1); // no-return`, `if (v8) { // branch-flip`, prototype-line `// early-return x3`) instead of full `/* WARNING: ... */` banner lines; unknown texts keep full info behind `warn:`; `option warnstyle banner` restores | 675/675 via 3 datatest opt-outs (inline, inlinetarget, switchreturn); 14 stage asserts re-worded to slug forms; print-only |
| DIV-40 | file front-ends default to `mode auto` | raw inputs `<500 KiB` select `aggressive`, `500 KiB–<2 MiB` select `reliable`, and `>=2 MiB` select `fast`; explicit `--mode reliable\|aggressive\|fast` restores a fixed policy | 0/675 (datatest console has no file-policy default); private PE `bc4c15d8…3ae1b` resolves to `fast`: 3.23 s median / 351 of 351 functions versus default-analysis control incomplete after 618.60 s; web and native share exact boundary tests |
| DIV-41 | `fast` and `aggressive` presets: `fast_funcdisc on` | correct the metadata-only fast inventory with a rooted recursive Listing walk plus conservatively validated absolute pointer-table roots (4096-candidate ceiling on non-ARM); full prologue patterns, AIF gap scanning, and ordinary Listing consumers remain off in `fast`; `aggressive` includes the option through its all-safe-off-options contract; address-selected commands retain their exact scope and skip the preset-provided walk | 0/675 (analysis tier parity-isolated); private PE `bc4c15d826aaebeace3fec6360eb687e5662cba8745605093254931dcdb3ae1b` previously exported 350 import veneers plus only its entry and omitted real `0x402d80` code; corrected `--max-fn-seconds 10` export: 462.28 s / 1,475 MiB peak RSS / 3,140 bodies + 13 isolated failures (3,153 definitions); public `pdb_prog.exe` recovers direct callee `0x140001000`, and `aif_gap_x86_64` recovers indirect-only target `0x13ae`, both with nonempty C bodies |
| DIV-42 | unfiltered `fast` whole-binary watchdog defaults to 10 seconds per function | native `decompile-all`/`decompile-project` and WASM whole-binary decompile/project isolate probed pathological functions after 10 s instead of inheriting the native 120 s default; native selectors retain 120 s and explicit `--max-fn-seconds` wins, including `0` | 0/675 (driver policy only); private PE `bc4c15d8…3ae1b` no-flag confirmation: 445.06 s / 1,511,288 KiB peak RSS / 3,140 bodies + 13 isolated failures (3,153 definitions); public native/WASM policy matrices plus Node-WASI and browser-shim fast-project coverage |
| DIV-43 | `aggressive` preset drops `dwarf_lines` (bug fix, no new flag) | the `.debug_line` pass annotates rather than recovers, so blanket-enabling it under `aggressive` — which `auto` selects below 500 KiB (DIV-40) — made `/* src.c:NNN */` per-instruction comments the DEFAULT rendering of every small `-g` binary; it joins `v850indirectbranch` as a manual `--option dwarf_lines on` opt-in, which still wins over the preset | 0/675 (mode presets are not a datatest surface); coreutils `fmt` (219 KiB, resolves to `aggressive`) `main` 321 → 179 lines with 140 source-line comments gone, `decompile-all` 7,106 → 5,299 lines with 1,740 gone; non-comment output identical apart from the `braceelide` single-statement bodies the comments had been suppressing; runtime unchanged within noise (4.36 s vs 4.80 s median of 3) |
| DIV-44 | Cortex-M vector-table oracle scans program-header-executable sections (bug fix, no flag) | the vector table is DATA the CPU reads, but the oracle only looked at `SHF_EXECINSTR` sections, so a firmware whose `.isr_vector` is flagged `WA` inside an `RWE` `PT_LOAD` — every FreeRTOS demo image — never matched, never region-painted `TMode=1`, and lost its whole Thumb call tree; the candidate set now also covers allocated sections contained in a `PF_X` `PT_LOAD` (`phdr_executable_sections`), with the acceptance test (SRAM initial SP, `word[1] == e_entry`, odd Thumb handlers landing in executable memory) unchanged | 0/675; decbench freertos `RTOSDemo.out` 8 → 146 (O0), 8 → 118 (O2), 9 → 147 (O2-noinline) functions, byte-identical to the control experiment that hand-flipped `.isr_vector` to `AX`; unchanged on coreutils `ls` (617), betaflight STM32F405 (5,798 `aggressive`), crazyflie (2,790), cleanflight (2,840), and on relocatable objects (no program headers) |
| DIV-45 | `kuna decompile` exits non-zero on a per-function pipeline abort (bug fix, no new flag) | three compounding defects made a failed function look like a success: the caught panic payload was passed as `&Box<dyn Any>` (so both downcasts failed and every `LOSS-131` record read "panic with non-string payload"), the CLI surfaced the console's stderr only when the emitted C was *empty* (an abort still renders the un-decompiled shell, so it never was), and the shell's comment blamed structuring. Now the payload round-trips, the emitted comment names the real reason (`/* WARNING: decompilation failed: … */`; the generic "structuring declined" text stays for a genuine no-`sblocks` render), and `kuna decompile` prints `error: decompilation failed for <fn> in <binary>: <reason>` plus the forwarded `decomp_dbg` stderr and exits 1 — still printing the recovered shell on stdout. `decompile-all`/`decompile-project`/wasm are unchanged (their per-function `error` field already isolated it, and now carries the real text) | 0/675 and stages PARITY OK (no datatest function aborts, so the console arm is inert); coreutils `comm` `sub_3994` @ 0x3994 before: exit 0, empty stderr, `/* structuring declined at a stub */`; after: exit 1, the real reason ("called Option::unwrap() on a None value") on stderr with the `jumptable.rs:1811` panic site forwarded |

| DIV-46 | unnamed spacebase references render as stack locations (bug fix, no new flag) | a `PTRSUB(spacebase, off)` whose offset P6 bound no Symbol to fell out of the P9 SPACEBASE arm into the *functional* render, emitting the internal p-code operator and a raw machine register into the C body (`*(void *)&PTRSUB(ESP,8)[v2] = 0;` — neither `PTRSUB` nor `ESP` is declared anywhere, so the function is not C); it now takes the upstream `PrintC::pushUnnamedLocation` leaf the arm had been declining, naming the storage in kuna's existing unnamed-location convention (`&Stack00000008`, the analogue of Ghidra's `&stack0x00000008`, undeclared extern-like by design). The symbol-bound path is untouched | 0/675 (no datatest reaches a symbol-less spacebase PTRSUB) and 305/305 stages; 217 of 13,759 functions across 12 decbench binaries emitted `PTRSUB(` before, 0 after (mydoom 4/161, betaflight 109/6,388, cleanflight 72/3,118, mirai 12/181, the rest 1–4 each); interleaved medians `decompile-all --mode reliable`: mydoom 1,553 → 1,563 ms (+0.66%), gzip 2,939 → 2,952 ms (+0.44%), cleanflight 24,453 → 24,339 ms (-0.47%); `tests/stages/ghdec-spacebase-unnamed.xml` (0/6 pre-fix, 6/6 post-fix) |
| DIV-47 | P6 Cover extension restored (bug fix, no new flag) | two dropped cover-extension steps in the merge port each emitted C that computes a **different value** than the binary. (a) `Funcdata::build_copy_pair_range` omitted `Cover::addRefPoint` (`merge.cc:1121`), so `Merge::checkCopyPair`'s dominance range was a point, no intervening write was ever found inside it, and `markRedundantCopies` silenced load-bearing restores — a `-O0` guard-clause function returned a failed call's NULL instead of its parameter, and `for` loops lost their initializer. (b) `ActionMarkImplied` dropped `Merge::markImplied`'s operand cover-dirtying (`merge.cc:1595-1605`) and a Varnode `coverdirty` never reached its HighVariable (`Varnode::setFlags`, `varnode.cc:377-378`), so Covers were never regrown through implied consumers and the speculative merges folded two values live at the same printed expression into one variable | 675/675 PARITY OK and 305/305 stages with no re-pin or opt-out; both directions are upstream-faithful restorations, not new policy; `verify_cover_miscompile` (fixture `covercopy_x86_64`) pins the value-carrying statement for each; `verify_declared_vars`' sanity floor drops 5→4 because (b) lets the size phi coalesce with `strlen` as upstream does |


| DIV-48 | the `realtypes` relabel keeps an unknown pointee's size (bug fix, no new flag) | `realtype_unknown_base` short-circuited to `void` for EVERY size under a pointer, so `undefined1 *`, `undefined4 *` and `undefined8 *` all rendered `void *` — but only the *declaration* was relabelled: `opLoad`/`opPtradd` still build index and cast expressions from the ORIGINAL pointee size, so the declaration contradicted its own body (`void *a3` alongside `a3[1]`, which means byte offset 1 under GCC's void-arithmetic extension where the expression was built for offset 8) and a store cast down to `*(void *)` lost its access width entirely. The same size table a scalar uses now applies to the pointee (`undefined8 *` → `unsigned long *`, `undefined4 *` → `unsigned int *`), and `void` survives only as the **fallback** for residual sizes with no natural single C type (0, 3, 5, 6, 7, 10, …). P5 never mis-typed anything — `option realtypes off` already produced the stride-consistent `xunknown8 *a3` — and a genuine `TYPE_VOID` pointee is not a residual unknown, so `free`/`malloc`/`memcpy`/`realloc` keep their opaque `void *` | 0/675 and 321/321 stages; libacl `set_acl_fd` (the case that surfaced it) before: `void *a3` with `(*(code *)a3[1])(a3,a0)` — `gcc -std=gnu11 -fsyntax-only` gives 12 "invalid use of void expression" errors + 12 "dereferencing 'void *' pointer" warnings; after: `unsigned long *a3`, same `a3[1]`, 0 errors 0 warnings, matching the source (`struct error_context` is three function pointers at +0/+8/+16). Across 5 whole-binary `decompile-all --mode reliable` runs (libacl, gzip, mydoom, mirai, coreutils `fmt`): `*(void *)` cast sites 737 → 1, `void *` occurrences 1,585 → 198, emitted line counts byte-for-byte unchanged; the single residual is a 10-byte x87-`long double` pointee, where `void` is the designed fallback. Least-contended-run timings (`decompile-all --mode reliable`, 7 interleaved pairs on a loaded box): gzip 2,924 → 2,939 ms (+0.51%), libacl 792 → 793 ms (+0.13%), mirai 4,651 → 4,582 ms (-1.48%); `tests/stages/ghdec-realtypes-pointee.xml` (3/7 pre-fix, 7/7 post-fix) |
| DIV-49 | structured components print in final order (bug fix, no new flag) | `ActionFinalStructure` never called `BlockGraph::orderBlocks` (`block.hh:437`, the first line of `blockaction.cc:2192`), so whenever structuring left more than one top-level component the printer walked the root list in **collapse order** — whichever component the collapse happened to append first. The function then opened in the middle of its body and reached its real entry only after an unconditional `goto`, i.e. emitted the entry as unreachable dead code (`libopencm3` `sdram` `main` opened on the `'p'`/`'P'` command handler and printed `clock_setup(); console_setup(); sdram_init();` after `goto label_8000792;`). The producer of the ordering key was already ported (`BlockGraph::add_block` keeps a composite `index` at the minimum over its members); only the sort was missing. It now runs, keyed on `FlowBlock::compareFinalOrder` (`block.cc:709`): entry component (`index == 0`) first, RETURN-terminated components last, everything else by ascending index, root graph only (upstream does not recurse), stable so the RETURN-terminated components upstream's comparator reports as equal keep a deterministic order. Applies to both structuring engines — `CollapseStructure` and the default-on region structurer hand `ActionFinalStructure` the same root list | 675/675 PARITY OK and 324/324 stages with no re-pin and no per-test opt-out (the stages delta is purely the 6 new keys); across 12 decbench binaries / 3,434 functions the "unconditional `goto` followed by a statement" signature goes 25 -> 0 and "body opens with a label" 29 -> 3 (the 3 residual labels sit at the entry block's own address, a legitimate back-edge target); blast radius 30 of 3,434 functions (0.87%) change text at all, and the whole changed set keeps its goto count and line count exactly (1,172 gotos / 16,123 lines before and after) — the emitted statements are a permutation; interleaved medians, `decompile-all --mode reliable`: O0 diffutils `diff` (526 functions) 7.38 s -> 7.07 s, O2 `bzip2` (113 functions) 15.06 s -> 15.18 s (+0.8%); `tests/stages/ghdec-finalorder-entryfirst.xml` (4 of 6 assertions fail pre-fix, 6/6 post-fix) |
| DIV-50 | renaming reconciles a full-size input with `guardInput`'s leftover pieces (bug fix, no new flag) | gnulib's `rpl_fcntl` at `-O2` produced **no body at all** in 48 of 267 optimized x86-64 binaries (`rename_recurse: set_input_varnode (empty stack): Lowlevel { explain: "Overlapping input varnodes" }`), one function per binary. `Heritage::renameRecurse` materializes an input Varnode for a read with no reaching definition; here that storage is already tiled by the *write-masked* input pieces a previous `Heritage::guardInput` left behind (it tiles a partially-input range, write-masks each piece so `Heritage::collect` stops seeing them, and represents the range by a PIECE concatenation that the rule pools later fold away), so `Funcdata::setInputVarnode` raised `LowlevelError("Overlapping input varnodes")` and the whole function was abandoned. This is **not** a port defect: Ghidra 12.1 headless raises the identical `Low-level Error: Overlapping input varnodes` on the same address of the same binary, which is why it is a DIV and not a Convergence. The leftover pieces are exactly the value the read wants, so the request is now completed into an end-to-end tiling (a fresh input for any hole, as `guardInput` does) and folded into one full-size input with the upstream `Funcdata::combineInputVarnodes` primitive, which destroys the pieces, rewrites concatenating PIECEs into COPYs and repoints every other reader at a SUBPIECE of the whole. Only write-masked pieces fully contained in the request are folded -- a write-masked Varnode is never pushed onto a `VariableStack`, so the rewrite cannot strand a renaming-stack entry -- and every other overlap still raises, exactly as upstream. (The recovered body still reads the spill slot as an undefined `stack - 0x38` local: keeping the va_list spill alive needs `Heritage::guardLoads`/`handleNewLoadCopies`, which kuna does not port and which Ghidra cannot demonstrate here because it fails on the same function.) | 675/675 PARITY OK and 346/346 stages with no re-pin and no per-test opt-out (the stages delta is purely the 8 new keys); across the 267-binary O2 decbench corpus (76,527 functions) the overlapping-input class goes 48 -> 0 functions in 48 -> 0 binaries and total error records 56 -> 8 (the 8 residuals are an unrelated "Unable to load 512 bytes" class, unchanged); 0 functions stop being produced and 48 start; whole-binary text diffs on gzip, coreutils `sort`, diffutils `diff`, findutils `find` and coreutils `nohup` change exactly 1 function each -- the one that used to fail; interleaved medians of 5, `decompile-all --mode reliable`: gzip 4,796 -> 4,792 ms (-0.08%), coreutils `sort` 13,477 -> 13,114 ms (-2.69%), findutils `find` 8,362 -> 8,279 ms (-0.99%); `tests/stages/ghdec-inputtile-overlap.xml` (1 of 8 assertions passes pre-fix, 8/8 post-fix) |
| DIV-51 | short-circuit `0`/`1` select diamonds re-roll into a boolean assignment (`iteboolean`, default-ON) | gcc at `-O0` materializes a source `x = a && b;` as a jump-based select whose constant-`0` arm is the SHARED target of every short-circuit exit, and kuna printed it as an explicit two-arm constant diamond (`if ((a0) && (v2 = a0[1] & 0x100, v2)) v2 = 1; else v2 = 0;`) where the source and Hex-Rays have one statement. P3 `RuleConditionalMove` is the rule that folds `MULTIEQUAL(0,1)` into `zext(cond)`, and its bail here is a **faithful port** of upstream Ghidra (`ruleaction.cc:9424-9439`, `if (inblock->sizeIn() != 1) return 0;` on each MULTIEQUAL input block) -- which is why this is a DIV and not a bug fix. Upstream's bail is also right at the IR level: the constant arm's extra predecessor is a DIFFERENT CBRANCH whose boolean does not dominate the join (folding to `zext(cond)` would be an SSA violation), and reconstructing the whole chain would hoist a guarded load (`p[1]` under `p != 0`) above its guard. The re-roll is therefore done at P8, after structuring, where the chain is already ONE `BlockCondition` that C's own `&&`/`||` renders with short-circuit semantics: `ActionIteBoolean` (`p8_structure/kuna_iteboolean.rs`, ELEM 4119) marks a 3-component gotoless `BlockIf` whose condition is that `BlockCondition` and whose two arms are each a single `COPY` of the constants `1`/`0` to the same integral storage, and `PrintC::emit_block_if_bool` emits `v = ( cond );` (arms `0`/`1` -> `v = !( cond );`). Print-only (an addl-flag on the condition's terminal CBRANCH; no p-code cloned or mutated), so `option iteboolean off` is byte-identical to the pre-DIV-51 render. Requiring the `BlockCondition` is what makes it VALUE-safe and not just shape-safe: `&&`/`||` and the negating `!` always yield exactly 0/1 in C, while a bare leaf condition renders truthy under DIV-37 (`if (x & 0x100)` prints `(x & 0x100)`, worth 0x100) and would store the wrong number -- that bare-leaf shape is also the one P3 already folds. Pointer/float/aggregate destinations, labelled arms, computed arms and multi-statement arms are all declined. Like `iteregion` (DIV-17) it stays a RUNTIME CHOICE: a source that really wrote `if (c) x = 1; else x = 0;` compiles to the same bytes | 675/675 PARITY OK with the flip and no re-pin (one stage test, `ghangr-earlyreturn`, takes a per-test `option iteboolean off` because its witness IS a 0/1 diamond); GED on the mined case `O0-bash-bash-time_command` 55 (recorded) / 26 (today's build) -> **6**, exactly the hand-rerolled pane the triage predicted (28 nodes / 43 edges vs the source's 28/44); corpus breadth over 14 `-O0` decbench binaries / 10,494 functions: 126 functions change text (1.2%) and 160 diamonds collapse (81 positive `v = ( a && b );`, 79 negated `v = !( ... );`), and a mechanical audit of all 160 confirms every emitted expression is boolean-valued at its top level; interleaved medians of 5, `decompile-all --mode reliable`: grep 7,308 -> 7,092 ms (-2.96%), gzip 2,563 -> 2,540 ms (-0.90%), diffutils `diff` 7,659 -> 7,431 ms (-2.98%) -- no measurable cost (the pass is one linear walk of the structured tree and mutates no p-code); `tests/stages/ghdec-iteboolean.xml` (2 of 4 assertions fail with the option off, 4/4 with it on) |
| DIV-52 | one local declaration per ScopeLocal Symbol (bug fix, no new flag) | `PrintC::emitLocalVarDecls` walks HighVariables where upstream `emitScopeVarDecls` walks the ScopeLocal **Symbol** table (printc.cc:2667/2696), which makes one symbol one declaration by construction. When two live ranges of one mapped stack slot did not merge, the shared-storage naming gave both the same name and P5 recovered different types for them, so the function opened with the *same identifier declared twice at two widths* — `unsigned int v12; // stack - 0x24` beside `uint4 v12; // stack - 0x24` in betaflight `applyLedFixedLayers`. That is not compilable C and leaves no way to tell which `v12` a body occurrence means (Ghidra emits one `undefined4 local_24;`, IDA one `int v23;`). The only collapse that existed, `option dedupvardecls` (DIV-7, default on), keys on the fully rendered **line**, so two spellings of one slot both survived by construction — the composite (array/struct/union) collapse beside it was already Symbol-keyed and the scalar case was the documented gap. The scalar collapse is now Symbol-keyed too, inside that same option: highs whose declaration representative resolves to the same containing ScopeLocal Symbol (base-byte containment, the `Funcdata::linkSymbol` query of `funcdata_varnode.cc:1190`) **and** that render the same identifier emit one declaration. Requiring the identifier to match is what makes the collapse incapable of removing the last declaration of a referenced name — the mirror-image failure `verify_declared_vars` was built for, now pinned in the same test. A group that disagrees about the type declares the Symbol's own type (upstream `emitVarDecl`'s `sym->getType()`, printc.cc:1719) unless that type is narrower than the widest storage the group covers, in which case the widest member wins: kuna's `ScopeLocal` ranges can be narrower than the accesses that reach them (upstream `restructure` would have grown the Symbol), and declaring an object smaller than the one the body writes through would be a fresh bug rather than a faithful one | 675/675 PARITY OK and 352/352 stages with no re-pin and no per-test opt-out (the stages delta is purely the 6 new keys); across 14 decbench binaries / 11,874 functions (x86-64 and ARM Cortex-M, -O0 and -O2) the functions emitting a duplicate local declaration go 18 -> 0, and the blast radius is exactly those 18 — no other function changes a byte, 21 declaration lines disappear (35,412 -> 35,391), no function loses a declared name and none gains one. Every survivor is at least as wide as every access it absorbed (`undefined3` / `unsigned short` / `char` shadows of a 4-byte slot become one `unsigned int`), and the campaign case declares `unsigned int v12; // stack - 0x24` exactly once, the same choice as Ghidra's single `undefined4 local_24`. The collapse only runs where two declarations share an identifier, so the Symbol lookups are skipped on almost every function; interleaved medians, `decompile-all --mode reliable`: betaflight STM32F405 (6,387 functions) 69,782 -> 69,754 ms (-0.04%, 5 pairs), bzip2 9,926 -> 9,863 ms (-0.63%, 5 pairs), gzip 3,162 -> 3,211 ms (+1.55%, 9 pairs). `tests/stages/ghdec-symbol-keyed-local-decls.xml` (4 of 6 assertions pass pre-fix, 6/6 post-fix) |
| DIV-53 | the browser inventory shares the decompile/project discovery policy (bug fix, no new flag) | `kuna_wasm list` — the command that fills the web UI's function sidebar — was the one surface that skipped the `decompile-all` driver injections (`listing`, plus the arch-gated `funcstart_patterns`/`aif` on a non-x86-64 target), inheriting `kuna functions`' cheap-enumeration trade. In the browser that trade is wrong in kind: the sidebar is the ONLY way to reach a function, so an entry the inventory omits is a function the UI cannot open at all, while the **same wasm module's** `project` button downloaded a `.c` full of functions the page had never listed. The `list` command now loads exactly as `decompile`/`project` do, so the inventory is by construction the project export's target set (canonical entries; import pointer slots stay in the inventory-only surplus, as DIV-33 defines). Only the `reliable` window (500 KiB–<2 MiB) was ever inconsistent — `aggressive` (<500 KiB) already turns all three options on and `fast` (>=2 MiB) turns all three off, and a mode that names an option still owns it | 0/675 and stages unchanged (the wasm front-end is not a datatest surface); private i386 PE `27d7b038…03ab5` (1,154,048 B, resolves to `reliable`): sidebar 308 entries (286 `plt` + 22 `func`) against its own project export of 3,015 functions — 2,850 of those 3,015 had no sidebar entry; after: 3,158 entries (1,952 `func` / 920 `thunk` / 286 `plt`) and 0 missing. Per-option contribution on that PE (canonical entries): baseline 308, `listing` 308, `aif` 308, `funcstart_patterns` 1,847, `listing`+`aif` 1,514, `listing`+`funcstart_patterns` 2,807, all three 3,158 — i.e. no single injection recovers it. Cost: native `list` 0.19 -> 3.25 s (47 -> 243 MB peak RSS); under Node WASI, the ABI the browser shim implements, 0.60 -> 4.29 s, against the 4.73 s one address decompile on the same binary already cost (that surface always ran the injections). x86-64 control `/bin/bash` (1,396,520 B, `reliable`): 2,503 -> 2,503 entries at 0.31 -> 2.18 s — on x86-64 the arch-gated scans stay off and oracle 5 already covers prologues, so that arm buys consistency, not entries. `--mode reliable` fixture deltas: `entrymain_arm` 10 -> 13, `entrymain_riscv64` 3 -> 6, `plt_sparc64` 17 -> 27, `plt_ppc64le` 14 -> 19, `i386_pie_nl` 139 -> 154, `entrymain_aarch64` 12 -> 13, `sample_aarch64.o` 6 -> 8, `fmt_riscv64` 8 -> 9, `plt_riscv64` 9 -> 10, every x86-64 fixture unchanged; web gates re-run green (parity 19/19 native==wasm byte-for-byte, glue/zip/auto-mode/worker OK); `kuna-wasm`'s `reliable_list_inventory_covers_the_project_export` pins the invariant (`entrymain_arm`: 2 of 12 exported functions unlisted pre-fix, 0 after) |
| DIV-54 | `returndup` default-ON (**supersedes the DIV-18 revert**) | The whole-block gotoless return duplicator (angr SAILR `ReturnDuplicatorHigh`, `p8_structure/kuna_returndup.rs`) becomes a shipped default again. DIV-18 first made it a default and it was reverted on 2026-07-05 after decbench measured **-976 aggregate GED-perfect** across 21,768 firings. That measurement described the *unselective* pass, which also split `return <variable>` epilogues; **#137** then ported angr's `ReturnDuplicatorHigh._should_duplicate_dst` const-return gate (`Funcdata::returndup_is_const_ret`, `substrate/funcdata_block.rs`), which declines every share whose return value is not a literal / COPY chain / phi-of-constants and drops ~40-60% of firings -- precisely the over-eager class the -976 was counting. The revert's justification therefore no longer describes the code, and the option is re-measured from scratch here rather than reinstated on the old evidence. It stays a runtime choice (`option returndup off` is byte-identical to upstream): the merged and split forms compile to the SAME object code, so nothing in the binary says which one the source wrote. `--mode aggressive` deliberately keeps naming the option so the preset re-enables it after an explicit `--option returndup off` | **Decbench A/B** (isolating ablation, same `36641d35` binary in both arms, `decompile-all --json --max-fn-seconds 120`, 536 slices / 52,862 scored functions, scored with decbench's own `GEDMetric`): ON 804,689 aggregate GED / **18,036 GED-perfect** versus OFF 812,445 / 17,619 -- **+417 perfect and -7,756 GED**, 608 perfects created against 191 destroyed (3.2:1), 3,676 functions improved against 2,079 worsened. Harness controls: the 46,005 functions the pass never fires on score identically in both arms, and the published `kuna_update-passwd.c` reproduces byte for byte. Net-positive in **all nine** partitions tested causally (split count 1-8, void vs value return, distinct return values, emitted return count, contains-a-`switch`, all-returns-identical, body size, project, opt level); the single net-negative cell -- a split that de-structures a loop (321 firings, -257 GED) -- had a gate built and scored at +2 perfect / -209 GED and was **not** kept. Full analysis: `docs/decbench/returndup-regression-triage.md`. **Datatests 675/675 PARITY OK with no re-pin**, via per-test `option returndup off` on the 9 upstream files whose merged rendering it changes (16 assertions moved without them: `ccmp`, `orcompare`, `condexesub`, `copytrim`, `condconst2`, `bitfields2`, `partialunion`, `statuscmp`, `union_datatype`); **stages 356/356 PARITY OK** with the same opt-out on 5 two-pass isolation tests (`gh6904-ppc-e500-concat44`, `gh9203-condexe-loopcopy`, `ghangr-1after909-doit-73591e`, `ghangr-earlyreturn`, `ghangr-ite-region-converter-missing-5db28e`) and no baseline re-record; `tests/stages/ghangr-returndup.xml` and `tests/stages/modes-aggressive.xml` already pinned the option explicitly. **Speed** (interleaved A/B on one binary, `decompile-all --mode reliable`, medians; the box runs concurrent builds so the blocked `scripts.pipeline.timeit` form is unusable here -- it reported factor::factor at -33.9% and again at -0.5%, and ls::main at +57.3% where the interleaved measurement gives +2.45%): base-passwd `update-passwd` 494.3 -> 502.9 ms (+1.74%, 7 pairs), coreutils `factor` 2,409.9 -> 2,427.6 ms (+0.74%, 7), coreutils `ls` 6,927.9 -> 7,006.9 ms (+1.14%, 7), gzip -O0 2,389.9 -> 2,453.4 ms (+2.66%, 9), gzip -O2 4,809.9 -> 4,753.3 ms (-1.18%, 7), coreutils `sort` -O2 12,944.2 -> 12,947.3 ms (+0.02%, 7) -- worst case +2.96%, inside the +5% budget. Per-function interleaved medians of 9: `ls` main 247.8 -> 253.9 ms (+2.45%), `factor` factor 111.9 -> 112.1 ms (+0.17%), `update-passwd` xmalloc 103.0 -> 103.4 ms (+0.34%), gzip main 519.8 -> 376.8 ms (-27.5%) |
| DIV-55 | a datatype copy split declines a read-only output (bug fix, no new flag) | `SplitDatatype::testCopyConstraints` (`p3_dataflow/subflow.rs`, upstream `subflow.cc:2390`) now also declines a COPY whose **output Varnode is read-only**. Without it kuna emitted a block of per-byte assignments **storing into a `.rodata` string literal** on a return path where the binary performs no store at all: `s_822f0[0xf] = SUB81(s_822f0[8],7);` in iproute2 `ip` `netns_add` (@0x20e10), 16 of them, on the source line `return -1`. `.rodata` there is `0x7e000..0x95335`, flags `A` and no `W`; the whole binary contains exactly ONE instruction that references `0x822f0` and it is the `movdqa` **load**. The chain: `strings` (P1, default on) plants a typelocked `char[N]` at the literal; the 16-byte SIMD read makes `ram:0x822f0(8)`/`ram:0x822f8(8)` heritaged persistent ranges, so `Heritage::guardReturns` inserts its synthetic `glob = glob` `return_copy` before each RETURN; kuna's own `stackguard` (DIV-14) strips the canary epilogue, leaving a bare-epilogue RETURN block that `ActionReturnSplit` node-splits; and `CloneBlockOps::buildOpClone` copies a flag mask that does **not** carry `return_copy` while `patchInputs` re-inputs the clone from a fresh unique-addressed COPY -- so the clone is neither marked `return_copy` nor address-tied at its own address, and both of upstream's guards miss it. The split then puts each one-byte piece in a different HighVariable, past `Merge::markInternalCopies`, and P9 prints them. The predicate `isReturnCopy()` (which the round-1 triage proposed) fixes nothing: instrumentation shows `rc=true` never reaches a successful split, because the same-address gate already declines the un-cloned form. Read-only-ness of the output is the property that survives the clone, and the invariant is one upstream already recognizes -- `Funcdata::fillinReadOnly` warns `Read-only address (ram,X) is written` -- it is just gated behind `readonlypropagate`. No option: a store the program cannot perform is never a judgment call (`option togglerule splitcopy off` reproduces the pre-fix pipeline for bisection) | **675/675 PARITY OK with no re-pin** and stages 375/375 with no per-test opt-out (the stages delta is purely the 6 new keys). Whole-binary `decompile-all` before/after over 12 corpus binaries / **15,386 functions** (x86-64, -O0 and -O2): exactly **3 functions change**, and they are the 3 the triage found -- `ip` `netns_add` (-18/+1 lines), `ip` `__get_hz` (-34/+1), openssh `sshd` `sub_230a0` (-64/+0); 116 lines removed and 2 added corpus-wide, every other function byte-identical. **112 phantom statements go to 0** and no real store is lost: after the fix every surviving assignment whose LHS is an address-named global lands in a **writable** (`WA`) section -- 22 in bash `.data`/`.bss`, 6 in tar `.bss`, 4 in coreutils `sort` `.data`, 1 in dpkg `.data`, 1 in sshd `.bss` -- and all of those counts are unchanged by the fix. -O0 `ip` (1,962 functions) is byte-identical, matching the triage's finding that the preconditions need an -O2 SIMD copy out of a literal. **Speed**: no mechanism for cost (one varnode-flag test per candidate COPY, and a declined split does strictly less work) and none measurable -- interleaved A/B medians on a box running concurrent agents: `ip` `netns_add` 1,349.4 -> 1,334.9 ms (-1.08%, 15 pairs), `ip` `__get_hz` 1,002.1 -> 1,006.2 ms (+0.41%, 15), openssh `sshd` `sub_230a0` 2,371.1 -> 2,117.5 ms (-10.69%, 15) after +5.25% on a 9-pair run of the same target, coreutils `sort` `decompile-all --mode reliable` 5,735.1 -> 5,816.3 ms (+1.42%, 11) after +9.17% on a 5-pair run, coreutils `ls` 9,179.3 -> 8,802.5 ms (-4.10%, 5). The noise floor is the honest caveat: a **byte-identical** control function (`ip` @0x20780) moved -12.18% and -20.23% across two runs, and `sort`'s output is byte-identical in both arms. GED is unchanged by construction (removing statements moves no block or edge): `O2-noinline-iproute2-ip-netns_add` 8.0 -> 8.0. `tests/stages/ghdec-returncopysplit.xml` (3 of 6 assertions pass pre-fix, 6/6 post-fix) |
| DIV-56 | a diamond whose condition was concatenated with its predecessor still re-rolls (`itecondlist`, default-ON) | `iteregion` (DIV-17) and `iteboolean` (DIV-51) both reach the diamond's condition component through one helper, `p8_structure/kuna_iteregion.rs (leaf_bblock)`, which descends a `BlockList` of ONE component but returns `None` for a list of two or more. The collapse structurer concatenates a just-collapsed predecessor together with the following condition block into a two-component `BlockList`, and that list becomes component 0 of the NEXT `BlockIf` -- so in a RUN of structurally identical diamonds every other one was declined and kuna re-rolled exactly ceil(N/2) of N (measured N=1..8: 1 1 2 2 3 3 4 4). The tell was that WHICH diamonds were missed depended on the neighbouring structure rather than on the diamond itself. `p8_structure/kuna_itecondlist.rs (cond_list_tail)` (ELEM 4121) now descends a condition-position `BlockList` to its LAST component before the leaf test -- `iteregion`'s `cond_cbranch`, and `iteboolean`'s `BlockCondition` gate (NOT inside `cond_terminal_cbranch`, whose leaf arm is only ever reached for the individual clauses of an already-identified chain). The arms keep the strict single-statement match, the labelled-goto-target guard still applies to the descended leaf, and the match keeps the OUTER list as its `cond_block`. Sound because the rewrite is print-only and the printer already renders this shape: `PrintC::emit_block_if` emits the condition component twice, once under NO_BRANCH (its statements) and once under ONLY_BRANCH (its branch), while `PrintC::emit_block_ls` emits every component under NO_BRANCH but only the LAST under ONLY_BRANCH -- so the leading components print as ordinary statements ahead of the `if` header either way, and `emit_block_if_ite`/`emit_block_if_bool` do the same two emissions on the same block. Restricted to `BlockList`: `PrintC::emit_block_graph` ignores ONLY_BRANCH and emits every component, so descending a `BlockGraph` would print the whole graph twice. It is a strictly narrower match than the two options it widens, so `option itecondlist off` can only ever fold FEWER diamonds, never different ones, and is byte-identical to the pre-DIV-56 render | **675/675 PARITY OK with the flip and no re-pin and no per-test opt-out**; **stages 381/381 PARITY OK** (the delta is purely the 6 new keys of `tests/stages/ghdec-itecondlist.xml`, which folds 2 of 3 identical diamonds with the option off and 3 of 3 with it on); `make rust-test` 4,424 passed / 0 failed; GED on the mined case `O0-bash-bash-shell_initialize` **46 -> 24** (-48%): all three source-identical `x = a||b||c||d;` call arguments now emit as one statement each instead of one plus two explicit `if (...) v = 1; else { v = 0; }` diamonds (the O2-noinline sibling was already GED 0 and is unmoved). Corpus breadth over 4 binaries / 12,241 functions (`decompile-all --mode reliable`): 53 functions change text (0.43%), 59 additional diamonds fold (coreutils `ls` +5, iproute2 `ip` +3, `bash` +17, betaflight STM32F405 ARM +34), ternaries 381 -> 434 and `iteboolean` re-rolls 124 -> 134, with error/diagnostic line counts unchanged in every binary (no function starts or stops producing output). A MECHANICAL audit of every diff hunk (`docs/features/itecondlist/audit.py`) requires each to be either a header comment whose ternary/re-roll count went UP or a removed `if (C) V = A; else { V = B; }` replaced by exactly one `V = (C) ? A : B;` / `V = (C);` / `V = !(C);` with the SAME destination, condition and arm expressions in order: 52 header-only hunks and 59 folded diamonds, **0 problems**; the two hunks the audit could not classify automatically were hand-checked (a `bash` comma-expression condition the arm regex cannot parse, and one function whose two accumulated `iteboolean` warning lines collapse into one with a byte-identical body). Speed: `scripts.pipeline.timeit` on the target function 356.33 -> 354.28 ms (**-0.58%**, 7 samples, inside the +5% budget); interleaved whole-binary medians of 5 pairs, `decompile-all --mode reliable`: coreutils `ls` 7,193 -> 7,220 ms (+0.38%), gzip 2,881 -> 2,873 ms (-0.28%) -- the predicate is one extra pointer chase per candidate `BlockIf` and mutates no p-code; `tests/stages/ghdec-itecondlist.xml` (2 of the 6 assertions describe the bug with the option off, 6/6 with it on) |
| DIV-57 | `peimportcall` (new kuna option, default-ON) | A Windows PE reaches its imports through an **Import Address Table slot**, which is *data*: `call dword ptr [slot]` lifts to a `CALLIND` whose target is the contents of a global, unlike an ELF PLT stub or a Mach-O `__stubs` entry, which are code and resolve as ordinary direct calls. The only pass that resolves such a target, `ActionDeindirect` (`coreaction.cc:1235`), gates its `queryExternalRefFunction` arm on `isPersist() && isExternalRef()` -- and `Varnode::externref` was set **nowhere in the workspace**, because Ghidra sets it from an `ExternRefSymbol` (`Scope::addExternalRef`, an `engine_unavailable` stub in kuna). So every Windows API call in every PE stayed an unnamed `(*dat_4112c4)(0)`: no name, no prototype, and -- the damaging part -- no no-return flow effect, so a function ending in `ExitThread`/`ExitProcess` kept its fall-through and swallowed the whole of the next function. Three parts, all behind the one gate: (a) `loader/kuna_peimportcall.rs (PeImportCallPass)` reports one `[slot, slot+ptr)` range per import-descriptor entry and the commit ORs `Varnode::externref` over each in the symbol-table property map -- the same `Database::setPropertyRange` the loader's read-only section ranges use, so no second symbol is mapped at the slot and `ActionDeindirect` resolves against the `FunctionSymbol` `pe_iat` already registered at that VA (kuna's `query_function` keys on the Varnode's own address where upstream indirects through `ExternRefSymbol::refaddr`); (b) `Database::build_global_query` now carries the resolved callee's no-return flag into `GlobalEntry`, which `Architecture::query_function` puts on the prototype it hands `ActionDeindirect` -- upstream returns the callee's live `Funcdata`, so it never had to, and without it `deindirect` took its `lateRestriction` success path and never scheduled the restart whose re-flow plants the artificial halt; (c) upstream's PE-only `PEFunctionsThatDoNotReturn` is vendored verbatim and matched against a **PE/COFF object only**. kuna merges the PE and Mach-O name sets into one shipped file where upstream keeps them separate; rather than widen that merged file with `ExitProcess`/`KeBugCheck`/the MSVC-mangled `std::_Xbad_alloc` family -- Windows names a Mach-O would then be matched against -- the Windows list stays its own file on its own format arm. PE/COFF-only by construction (the pass is registered only for those formats and self-gates in `run`), so every ELF and Mach-O target is byte-identical either way; `option peimportcall off` restores the pre-DIV-55 rendering byte for byte on a PE too | **675/675 PARITY OK with the flip and no re-pin, and 374/374 stages** (the stages delta is purely the 5 new keys). **Benchmark**: the mined case `O0-mydoom-mydoom-mmsender_th` goes GED **37 -> 0 (perfect)**, and its un-triaged ida-pool sibling `O0-mydoom-mydoom-sync_visual_th` **39 (recomputed; 34 recorded) -> 0 (perfect)** -- both now matching IDA, Ghidra and angr, which all scored 0. **Corpus breadth**, whole-binary `decompile-all` over the two PE projects in the decbench corpus: unnamed `(*dat_...)()` IAT-slot call sites **927 -> 8** (mydoom 501 -> 4, dexter 426 -> 4) with **71 (mydoom) and 90 (dexter) distinct Win32 API names** appearing in the emitted C where there had been zero (`ExitProcess`, `CloseHandle`, `HeapFree`, `RegOpenKeyExA`, `WideCharToMultiByte`, ...); mydoom 5,247 -> 4,768 lines and dexter 3,422 -> 3,115 with **no function gained or lost** in either (156 and 109); 8 functions stop overrunning a no-return API (`// no-return` marks 9 -> 14 and 7 -> 10) and the phantom loops they had absorbed disappear (mydoom `while` 65 -> 60); gotos never increase (23 -> 23, 20 -> 19); of the 79 functions that shrink, the largest are dexter `Uninstall` 152 -> 19 lines (now 16 statements ending `ExitProcess(0);`) and mydoom `sub_402a62` 169 -> 64 and `sub_402eda` 96 -> 28; exactly two functions grow, both by one line, and both because a named import now takes a typed argument through a temporary (`a1 = (*dat_411464)(*(unsigned short *)(a0+a1)); a1 &= 0x3fff;` -> `v2 = *(unsigned short *)(a0+a1); a1 = ntohs(v2) & 0x3fff;`). On the shipped MinGW PE fixtures: `pe_imports_stripped.exe` 79 -> 3 unnamed slot calls, `pe_dwarf.exe` 50 -> 0. **Controls**: option-ON output is byte-identical to option-OFF on every non-PE target measured (coreutils `b2sum`, `mirai`, `tar` -O2), and option-OFF is byte-identical to the pre-change binary on both PEs. **Speed**: `scripts.pipeline.timeit` on the target function, 15 repeats, 387.96 -> 380.58 ms (**-1.9%**, within the +5% budget) -- an earlier 7-repeat run on a loaded box reported +49% and did not reproduce in three re-runs (+0.67%, -0.99%, -3.74%), so the interleaved form is quoted alongside: per-function interleaved medians of 11 pairs 338.0 -> 340.7 ms (+0.79%), and whole-binary interleaved medians of 7 pairs are **negative** because the truncated functions are smaller work -- mydoom 1,820 -> 1,689 ms (-7.23%), dexter 1,552 -> 1,520 ms (-2.09%), ELF control `b2sum` 5,663 -> 5,575 ms (-1.55%). `tests/stages/ghdec-peimportcall.xml` (2 of 5 assertions fail with the option off, 5/5 with it on) |
| DIV-58 | no `$$undef` placeholder survives the naming pass (bug fix, no new flag) | `Scope::buildUndefinedName` (`database.cc:2854`) names a Symbol created with an empty name `$$undef` plus 8 hex digits. Upstream that string is **unobservable**, for two reasons kuna inherits neither of: `ActionNameVars::apply` renames every still-undefined Symbol before it returns (its final statement is `localmap->assignDefaultNames(base)`, coreaction.cc:3079), and `PrintC` reads `sym->getDisplayName()` **live** at emit time. kuna caches the name per HighVariable (`HighVariable::kuna_name`) and its port of `ScopeInternal::assignDefaultNames` (`Database::assign_default_names`) had **zero call sites**, so the placeholder reached emitted C, the `variables` array of `decompile-all --json`, and `decompile-project`'s `.asm` stack-frame comments. `$$` is not a legal C identifier character, and the leak was worse than cosmetic: the cache is written by `ScopeLocal::resolve_default_name_override`, which renames the covering Symbol only for a WHOLE-symbol cover (the C++ namerec gate `sym->isNameUndefined() && high->getSymbolOffset() < 0`) and otherwise returns whatever the Symbol is called at that moment -- so a partial cover visited before its whole-cover sibling froze the placeholder while the sibling renamed the shared Symbol to its `vN`. One stack slot, two identifiers, and a body that **reads a name nothing ever writes**: in `tar` `xstrcat` the x86-64 `va_arg` loop tested and incremented `$$undef00000000` while the induction update landed on `v12`. The instrumented proof is a backtrace on `set_kuna_name` plus the `--json` `variables` array, which shows exactly ONE Symbol at that offset and no `$$undef` Symbol at all -- so the printed placeholder corresponded to no live Symbol, ruling out the second-Symbol reading. The gate itself is a **faithful port** and is left alone; relaxing it would move when a Symbol consumes `base` and renumber every later local. Instead `ActionNameVars` grows the tail it was missing (`p6_variables/kuna_undefname.rs`): wire the catch-all `assignDefaultNames`, then re-resolve every HighVariable still holding a placeholder through the same `queryProperties(addr,1,usepoint)` containment query that bound it -- kuna's stand-in for the printer's live read. Both steps run after every `vN` is handed out and after the spacebase attach pass, so neither renumbers an existing local (a Symbol named by the catch-all takes the highest index in the function) and every `&symbol` render stays byte-identical. No option: an identifier containing `$$` is not valid C under any naming policy (`namestyle ghidra` reaches the same tail and produces upstream's `xStack<offset>` form) | **675/675 PARITY OK with no re-pin and no per-test opt-out**, and stages 386 -> 394 where the delta is purely this test's 8 new keys; `make rust-test` green (4,427 tests), `make check-spec` OK, `kuna catalog --check` OK. Breadth over a stratified decbench sample -- one binary per project per optimisation level, **116 stripped binaries / 90,808 functions**, x86-64 and ARM Cortex-M, O0/O2/O2-noinline -- before vs after on the same tree: binaries emitting a placeholder **73 -> 0**, functions **371 -> 0**, occurrences **2,142 -> 0** (1,503 reads / 639 writes), functions whose ScopeLocal Symbol table itself carried one **93 -> 0** (125 -> 0 Symbols); 90,808 functions produced in both arms and 0 error records in either, so nothing stopped being emitted. The blast radius is the same functions and nothing else: twelve binaries (the eight leakiest plus four clean controls), 18,590 functions, decompiled whole in both arms and diffed function by function -- **162 changed, and every one of the 162 had a `$$undef` identifier in its before text (0 unexplained)**; the function set is identical in both arms, error-record counts match (2/2), all four controls are byte-identical, and stack-local declaration lines go 16,828 -> 16,612 as the printer's existing whole-sibling guard folds the duplicate declarations the two names had been keeping apart. (A first pass with the watchdog ENABLED reported 3 'unexplained' changes; all three were per-function timeouts in the loaded before arm -- an aborted function has no `code`, so it diffs against a real body with no placeholder to match -- and re-run with `--max-fn-seconds 0` they are ordinary placeholder fixes, one of them byte-identical.) `tests/stages/ghdec-undefname.xml` carries both mechanisms as real bytes (tar `xstrcat` for the stale cache, iproute2 `set_qlen` for the unwired catch-all): **3 of 8 assertions pass against the pre-fix build, 8/8 with the fix**, and pass 2 (`option dedupvardecls off`) pins that neither witness depends on the P9 declaration collapse. Benchmark: GED is a CFG-shape metric and a rename cannot move it -- `scripts.decbench.rescore` on `O2-noinline-iproute2-ip-netns_add` (a case whose stored pane carries the placeholder) gives before 8.0 / after 8.0, **delta 0.0**, with no metric drift. Speed (interleaved before/after medians, `decompile-all --mode reliable`; `scripts.pipeline.timeit` flips an option on ONE binary and so does not apply to a default-path fix): tar-O2 52,998.0 -> 50,085.7 ms (-5.50%, 7 pairs), ip-O2-noinline 30,959.3 -> 31,025.3 ms (+0.21%, 7 pairs), gzip-O2 4,733.7 -> 4,891.3 ms (+3.33%, 7 pairs) -- the added work is one bounded `nametree` range query plus one high walk that exits immediately on any function with no placeholder |
| DIV-59 | a C++ (namespaced) callee renders its qualified name at the call site, and `--addr` names the function it addresses (bug fix, no new flag) | `kuna decompile`/`decompile-all` printed `sub_<addr>` at every call to a demangled C++ function on an UNSTRIPPED binary, for a name kuna already had: `kuna functions` listed `Account::deposit`, `kuna decompile <bin> 'Account::deposit'` resolved, and `--mode reliable` rendered `d = Account::deposit(a,amount);` -- while the default (`auto` -> `aggressive` under 500 KiB) rendered `d = sub_271c(a,amount);`. Not a demangler defect: a demangled name with no namespace (`_Z8topleveli` -> `toplevel`) rendered fine, and there is no identifier filter on `::` anywhere. The mechanism is **scope shadowing in the gated-commit seam**. `read_loader_symbols` installs a demangled funcsym in its NAMESPACE scope (`std::terminate` = base `terminate` in scope `std`); a discovery pass (`fast_funcdisc` / `funcstart_patterns`, both in `AGGRESSIVE_OVERRIDES`) then re-reports the same VMA, and `commit_analysis_output`'s discovered-entry arm named it `sub_<addr>`, resolved that synthetic name to a scope -- no `::`, so GLOBAL -- and probed `find_function(scope, addr)` in that scope ONLY. The real symbol was in scope `Account`/`std`, so the probe missed it and installed a DUPLICATE generic FunctionSymbol in GLOBAL at the same address; `FlowInfo::queryCall` resolves through `Database::find_function_across_scopes`, which searches global FIRST, so the duplicate shadowed the real name for every call site. Either discovery pass alone was enough (both report the VMA), which is why `--option fast_funcdisc off` alone did not restore the names. The in-source comment claiming the probe "still lets a real `.symtab`/`.dynsym` name win on a non-stripped binary" was false for every `::`-qualified symbol, and `find_function_across_scopes` already documented the hazard verbatim -- the two facts had never been connected. Fix: the probe resolves ACROSS SCOPES (the port of C++ `Scope::queryFunction`, which spans the scope tree), so an entry already known under a namespaced name is recognized as present and no placeholder is installed over it. Discovery is unchanged -- the passes still run, still find the same VMAs, a genuinely unnamed find is still `sub_<addr>`, and `register_symbol` is untouched so `sub_<addr>` remains a working selection alias. A second, independent defect closed in the same PR: `IfcAddrrangeLoad` jumped straight to `Architecture::name_function`, so `kuna decompile <bin> --addr 0x271c` printed a `sub_271c` header on an UNSTRIPPED binary while the by-name path printed `Account::deposit`; it now prefers the qualified FunctionSymbol installed at that address (an explicit `load addr <vma> <name>` still wins). No option: this is the driver tier, which has no settable axis (all 76 `phases.toml` rows carry an engine phase P1-P9), the post-fix rendering already ships under `--mode reliable`, and a synthetic placeholder shadowing a real symbol at the same address is wrong under every naming policy including `namestyle ghidra` | **675/675 PARITY OK and stages PARITY OK with NO re-pin of either baseline** -- structurally guaranteed: every datatest/stage file is an XML `<binaryimage>`, whose bootstrap never produces an `AnalysisOutput`, so `commit_analysis_output` is a no-op there and neither corpus can reach the defect (which is also why nothing caught it: `verify_noreturn_demangle` drives the CONSOLE with shipped defaults, i.e. no discovery pass, and printed `std::terminate()` and passed, while the CLI on that same fixture printed `sub_401070()`). `make rust-test` green, `make check-spec` OK, `kuna catalog --check` OK. Blast radius, `decompile-all` before vs after over 13 binaries with every changed line classified by script: the 8 plain-C binaries are **byte-identical** (including `/bin/grep`, 35,070 lines, and `/usr/bin/ls`, 14,788 lines) -- structurally, a C symbol has no `::`, so the global-only probe always found it and no duplicate was ever created; the 5 C++ binaries change 184 lines, of which **181 are call-target renames** (`sub_2170` -> `std::runtime_error::runtime_error`, `sub_401136` -> `foo::Bar::baz`, `sub_4011fa` -> `app::Guard::throw_it`) and **3 are address-taken renames** (`sub_2ca0` -> `perimeter`, `sub_401120` -> `~runtime_error`), with 0 unexplained. Every C++ diff is exactly -N/+N: no line added or removed, no structural or signature change. The 3 address-taken lines render the symbol's BASE name because `Funcdata::link_spacebase_symbol` binds `name_for_global_varnode` (unscoped) where the sibling `ActionNameVars` path uses `name_for_global_varnode_scoped` + `kuna_qualify_global_name`; that gap is PRE-EXISTING and unrelated -- on `main` the same fixture already emits `{virtual override thunk}` in exactly that position and `void Account::~Account(void)` as a function header -- and is recorded as a follow-up in `docs/features/cppcallnames/analysis.md`. Recompilability: the `decompile-project` C export of a C++ binary was already not C (316 `gcc -fsyntax-only` errors before) and its **`.h` is bit-identical before and after** (122 `::` lines, 22 `~ident`, 3 `{...}` in both), because the header is built from the canonical function names this change does not touch; the `.c` bodies gain `::` at call sites (246 -> 410 lines, 316 -> 407 errors), which is what IDA 9.2 and Ghidra 12.1 print for the same functions. The C export is untouched on every axis (0 `::`, 8 errors, before and after), so no C-binary recompile metric can move. New gate `verify_cppcallnames.rs` over the already-vendored `cpp_noreturn_x86_64` with the discovery passes enabled in the live-CLI order: the call-site render, the `load addr` header, and a control that discovery still names its genuinely-unknown finds `sub_<addr>` and an explicit `load addr <vma> <name>` still wins. One existing test changed with it and is worth naming: `verify_multiformat_dwarf` used `load addr` as its "no-DWARF-name baseline" and asserted `sub_140001550`/`sub_0` -- a baseline that only held because `load addr` ignored the symbol table, i.e. it was pinning the bug. Its baseline arm now turns the pass off (`option dwarf off`), which is what it always meant: on the PE nothing else names that address so the placeholder assertion is unchanged and now truthful, and on the Mach-O the `--redefine-sym`ed symtab entry `_l0` remains, a sharper statement of the same claim (`first_byte` exists only in the debug info). Both DWARF-recovery assertions are untouched. Speed (interleaved before/after, MINIMUM of 11 pairs; `scripts.pipeline.timeit` flips an option on one binary and so does not apply to a no-option fix, same as DIV-58): `decompile-all` probe_O0 (C++, ~150 namespace scopes) 358.1 -> 356.5 ms (-0.46%), cpp_noreturn 111.7 -> 110.4 ms (-1.14%), eh_lsda 118.9 -> 117.6 ms (-1.12%); `kuna functions` probe_O0 -- load + commit only, the tightest isolation of the changed code -- 149.2 -> 142.8 ms (-4.29%). Earlier 5-pair medians on a box shared with concurrent agents reported +40.96% on probe_O0; the 11-pair minimum resolves it to -0.46%, and the C rows are flat throughout (/usr/bin/ls -1.32%, /bin/grep `functions` -0.06%). Worst delta -0.46%, budget 5%. The added work is one probe per discovered entry; `find_function_across_scopes` still hits the global scope first and walks the rest only on a miss |
| DIV-60 | `cppproto` (new kuna option, default-ON) | kuna's DWARF pass keyed every recovery off a subprogram DIE's own `DW_AT_name`. That is right for C and wrong for C++: an out-of-line member or namespace DEFINITION is emitted with only `DW_AT_specification` pointing at its in-class/in-namespace declaration, so it carries no name of its own and the `snap.name.is_empty()` guard dropped the whole DIE -- name, typed signature AND `DW_OP_fbreg` stack locals -- before any binding was attempted. On a `-g` C++ binary that is most of the program (`Account::deposit`, `db::inner::scaled_add`, `Shape::shape_count`), and the `static` members prove the artificial-`this` DIE was never the cause. A SECOND, independent defect only the templates expose: the prototype was parked by NAME through the global scope while the read side is keyed by ADDRESS -- kuna files the demangled `maxof<int>` as `maxof`, and `find_create_scope_from_symbol_name` files `Account::deposit` in a nested scope `Account` that `query_function_by_name` never descends into. `analyzers/dwarf/kuna_cppproto.rs` fuses a definition with its `DW_AT_specification` declaration (a single hop -- the reduction of Ghidra's `DIEAggregate`), builds the source name from the DIE's namespace/class ancestry (`DWARFName`), and parks the result by `DW_AT_low_pc` (`set_function_prototype_pieces_at`), with both prototype READ paths widened past the global scope -- inert before this arm, because a by-name park can only ever reach a global symbol. Four type-mapper corrections ride along: `DW_TAG_class_type` maps like a structure and a C++ reference like a pointer (both are `DWARFDataTypeImporter`'s own reductions, and without the first every `Foo *this` was `void *`); transparent `typedef`/`const`/`volatile`/`restrict` hops stop consuming the depth-3 recursion budget, because a `const` member's `this` is `const Account *const`, four DIEs deep -- the same cap that made plain C `char **authors`, `idx_t *pn` and `mbstate_t *ps` render `void *`; an anonymous aggregate is named after the typedef it was reached through (`__mbstate_t`, not a shared `anon_struct`); and a parameter whose type will not map, or maps to a zero-width opaque aggregate, degrades to an `undefined<n>` at the DIE's own byte size instead of discarding the whole signature. `DW_AT_abstract_origin` is deliberately NOT followed for the subprogram link: a GCC IPA clone is also a nameless subprogram, and in coreutils `fmt` `put_word.isra.1`'s TWO parameters both point at the ONE source parameter `Word *w`, which produced `put_word(Word *w,Word *w)` and pushed the wrong callee signature into `put_line`. The pass runs at `load file`, upstream of the `option` commands, so its C++ facts are stashed in `AnalysisOutput::cpp_dwarf` and the gate is applied where they are COMMITTED; `option cppproto off` restores the name-only walk byte for byte | **675/675 PARITY OK with no re-pin and no per-test opt-out** (the analysis tier is bound to the real-ELF bootstrap; the `<binaryimage>` datatest path never calls `run_default_analyses`), stages 394/394 PARITY OK, `make rust-test` green, `make check-spec` OK (lenient + strict), `kuna catalog --check` OK. **Breadth** -- whole-binary `decompile-all` off vs on with every hunk classified: coreutils `fmt` (real C, `-O2 -g`, 169 functions) **9 changed, all improvements** (four `void *` parameters resolved to their real pointee, one DWARF parameter name), 0 new errors, 0 arity drops; a C++/STL probe at `-O0` (190 functions) 83 changed and at `-O2` (72 functions) 7 changed, 0 new errors and 0 arity drops in both; `dwarf_globals_x86_64` (small C `-O0 -g`) byte-identical. The sweep is what found the three defects the witness could not: the IPA-clone aliasing above, `std::vector<int>::emplace_back<int>` losing its argument because the expanded pack lives inside a `DW_TAG_GNU_formal_parameter_pack` grouping DIE, and `std::vector<int>::_M_realloc_insert` failing outright (`dereferencing a null/sentinel space pointer`) on a zero-width by-value class parameter. **Speed**: the DWARF walk runs identically either way (the facts are always computed, then dropped at commit when off), so the only marginal cost is the commit -- `scripts.pipeline.timeit` 147.43 -> 149.78 ms on the witness (**+1.59%**) and 472.91 -> 414.47 ms on coreutils `fmt` (**-12.36%**), both inside the +5% budget, with interleaved min-of-15 deltas of **+1.72%** (witness) and **-0.83%** (`fmt`); single-shot medians swing hard with machine load (an early `fmt` run reported +38.6% and did not reproduce in four re-runs), and where the on side is genuinely faster it is because a locked prototype skips output-trial recovery. Covered by `kuna-console/tests/verify_cppproto.rs` (7 two-pass assertions over the new `cppproto_x86_64` fixture; no stage XML, because the datatest path cannot reach the analysis tier) |
| DIV-61 | `fdeinterior` (new kuna option, default-ON) | A kuna `FunctionSymbol` is an **entry address with no extent** -- `Database::add_function` takes an address and `find_function` answers *is a function registered AT this address*, never *is this address INSIDE one* -- so every discovery oracle's dedup is an equality test and a candidate 12 bytes into a body passes it. Three oracles do exactly that on ordinary compiler output, and `--mode auto` turns all three on for anything under 500 KiB by resolving to `aggressive`: `eh_frame_full` promotes each `.gcc_except_table` **landing pad** to a top-level function (it is designed to -- "a landing pad sits mid-function"), `aif` starts one at the first undecoded byte of an unwinder-only region (routinely NOT an instruction boundary), and the prologue patterns match an aligned `push rbp; mov rbp,rsp` inside a larger body. The result inherits its parent's live frame pointer, so it decompiles with an undefined `rbp` and every local is a garbage dereference: on a `g++ -O0` probe, `probe_try_catch` `[0x2acf,0x2ba7)` yields `sub_2b0b` / `sub_2b1a` / `sub_2b8a`, and `sub_2b0b`'s body is `*(unsigned int *)(v3 + -0x20) = 0xffffffff;` with `v3` never assigned. The defect survives stripping, so a per-function benchmark scores it. `.eh_frame` carries the missing extent: each FDE records ONE function's `[pcBegin, pcBegin+pcRange)` (one `.cfi_startproc`/`.cfi_endproc` pair), so an entry strictly inside one is not a function on the unwinder's own authority -- IDA Pro's model, where `get_func(0x2b0b).start_ea == 0x2acf` (Ghidra 12.1 returns no containing function at all here; its `GccExceptionAnalyzer` errors on this PIE, a Ghidra bug rather than a model). `analyzers/entry/kuna_fdeinterior.rs` reuses `scan_eh_frame_starts`'s record walk and adds the one field it skips -- `pcRange`, decoded with the CIE `R` encoding's FORMAT bits only since it is a length, never its pc-relative application -- and the commit filters the FULLY MERGED entry set (after the deferred Listing consumers, because one producer is `aif`). Not every FDE describes one function: the linker gives the WHOLE PLT a single FDE and every stub inside it is real, so a range is used only when no other named function start, no other FDE `pcBegin`, and no `.plt`/`.plt.sec`/`.plt.got`/`.iplt`/`.MIPS.stubs` section falls inside it; an entry AT an FDE start is never suppressed, so oracle 3's own product is preserved. ELF-only. `option fdeinterior off` restores the previous discovery set exactly | **675/675 PARITY OK with the flip, no re-pin and no per-test opt-out**, and stages 394/394 -- the XML `<binaryimage>` bootstrap never runs the analyzer tier, so the datatest oracle is structurally untouched; `make rust-test` green, `make check-spec` OK (strict), `kuna catalog --check` OK. **Breadth**: `kuna functions` off vs on over 72 stripped x86-64 ELF binaries from the decbench O0+O2 trees (coreutils, bash, gzip, tar, grep, diffutils, openssh, e2fsprogs, gnutls, dpkg, zlib, bzip2, findutils, kmod) -- entries 36,568 -> 35,538, **1,030 dropped, 0 added**, and every dropped entry classified against the matching UNSTRIPPED binary's `FUNC` symbols (address AND size): **0 are a real function's entry**, 1,030 fall strictly inside a real function body, 52 are not even an instruction boundary. C++: `probe_O0.stripped` 189 -> 162 (14 landing pads + 13 interior, 0 real), `probe_O2.stripped` 71 -> 64, `big_O0.stripped` (STL + exceptions) **599 -> 449** (98 landing pads + 52 interior, 0 real -- 25% of the reported function list was junk), `big_O2.stripped` `--mode aggressive` 161 -> 86. Static glibc `--mode aggressive` 1,358 -> 1,087 with 0 real and **0 named** functions lost (the static `.plt`/`.iplt` is untouched); PE `mydoom.exe` 280 -> 280 and `dexter.dll` 270 -> 270 (ELF-only => inert). **Emitted C**: whole-binary `decompile-all` off vs on -- `big_O0.stripped` 599 -> 449, `big_O0` 600 -> 450, `probe_O0.stripped` 189 -> 162, `probe_O0` 190 -> 163, gzip 263 -> 248, b2sum 228 -> 216, coreutils `fmt` 198 -> 191 and `fmt`-O2 151 -> 151, **0 functions added, 0 surviving bodies changed and 0 surviving names changed** in any of the eight (both arms on the same build, so DIV-59's C++ callee renaming and DIV-60's `MAX_TYPE_DEPTH` change -- which is why `fmt` is measured here -- both cancel). **Re-confirmed against DIV-60's widened symbol set**: `cppproto` recovers out-of-line C++ member/namespace definitions, so `big_O0` now has 447 of its 450 surviving entries named (`probe_O0` 161 of 163) -- the eligibility rule's input -- and the filter still drops 150 with 0 real and 0 named lost, while the stripped counterparts are unchanged at 449 / 162. **#259 (`tailcallentry`) non-regression, measured**: the same surface `docs/features/tailcallentry/` used (`kuna functions --mode aggressive`) with `--option tailcallentry on`, off vs on, over all 98 ARM images in the three decbench optimisation trees -- entries **57,578 -> 57,578, zero images changed**; structural, because bare-metal ARM unwinds through `.ARM.exidx` and only **3 of 98** images carry any `.eh_frame` FDE. **Speed** (the added work is one linear `.eh_frame` record walk -- the same section the always-on FDE-pcBegin oracle already reads -- plus one binary search per discovered entry, and the savings are the suppressed entries never being decompiled, so every measurement is negative or neutral): `scripts.pipeline.timeit` on the probe target, 15 samples, 151.76 -> 107.33 ms (**-29.28%**); interleaved `kuna functions` medians, gzip-O0 450.9 -> 449.3 ms (-0.35%, 15 pairs), tar-O0 164.9 -> 158.5 ms (-3.87%, 15), `big_O0.stripped` 343.9 -> 269.3 ms (-21.68%, 15), static glibc `st_O2.stripped` 244.0 -> 244.1 ms (+0.06%, 11); interleaved whole-binary `decompile-all` medians, gzip 2,867.8 -> 2,746.7 ms (-4.22%, 9 pairs), b2sum 5,719.1 -> 5,439.4 ms (-4.89%, 9). An 11-sample `timeit` run taken while the workspace suite was running reported +4.67% and did not reproduce. `decompiler/crates/kuna-console/tests/verify_fdeinterior.rs` is the two-pass e2e (option off: the landing pad `sub_4012e2` registers and decompiles; default: it is absent while its FDE owner `sub_4012d6` still decompiles; plus the no-named-function-lost property). Bundle: `docs/features/fdeinterior/` |
| DIV-62 | `cppsig` (new kuna option, default-`proven`) | kuna applied **no** demangled C++ signature. The demangler reduces a mangled symbol to its qualified NAME only -- a hard requirement of the `::`-scope splitter, which nests on every `::` and would otherwise build junk scopes out of a signature tail -- and `demangle_raw`, the full c++filt form, had shipped with **no production caller** since the demangler landed, its own header recording the signature application as "a deferred follow-up". The cost is a **stripped** C++ shared library, where the exported `.dynsym` mangled name is the ONLY signature source left: on stripped `google/leveldb` `-O0`, functions whose first parameter is a NAMED pointer type ran ghidra 188 / ida 150 / binja 60 / angr 0 / **kuna 0** across 527 functions, and 37 of ghidra's 42 outright wins were `.dynsym`-exported (on the 112 non-exported `this`-shaped functions ghidra gets 1) -- so the lever is the demangler, not type inference, which the plain-C control corpus confirms by showing kuna's type recovery to be ordinary rather than weak. `analyzers/demangle/kuna_cppsig.rs` parses the demangled STRING, the same shape as upstream's `GnuDemanglerParser` (which consumes the c++filt text rather than an AST, because Ghidra's GNU demangler has no Java grammar to mirror): the last depth-0 parenthesis group is the parameter list, the last depth-0 token before it the qualified name, a trailing `const`/`volatile`/`&`/`&&` the cv/ref qualifier. Each declared parameter maps to a pointer of any depth (a C++ **reference** included -- a reference is a pointer at the ABI level, the same reduction `cppproto` gives `DW_TAG_reference_type`), a primitive at its LP64/ILP32 width, or -- as a POINTEE ONLY -- a named opaque structure carrying the bare innermost class name (upstream's `createPlaceHolderStructure`, and what the DWARF ground truth spells). An aggregate passed **by value**, an array, a function pointer or a pointer-to-member refuses the whole signature: the mangling carries declarations, not layouts, and a wrong width shifts every following parameter. The **return type is deliberately not applied**. Itanium encodes one only for a template function, so upstream's `resolveReturnType` returns null and `ApplyFunctionSignatureCmd` keeps whatever the analysis recovered -- which is why ghidra still prints `undefined8 leveldb::TableBuilder::NumEntries(TableBuilder *this)`. kuna expresses it as a `PrototypePieces` with no `outtype`, read by `FuncProto::seed_locked_from_pieces` as "lock the INPUT half only"; inventing `void` would DELETE every recovered return value. What makes this a **three-valued** option rather than a flag is the implicit object parameter: Itanium mangles a **static** member function exactly like a non-static one and like a namespaced free function (`leveldb::Status::OK()`, `leveldb::TableBuilder::WriteBlock(...)`, `leveldb::NewMemEnv(Env*)` share one shape), and inventing a `this` that is not there does not cost precision -- it shifts every following parameter, which is binja's bug in the other direction (it applies the declared parameters and OMITS `this`). Both tiers were measured on 1329 mangled `.dynsym` symbols with DWARF ground truth (915 really take `this`) BEFORE either was written: `proven` -- constructors (`C1`/`C2`/`C3`), destructors (`D0`/`D1`/`D2`), cv-/ref-qualified members (`_ZNK`), unqualified globals, and the MSVC forms, which state the access specifier, `static` and the convention outright -- is **precision 1.0000 at recall 0.7093**; `inferred`, which additionally decides the ambiguous nested names from class evidence mined out of the binary's own symbols (a scope owning a ctor/dtor/cv-member or a `_ZTV`/`_ZTI`/`_ZTS` symbol is a class), is precision 0.9278 at recall 1.0000; the naive "every nested name takes `this`" rule is 0.6942. For calibration, **Ghidra 12.1's own `this` decision on the same binary runs at precision 0.85** -- upstream resolves the ambiguity in `DemangledFunction.isThisCall` by comparing the mangled parameter count to the count its analysis recovered, which kuna has no access to at the analysis tier. In `inferred` BOTH directions need positive evidence: a scope with no witness at all is DECLINED, not assumed to be a namespace (a class whose constructor is implicit emits no witness, and calling its one exported member a free function drops the `this` and shifts every parameter LEFT); that refinement takes recall 0.9978 -> 1.0000 at unchanged precision by declining 100 of 1322 symbols. Complementary to `cppproto` (DIV-60), not overlapping: DWARF is ground truth and wins on an unstripped binary (the commit applies `cppsig` FIRST so the DWARF prototype overwrites it), the mangled symbol is what survives `strip`. Deliberately NOT here, and named as the follow-up: automatic `__thiscall` selection -- #265 registered the model on all 294 language:compiler pairs, but `PrototypePieces` carries no model back-pointer and `ArchContext` exposes only `defaultfp`; it is not needed for the win either, since every ABI in the corpus passes `this` as ordinary argument 0, and the one that does not (**32-bit MSVC `__thiscall`**, `this` in ECX) is REFUSED rather than mis-placed. Like `cppproto` the pass runs at `load file`, upstream of the `option` commands, so BOTH certainty tiers are computed there and stashed in `AnalysisOutput::cpp_sig`, and the mode selects which the analysis commit applies; `option cppsig off` restores name-only demangling byte for byte | **675/675 PARITY OK with no re-pin and no per-test opt-out**, stages 400/400 PARITY OK, `make rust-test` green, `make check-spec` OK (lenient + strict), `kuna catalog --check` OK -- structural, because the XML `<binaryimage>` datatest bootstrap never runs the analyzer tier and so cannot reach this pass. **Benchmark, the number this PR exists for**: decbench leveldb O0, n=525 scored functions, scored with decbench `origin/main` @325046f (PR #60, the C++ `type_match` artifact fixes; `cache_version` 5, `DECBENCH_NO_CACHE=1`) so the pointer-spelling, namespace-qualifier and `DW_TAG_reference_type` artifacts are all corrected -- `type_match` perfects **9 -> 35 at the shipped default (+26, and ZERO lost)**, 38 under `inferred` (+29, zero lost); mean 0.1277 -> 0.1784 (`proven`) / 0.1920 (`inferred`); 28 (`proven`) / 44 (`inferred`) functions improved and **0 worse in either**. Against the same fixed scorer the rivals are ghidra 44, ida 35, binja 11, angr 8 -- so kuna moves from LAST to SECOND, tied with ida at the default and 6 short of ghidra. The 26 newly perfect functions are exactly the destructors and trivial accessors decbench's own improvement miner nominated (`~Cache ~Comparator ~DB ~Env ~EnvWrapper ~FileLock ~FilterPolicy ~Handler ~Logger ~RandomAccessFile ~Rep ~SequentialFile ~Snapshot ~Table ~TableBuilder ~WritableFile ~WriteBatch` + `ApproximateSize Env FileSize Iterator NumEntries Options Rep TableBuilder WriteBatch`), and `inferred` adds `ChangeOptions NewErrorIterator NewMemEnv`. **Breadth**: whole-binary `decompile-all` off vs on with every changed function classified. **Plain C is untouched on every axis** -- 11 binaries / 1,351 functions (coreutils `fmt` `-O2 -g`, `fmt_x86_64`, `dwarf_globals`, `fauxware`, static `mcount_x86_64` at 1,073 functions, `stripped_dynamic`, `switchtab`, `noreturn_propagate`, `fmt_aarch64`, `fmt_arm`, and the Rust-mangled `rust_hello`): **0 functions changed** in either mode, which is structural (a C symbol is not C++-mangled, so the pass emits nothing). C++: leveldb `-O0` stripped (3,962 functions) 257 changed at `proven` / 503 at `inferred`, leveldb `-O2` stripped (919) 43 / 93, the `cppsig_x86_64.so` fixture (14) 3 / 6, `cppproto_x86_64` (25) 3 / 3, `cpp_mangled_x86_64` (12) 0 / 0, `cpp_noreturn_x86_64` (16) 0 / 0, `eh_lsda_x86_64` (21) 1 / 1 -- **0 new errors anywhere in either mode**. **The wrongness axis, measured rather than argued**: raw arity change is not the harm signal (an arity DECREASE is often kuna over-recovering, with the demangled list right), so it is counted as a NEW read-before-write of an argument register (`int8 *v1; // rsi`) -- the symptom of a locked parameter list shorter than the ABI uses. Over leveldb `-O0` stripped: `proven` **2 of 3,962 functions (0.05%)**, `inferred` 29. Both `proven` cases are one mechanism, the SysV **hidden return pointer**: a function returning a class by value takes an sret pointer in argument slot 0, which the mangling does not mention, so the declared `this` binds to it (`leveldb::TableBuilder::status`, `leveldb::WriteBatch::Iterate`). Ghidra gets these wrong too and worse -- its parameter-count heuristic sees 3 recovered vs 1 mangled, concludes "not a thiscall", and emits `Handler * leveldb::WriteBatch::Iterate(Handler *param_1)` with `this` dropped entirely and slot 0 mis-typed. The sweep is what found the three defects the witness could not: (1) `outtype: None` handled at `Funcdata::apply_locked_prototype` instead of `FuncProto::seed_locked_from_pieces` left the CALLER-side rebuild (`ActionDefaultParams`, which reconstructs a callee's prototype to type a call's arguments) hitting `assign_map`'s `expect("outtype null")`, killing every `main` that called such a function; (2) `cpp_demangle` renders the two-parameter forwarding constructor `_ZNSt4pairI...EC1IRS1_S4_Lb1EEEOT_OT0_` with ONE parameter where c++filt renders two, so explicit function-template specializations are now refused outright -- which also sidesteps the module's known `maxof<int>`/`maxof<double>` collision, and alone cut the measured harm from 8 functions to 2; (3) overloaded operators put unbalanced brackets into the demangled text and are the densest source of static/free false positives, so they are refused. **Speed** (the pass walks the symbol table once at load and demangles each C++ name, so LOAD is the metric; interleaved off/on, MINIMUM of 13 pairs on a box shared with concurrent agents): `kuna functions` (load + commit only, the tightest isolation of the changed code) on stripped leveldb-O0 183.1 -> 183.8 ms (**+0.35%** `proven`) / 184.0 ms (+0.48% `inferred`) and on the plain-C `regglobal_fmt_x86_64` 175.3 -> 175.2 ms (-0.08% / -0.07%); `kuna decompile` leveldb `~Cache` 894.0 -> 897.0 ms (+0.34% / +0.04%); `decompile-all` `cppproto_x86_64` 127.8 -> 128.2 ms (+0.36% / -0.08%) and the `cppsig_x86_64.so` fixture 107.9 -> 108.1 ms (+0.19% / +0.10%) at n=31 -- an n=13 run of that last, 108 ms case reported +4.01% and did not reproduce. **Worst delta +0.48%, budget 5%.** The marginal cost is one demangle per C++-mangled symbol at load plus one parked prototype per applied function, and `off` still pays the demangle (the facts are computed then dropped at the commit), so the C rows are flat because a C binary has no mangled symbol to walk. Covered by `decompiler/crates/kuna-console/tests/verify_cppsig.rs` (7 two-pass e2e assertions over a new `strip --strip-all`ped `cppsig_x86_64.so` fixture, including the DWARF-wins precedence check and a test that PINS `inferred`'s known static-member cost so it cannot drift silently) plus 19 hermetic parser/`this`-decision unit tests; no stage XML, because the datatest path cannot reach the analysis tier. Two `verify_cppproto` baseline arms now set `cppsig off` explicitly -- with the sibling recovery at its default the "gate off = the bug" baseline stopped reproducing, since a `const` member and an unqualified global are both PROVEN shapes |
| DIV-63 | full-depth DWARF type resolution (`typedepth`, new kuna option, default-ON) | kuna's DWARF type mapper truncated ordinary C declarations to `void`. `MAX_TYPE_DEPTH = 3` counted **every** hop of the `DW_AT_type` chain, the transparent `typedef`/`const`/`volatile`/`restrict` links included, and its comment cited Ghidra's `DWARFDataTypeImporter.trackRecursion` -- which is not what upstream does. Upstream counts **re-entries of one DIE offset** and refuses the third (`recursionTrackingOffsetToLoopCount`, `case 3`), so it fires only on a genuine cycle; an acyclic chain visits each offset once however long it is. The reduction conflates "the same DIE again" with "a deep but finite chain", and four DIEs is ordinary C: `const char *const *` (5), `const size_t *` (4), `char *const []` (4), `char ***` (4), a `const` member function's `this` (4). Counting the sites straight out of `.debug_info` across six `-g` C binaries (coreutils `fmt` O0/O2, grep, bzip2, zlib `minigzip`, gzip): **294 truncated** -- 238 `DW_OP_fbreg` stack locals/params, 55 CU-top-level globals, 1 prototype parameter -- and the new rule resolves **all 294**. Raising the cap was never the fix: with no cycle detection under it, any cap is simultaneously too low for real C and unable to bound a self-referential chain, so it only converts a hang into a wrong answer. `analyzers/dwarf/kuna_typedepth.rs` restores upstream's counter (`TypeWalk::Cycle`, a per-offset re-entry map bracketing every recursive step) plus `MAX_NESTING = 64` as a native-stack backstop a Java port does not need; `TypeWalk::Depth` is the old budget verbatim, so `--option typedepth off` is byte-identical. Termination no longer rests on a bound that also has to be small: an offset already twice on the path is refused and a compilation unit has finitely many offsets. **Scope note, because half of the reported symptom was already gone**: #264 (`cppproto`, DIV-60) added `strip_qualifiers` on its own `cpp` path, and because `cppproto` is default-ON and rebuilds *every* subprogram's prototype -- C included -- binding it by address over the name-keyed one, plain-C **prototypes** and the C++ `const`-method `this` (`Account::available` renders `Account *this` on main today) inherited that fix. What remained was the `cpp = false` callers: `collect_fbreg_locals` (stack locals) and the global-sizing arm, where a truncated element type sizes the global at **one byte** so the covering-`SymbolEntry` query misses and it renders `dat_<addr>` or address-taken -- plus purely structural depth, where three *pointer* hops exhaust the budget even with the qualifiers collapsed (gzip's `add_envopt(int *argcp, char ***argvp, ...)` is `void ***` on main). Two consequences ride along. The qualifier collapse now runs for the C callers too, which is what carries an anonymous aggregate's typedef name onto it (a local `mbstate_t`/`position`/`direntry_t`, not the shared `anon_struct` every unnamed struct fuses into); and because a borrowed name can already be taken, `intern_aggregate` falls back to the anonymous name when the alias does not yield an aggregate. That guard was found by the sweep, not by argument: kuna registers a **core type named `code`** and zlib's `inftrees.h` really does `typedef struct {...} code;`, so `get_type_struct("code")` hit the core type, the factory refused the redefinition, and the pointer arm's `.or_else(get_type_void)` degraded `code *next` to `void *next` -- the exact degradation this change exists to remove (and already live on main for the *parameter*, via #264's path). Only an aggregate with no name of its own falls back, so no new name is ever asserted for a genuinely named type | **675/675 PARITY OK with no re-pin and no per-test opt-out**, stages 400/400 PARITY OK, `make rust-test` green, `make check-spec` OK (lenient + strict), `kuna catalog --check` OK -- structural, because the XML `<binaryimage>` datatest bootstrap never runs the analyzer tier. **Benchmark**: the decbench scored corpus is `strip`ped, so it carries no `.debug_*` and this change is a structural **zero** there -- verified, not assumed (`decompile-all` off vs on over the stripped zlib `minigzip`: 0 lines differ). That also re-frames the C `type_match` standing (kuna 5.17% vs angr 7.29%, ida 5.55%): that gap is measured with no debug info present, so it is an *inference* gap, not a DWARF-consumption one. Where debug info exists the delta is large: decbench `type_match` over the same projects' **unstripped** `compiled/` copies (zlib + bzip2 O0, 9 binaries, n=872, scorer `main` @325046f, `DECBENCH_NO_CACHE=1`) moves perfects **458 -> 626 (+168)**, mean 0.8438 -> 0.9083, **193 functions improved and 0 worse** (zlib 401 -> 561 of 764, bzip2 57 -> 65 of 108). **Breadth**: whole-binary `decompile-all` off vs on with every changed line classified -- 204 changed lines over four `-g -O0` C binaries (zlib `minigzip` 91, grep 83, gzip 28, coreutils `fmt` 2), of which 102 are `void *` -> a concrete type, the rest `anon_struct` -> the real typedef name and globals whose corrected extent lets an array decay (`&(&crc_table)[i*4]` -> `crc_table[i*4]`, `dat_337e8` -> `unibyte_mask`, `*(int8 *)((int8)v13 + (uint8)*p * 8)` -> `v13[*p]`); **0 changed lines** on coreutils `fmt` O2, bzip2 O0, the stripped `minigzip`, and the three C++ fixtures (`cppproto_x86_64`, `cppsig_x86_64.so`, `eh_lsda_x86_64`, `cpp_noreturn_x86_64`). **The wrongness axis** -- a wrong concrete type asserted where `void *` was honest -- is **0 after the `intern_aggregate` guard** (2 before it, both the `code` collision). The one class that is noisier rather than cleaner, and is reported as such: a local now typed as a named **opaque** struct (`z_stream_s *strm`) renders its field accesses through a cast, because kuna maps an aggregate to a named opaque with no fields -- the existing `cppproto` field-population seam, not a new one. **Speed** (the mapper runs once per binary inside `load file`, so LOAD is the metric; interleaved off/on, MINIMUM of 15 pairs on a box shared with concurrent agents): `kuna functions` grep-O0 693.5 -> 695.9 ms (**+0.35%**) and zlib `minigzip`-O0 450.8 -> 451.5 ms (+0.16%); `kuna decompile` `fmt`-O2 `main` 304.3 -> 305.0 ms (+0.24%); `decompile-all` `fmt`-O0 1835.6 -> 1836.0 ms (+0.02%) and the fixture 126.8 -> 126.6 ms (-0.14%). **Worst delta +0.35%, budget 5%** -- the guard replaces an integer compare with a `BTreeMap` entry per DIE hop on a walk a handful of hops deep, and nothing in the decompile loop is touched. Covered by `decompiler/crates/kuna-console/tests/verify_typedepth.rs` (6 two-pass e2e assertions over a new `-g -O0` C fixture `typedepth_x86_64`: the `const char *const *` local, three pointer hops, the anonymous-typedef alias, the `code` collision, the global's extent, and termination on self-referential + mutually recursive structs) plus 6 hermetic unit tests including a **forged** `.debug_info` whose type chain closes on itself (self-pointer, mutually pointing `typedef`/`const`, self-referential array) asserted to terminate under BOTH arms; no stage XML, because the datatest path cannot reach the analysis tier |
| DIV-64 | `aggressive` preset carries `itaniumrtti` (new kuna option, default-OFF; the preset makes it the default rendering) | The option itself diverges from nothing -- it is default-OFF and `--mode reliable` (the shipped defaults) is byte-identical -- but leaving it out of `AGGRESSIVE_OVERRIDES` would have made it **unreachable in practice**, which is the state it was first written in. `auto` selects `aggressive` below 500 KiB (DIV-40), so the preset is the default path for `decompile-all`, the `kuna` CLI, the web front-end and the benchmark; measured on the stripped fixture, `decompile-all` with the option outside the preset recovered **0** vtable-method names under `--mode aggressive` and **29** inside it. The preset's own documented contract is already "every off-by-default option flipped ON except `v850indirectbranch` and `dwarf_lines`", and `itaniumrtti` is the same shape as the format-gated members that clause names (`rtti`/`pdb` = PE, `objc`/`macho-arm64e` = Mach-O, `sparcstructret` = SPARC): inert off ELF, and additionally inert on any ELF whose `.rela.dyn` carries no `__cxxabiv1` typeinfo relocation -- a strictly narrower firing condition than `rtti`'s PE gate. This is recorded as a divergence for the same reason DIV-43 was (which REMOVED `dwarf_lines` from the same preset): mode presets are not a datatest surface, so a membership change alters default rendering where **no parity oracle can see it**. It also exposed that the preset's headline invariant was only ever *sampled* by a test, never enforced; `modes.rs::aggressive_carries_every_default_off_option` now enforces it and immediately found four further default-off options in the same trap (`cortexmvectors`, `ptrentry`, `tailcallentry`, `paramcopyhoist`), which are recorded as an explicit UNEVALUATED list rather than silently flipped -- each needs its own sweep and PR | `make test` 675/675 PARITY OK (mode presets are not a datatest surface), `make test-stages` PARITY OK, `make rust-test` green, `make check-spec` OK (lenient + strict), `kuna catalog --check` OK. **Reachability**: `decompile-all` on the stripped fixture recovered 0 vtable-method names at the default before and 29 after; `--mode reliable` stays at 0, so the shipped defaults are untouched. **Sweep** (pre-PR default path `--mode aggressive --option itaniumrtti off` vs post-PR `--mode aggressive`, restricted to the <500 KiB binaries the preset actually applies to): plain C 14 binaries / 357 functions **0 changed**; C++ 8 binaries / 364 functions, changed only the fixture (29 renamed) and the two probes (23 and 14), plus ONE hunk in `cppproto_x86_64` which is strictly better (`*(unsigned long *)this = 0x402028` -> `*(code **)this = Shape_vptr`); 0 new errors and the function ENTRY set byte-identical everywhere. Above the threshold at the option level: leveldb -O0 3,962 functions 0 changed, -O2 919 0 changed, `mcount_x86_64` 1,073 0 changed. **Speed** (interleaved, min of 15; 31 for the fire row): the cost where it does NOT fire -- what every user pays -- is +0.24% on `mcount_x86_64` (896 KiB static C, 1,969 symbols), +0.26% on stripped leveldb (1,546 relocs), +0.28% on `regglobal_fmt_x86_64`, and -0.38%..+0.25% end-to-end under `--mode aggressive`; where it DOES fire, +0.99% (vs +2.13% measured pre-preset in isolation -- same work, larger denominator). Worst no-fire +0.28%, worst overall +0.99%, budget 5% |
| DIV-65 | `libcsigs` (new kuna option, default-ON) | kuna's built-in libc prototype table carried **27 entries**. Its own header calls the shortfall a documented LOSS and says why: Ghidra applies parsed C headers shipped as binary `.gdt` data-type archives, that format is not vendored into the kuna tree, and the table is "a faithful, minimal stand-in". The constraint is that no header archive exists here -- not licensing, since these are standard C library declarations, and `docs/missing-ghidra-analyses.md` classifies the whole item as a dependency/data substitution of the same kind as BFD -> `object`. So the table was small because nobody had grown it. The cost is that **every libc callee outside those 27 leaves its callers' arguments an inferred `unsigned long`**, which is kuna's weakest axis on plain C: of the 1,587 cases where IDA scores a perfect C `type_match` and kuna does not, 568 (35.8%) sit in a function that calls a libc name the table did not know, against 395 (24.9%) that call one it did. `analyzers/protos/kuna_libcsigs.rs` adds the measured remainder as a SECOND table (the 27-entry base table is left byte-for-byte alone, so its behaviour cannot move). **Which names was measured, not guessed**: `objdump` over the 547 frozen-decbench C binaries that carry a scored case gives a PLT call-site histogram (1,930 distinct imports), and each of those 1,587 cases mapped into its containing function block gives a per-callee ranking of what would actually move the metric; the rule is **>= 100 corpus call sites or >= 3 such cases**, plus a named-witness set, and 206 entries result -- headed by `exit` (119 cases / 5,456 sites), `gettext` (91 / 8,311), `__errno_location` (87 / 24,084), `strcasecmp` (63), `close` (55), `__stack_chk_fail` (50 / 29,055), `error` (42), `strncasecmp` (38 / 86 binaries), `open`, `strdup`, `strerror`, `dcgettext` (14,664 sites), and the FORTIFY `__fprintf_chk` (12,824) / `__printf_chk` (3,806). **Which signature is machine-derived, never written from memory**, because a wrong prototype is worse than a missing one -- it asserts a false type where `unsigned long` was merely uninformative: 193 entries are reduced by a fixed rule from the 1,520 `NC`-line prototypes `gcc -aux-info` emits over a TU including the platform headers with `_GNU_SOURCE` + `_FORTIFY_SOURCE=2`; `__isoc99_{scanf,sscanf,fscanf}` from the `__REDIRECT` at `/usr/include/stdio.h:434-441`, which is the header *proving* them ABI-identical to the names they replace; the FORTIFY `mem`/`str` `_chk` family (no header prototype exists -- they are GCC builtins) from GCC's own builtin table via nine compiling `_Static_assert(__builtin_types_compatible_p(...))` checks; `__stack_chk_fail` from glibc `debug/stack_chk_fail.c`. Sixteen were spot-checked by hand against their man pages as well, all matching. A declaration whose every slot is not **width-stable** across ILP32/LP64 is **REJECTED rather than approximated** -- `Ty` deliberately has no spelling for `off_t`, `time_t`, `long long`, a function pointer or a `char` parameter -- so `lseek` (746 sites), `signal` (574), `time` (399), `qsort` (323), `strtoll` (247), `fseeko`, `mmap`, `poll`, `iconv`, `connect` and `pthread_once` are deliberately absent; `strtoll` is the one that mattered, because an earlier reducer classified `long long int` as a 4-byte integer and would have shipped `int strtoll(...)`. The same principle drives the **one novel rule**: an entry here applies only to a name the image **IMPORTS** and does not itself define. `readelf` over all 677 binaries of the frozen O0/O2/O2-noinline trees found exactly one real collision outside NuttX (which implements the genuine POSIX API anyway) -- zlib's `minigzip` DEFINES `void error(const char *msg)` where glibc's is `void error(int,int,const char*,...)`, and typing that call `error(0,0,...)` would be strictly worse than what it replaces. A PLT/IAT import named `error` is definitively the platform's; a defined one is the program's own function that happens to share a spelling. This is also Ghidra's model (archives apply to EXTERNAL functions), and it makes a statically linked image inert. Finally the FORTIFY entry points are modelled as the distinct functions they are, not as aliases: `__printf_chk` takes a leading `int flag` so the format sits at slot 1, `__fprintf_chk` a `FILE *` then a flag -- treating either as its plain namesake would shift every argument of the most frequent call in the C corpus. Gated at the analysis COMMIT boundary (`engine.rs::analysis_pass_enabled`), the same seam `cortexmvectors`/`fdeinterior`/`cppsig` use, since the producing pass runs at `load file` upstream of any `option` command; `option libcsigs off` renders exactly what the 27-entry base table alone renders | **675/675 PARITY OK with no re-pin and no per-test opt-out**, stages 400/400 PARITY OK, `make rust-test` green, `make check-spec` OK (lenient + strict), `kuna catalog --check` OK -- structural, because the XML `<binaryimage>` datatest bootstrap never runs the analyzer tier and so cannot reach this pass. **Benchmark, the number this PR exists for**: decbench `origin/main` @325046f, `DECBENCH_NO_CACHE=1`, `type_match` over the frozen C corpus, option off vs on across all three optimisation levels -- **grep** n=247 perfects 33 -> 37, mean 0.3105 -> 0.3243 (9 improved / 2 worse); **diffutils** n=420 perfects 24 -> 35, mean 0.2522 -> 0.2987 (40 / 0); **gzip** n=368 perfects 57 -> 62, mean 0.3740 -> 0.4007 (35 / 1); **bzip2** n=267 perfects 8 -> 8, mean 0.2617 -> 0.2712 (8 / 0); **findutils** n=790 perfects 21 -> 23, mean 0.1079 -> 0.1237 (45 / 0). Pooled over those five projects, **n=2,092: perfects 143 -> 165 (+22, +15.4%), mean 0.2272 -> 0.2501 (+10.1%), 137 functions improved against 3 worse**. **Breadth**: whole-binary `decompile-all` off vs on over 11 stripped binaries / 5,589 functions (coreutils dircolors-O0 + sort-O2 + ls-O2ni, grep-O0 + grep-O2ni, gzip-O2, zlib minigzip, openssh scp, statically linked nuttx, and C++ leveldbutil + libleveldb.so), **function count identical in every one**, every changed function classified: 1,141 changed -- 471 (41.3%) the import's own thunk gaining its signature, 281 (24.6%) a cast removed because the callee's return is now typed, 120 (10.5%) an `unsigned long` replaced by a concrete type, 116 variable renumbering, 74 the function's own signature retyped, 74 a call's arity restored to the declared signature, 5 structuring churn. **The wrongness axis, measured rather than argued**, on two independent counts: (1) **argument-count drift** -- 75 call sites had their arity RESTORED to the declared signature (`abort(a0,a2,0)` -> `abort()`, `openat` 0 -> 4 args, `reallocarray` 0 -> 3, `getopt_long` 4 -> 5), and the only 4 flagged as moving away sit in two functions (`gzip-O2 sub_f900`, `coreutils-ls-O2ni sub_a550`) that **already** render `free(v43,v24[-8])`, `malloc(v10,v24[-8])`, `memcpy(...,v24[-8])` and `strlen(v10,v4)` -- BASE-table names with 1 parameter -- with this option OFF, so the phantom trailing stack argument is a pre-existing frame-recovery defect in those functions, not one this table introduces; (2) **parameter specificity** -- 296 slots gained it, **2 lost** it, and both losses are the same function (grep-O2ni `Pexecute`, `char *` -> `void *`) where the loss is the *truthful* prototype, since `memchr`/`rawmemchr` really do declare `const void *`. The statically linked `nuttx` image is **0 of 924 functions changed**, which is the imports-only guard doing exactly its job; the no-return annotations are identical on every C binary (coreutils-sort `abort` 24->24 / `exit` 5->5 / `__assert_fail` 3->3, grep 28/7/8, openssh-scp `error` 10->10 / `exit` 39->39). **Speed** (the marginal cost is building + committing up to 206 more `PrototypePieces` at load, so LOAD is the metric): `kuna functions` -- load + the whole analysis pass list + commit, no function body decompiled -- interleaved off/on, min AND median of 21 pairs: coreutils-dircolors 293.6 -> 289.4 ms min (-1.45%) / 321.6 -> 320.7 median (-0.28%), openssh-scp 836.5 -> 835.4 (-0.12%) / 851.9 -> 846.0 (-0.69%), nuttx 1423.4 -> 1432.2 (**+0.62%**) / 1465.9 -> 1452.2 (-0.93%), grep 849.9 -> 695.4 (-18.18%) / 1012.6 -> 1014.9 (+0.23%) -- the grep minimum is noise in both directions (an earlier 15-pair run of the same case reported +13.99%), the medians are the honest read. `scripts.pipeline.timeit` on grep `main`, 15 samples: 1547.61 -> 1547.82 ms (**+0.01%**). Whole-binary `decompile-all`, interleaved min/median of 13: dircolors 1832.4 -> 1827.1 (-0.29%) / 1852.6 -> 1848.6 (-0.22%), zlib minigzip 4973.3 -> 4981.3 (+0.16%) / 5034.3 -> 5047.5 (+0.26%). **Worst stable delta +0.62%, budget 5%.** Covered by `decompiler/crates/kuna-console/tests/verify_libcsigs.rs` (5 two-pass e2e assertions over the vendored `fauxware` fixture: the caller's `unsigned long a0` -> `char *a0` from `open`, the fd `unsigned int` -> `int4` from `open`'s return, the thunk's full `int open(char *,int,...)` including the variadic tail, `void exit(int4)`, and a function the table does not touch being byte-identical) plus 5 hermetic table-invariant unit tests including the FORTIFY argument slots and the imports-only rule; no stage XML, because the datatest path cannot reach the analysis tier. Deliberately NOT here and recorded as the follow-up: `formatstring/apply.rs`'s `VARIADIC_SUBSTRINGS` matches by `contains`, so `__printf_chk` is already treated as a format-string callee with the format at slot 0 when it is at slot 1 -- a separate, pre-existing behaviour change that belongs in its own PR. Bundle: `docs/features/libcsigs/` **Preset decision, made on evidence rather than left implicit**: `libcsigs` is deliberately NOT in `AGGRESSIVE_OVERRIDES`. #267's `aggressive_carries_every_default_off_option` invariant exists because `auto` resolves to `aggressive` under 500 KiB, so a default-OFF option absent from the preset ships inert on the default path -- which is how `itaniumrtti` recovered 0 of 29 until it was caught. This option ships default-ON, so it is out of that invariant's scope by construction, and the liveness was measured rather than assumed: on `dircolors` (55,680 bytes, so `auto` -> `aggressive`), default vs `--option libcsigs off` differs in 57 of 184 functions under `auto`, 57/184 under `aggressive`, 58/183 under `reliable` and 58/184 under `fast`, with the cast-removal witness present in all four -- no preset can silence it, since none turns anything off except `fast`'s three discovery gates. Adding it would be worse than redundant: the preset **owns** an option it lists (its own doc records that `--option returndup off` earlier on the command line is re-enabled by the preset), and every benchmark and sweep number above was produced by `--option libcsigs off` on sub-500 KiB binaries, i.e. straight through `auto` -> `aggressive`; preset membership would silently re-enable it there and destroy the ablation hook that is half the reason the flag exists. So no second DIV row for preset membership is needed -- this row covers the default-ON decision alone. **Counter derivation**: the counts here were COUNTED out of a freshly captured `phase_catalog.json`, not computed from the previous value -- the rebase onto #267 auto-merged `catalog_bytecompat.rs` to 94 with NO conflict, because #267 and this PR had independently made the identical 93 -> 94 edit, so the committed number was short by one until the capture corrected it to 95 (tiers (20, 41, 34), 94 JSON rows, and `docs/options.md` independently reporting 95 option headings). |

### Convergences (upstream behavior restored — not divergences)

A port defect whose fix moves kuna *back onto* upstream's default earns no DIV row, but a
corpus-wide output shift still needs a record for anyone bisecting. Those go here.

| Area | What changed | Evidence/effect |
|---|---|---|
| `callsitestackargs` (P4 active-input trial scoring) | kuna dropped every stack-passed call argument at call sites whose callee prototype is unlocked — x86-64 everything past the 6th register argument, i386 every argument. `check_input_trial_use` handed the trial's **callee**-relative address to `FuncProto::get_local_range().in_range()` where upstream `fspec.cc:5618` hands the argument Varnode's **caller**-relative address; on a downward-growing stack the callee-relative offsets are always positive and the caller's `localrange` negative, so every stack trial was scored no-use — and a definitely-unused trial has its CALL input replaced with constant 0, handing the argument-producing computation to dead-code elimination. Not an unported feature: `guard_calls` registers the trials and appends the argument Varnodes correctly; they were mis-scored one pass later. Introduced in `5cd4d518 rport/w10-callsite-args`. The new option is default-ON (= upstream); `off` reproduces the pre-fix output for bisection | 0/675 — `docs/baseline.json` unmoved; stages 316/316, and the one assertion whose text changed (`gh6882-sparcstructret` #2, `return v1 + a0` → `return v1[0] + a0`) changed because the OLD text was wrong: `f` is a struct-return function whose hidden return-buffer pointer kuna had also been dropping, so `f()` became `f(v2)` and the buffer typed correctly as `int4 v1 [2]`. 30-binary stratified decbench sweep (x86-64 / ARM Cortex-M / PE-i386, all three optimisation levels): calls rendered with ≥7 arguments 7 → 250, i386 empty-argument-list fraction 42.5% → 2.5%, short calls −36%; 72 runs, zero timeouts, stderr byte-identical off vs on. Speed unchanged (`decompile-all` ssh-add 10.80 → 10.79 s median, mydoom 2.03 → 1.98 s). Decbench cases closed: `O0-mydoom-mydoom-msg_b64enc` (a deleted `for(;;)` loop restored, 3 → 22 blocks) and `O2-noinline-openssh-portable-ssh-add-parse_dest_constraint` (2 → 6 `if`s, full 16-argument call) |
| `funcptrencoding` (P3 rule pool, the ISA-mode / alignment mask on an indirect-call target) | On processors that steal the low bits of a function pointer the *instruction* clears them before branching, so SLEIGH lifts the clear as a real `INT_AND` feeding the `CALLIND`: Thumb `blx` through `BXWritePC` (`ARM.sinc:221`, `local tmp = addr & 0xfffffffe`), MIPS `jalr` through `JXWritePC` (`mips.sinc:925`, `tmp = -2 & addr`). Upstream erases it in `RuleFuncPtrEncoding` (`ruleaction.cc:9928`) whenever the compiler spec declares `<funcptr align="N"/>`. kuna's port of that rule is a line-for-line transcription and has been registered in the pool (`universalaction.rs:515`) since W8, but its **only input** was a hard-coded stub — `fn funcptr_align(_data: &Funcdata) -> int4 { 0 }` in `p3_dataflow/ruleaction_8.rs`, whose first two lines are `let align = funcptr_align(data); if align == 0 { return 0; }` — so **the rule could never fire on any architecture**. The stub's premise ("the W4 `Architecture` skeleton does not carry `funcptr_align`") was stale: `architecture.rs:2105 decode_funcptr_align()` has parsed the cspec element into the field since W4, `:3902` calls it during `parse_compiler_config`, `:1869` copies it into the ArchSeam, and two other consumers (`jumptable.rs:3661`, `ruleaction_6.rs:609`'s `preserve_thumb_funcptr`, GH-8471/DIV-2) already read it live. The accessor now reads `data.get_arch().funcptr_align` like they do; nothing else changes. Cost of the stub, on every ARM/Thumb and MIPS16-capable target: the emitted C asserted a bit-clear the program never performs, *and* the mask stood between the `CALLIND` and its pointer operand so the pointer-to-code type never back-propagated onto the LOAD — `(*(code *)(*(uint4 *)(a0 + 0x44) & 0xfffffffe))(a0)` where ghidra prints `(**(code **)(param_1 + 0x44))(param_1)` and the source (`libdw1000.i`, `dwHandleInterrupt`) writes `(*dev->handleError)(dev)` with no mask at all. The width is **not** a kuna policy — the vendored cspecs declare `align="2"` for the four ARM, nine MIPS and four Loongarch cspecs plus 8051, and `align="4"` for the five AARCH64 cspecs | **675/675 PARITY OK and stages PARITY OK**, no re-pin and no per-test opt-out (the stages delta is purely the 2 new keys); the corpus's two `ARM:LE:32:v8` and six `MIPS:*:32:default` datatests contain no masked indirect call. Whole-binary `decompile-all`, occurrences of `& 0xfffffffe` / `(code *)` / `(code **)`: crazyflie `cf2.elf` 264 → 15 / 255 → 46 / **0 → 205**, nuttx 459 → 40 / 435 → 42 / **0 → 397**, libopencm3 `msc.elf` 56 → 6 / 50 → 10 / **0 → 41**, chibios `ch.elf` 38 → 13 / 40 → 30 / **0 → 16** — every ARM project in the corpus had **zero** `(code **)` before, because a mask sat on every indirect-call target; the triage measured the class at 4,027 masked indirect calls in 2,496 function-instances across 97 binary-instances in 9 ARM projects. x86-64/i386 are **byte-identical** (their cspecs declare no `<funcptr>`, so `funcptr_align == 0` and the rule stays inert): whole-binary diffs with the watchdog disabled change 0 lines on zlib `minigzip` (8,923), gzip (9,465), bash (120,695), coreutils `head` (4,403) and the i386 PE `x0r-usb.exe` (2,205). **AARCH64 `align=4` was verified rather than assumed**: upstream's `applyOp` is a single code path (`slide = ~0 << align`) with no `align == 1` branch and an exact comparison, `blr` itself emits no mask (`AARCH64base.sinc:887`), and stock **Ghidra 12.1 headless** on a purpose-built probe (`clang --target=aarch64-linux-gnu`, one plain `blr x8`, one real `and x8, x8, #0xfffffffffffffffc; blr x8`) prints `(**(code **)(param_1 + 2))(*param_1)` — byte-for-byte what kuna now prints, where kuna previously printed `(*(code *)(*(uint8 *)&a0[2] & 0xfffffffffffffffc))(*a0)`. Interleaved `decompile-all` medians, two builds of the same tree on a box running three concurrent agents: libopencm3 `msc.elf` 592 → 597 ms (+0.84%, 5 pairs), coreutils `head` 5,368 → 5,443 ms (+1.40%, 5 pairs — that binary's output is byte-identical, so it is noise, not cost), crazyflie `cf2.elf` 36,838 → 32,052 ms (**-12.99%**, 3 pairs: one op leaves the graph at every indirect call). `tests/stages/ghdec-funcptralign.xml` (0 of 2 assertions pass pre-fix, 2/2 post-fix); rule-level unit tests pin all three alignments (`funcptrencoding_noops_without_cspec_funcptr`, `..._strips_thumb_mode_bit`, `..._strips_aarch64_word_alignment`, the last also asserting that `& ~1` is declined at align=2) |
| `subright` (P3 cleanup pool, non-least-significant truncation) | `RuleSubRight::apply_op` (`ruleaction.cc:7271`) was a **port stub**: only the leading `doesSpecialPrinting`/`isPieceStructured` half was transcribed and the body then returned 0 under a `// Remainder transcribed for the next wave ... W6` comment, so upstream's `sub(V,c) => sub(V>>c*8,0)` rewrite never ran. Every SUBPIECE with a **nonzero byte offset** therefore reached the printer, which renders one as the raw p-code operator — `*v2 = SUB81(a1,7);` in openssh `ssh` `sshbuf_put_u64` @ `0x4cd60`, where Ghidra and the source both have a shift (`*local_18 = (char)((ulong)param_2 >> 0x38);` for `((u_char *)(p))[0] = (__v >> 56) & 0xff;`). `SUB81`/`SUB84`/`SUB41` are **undeclared identifiers** — kuna emits no header defining them — so those function bodies were not compilable C. kuna's own offset-0 SUBPIECE already rendered `(char)a1`, which is what proves the printer was never at fault. The rule was already registered and scheduled in upstream's position (`rrow!("subright", "cleanup", ...)`, `universalaction.rs:552`); only the ~45-line tail was missing, and it is now transcribed with every guard upstream applies — the `c == 0` bail, the address-tied `overlap == c` bail that leaves `ActionCopyMarker`'s storage markers alone, and the lone-`INT_RIGHT`/`INT_SRIGHT`-descendant lumping (declining an `INT_RIGHT` whose combined amount would evaluate to zero, clamping `INT_SRIGHT` to the sign bit). No option: restoring upstream's own rendering of ordinary arithmetic is not a judgment call, and `subright` is already a rule-group name, so `option togglerule subright off` reproduces the pre-fix output for bisection | **0/675 PARITY OK** with no re-pin (restoring upstream behavior moves no upstream assertion) and stages 364/364 with no per-test opt-out — the stages delta is purely the 8 new keys. Across 7 whole-binary `decompile-all --mode reliable` runs (x86-64 -O0/-O2 and ARM Cortex-M: openssh `ssh`, iproute2 `ip`, e2fsprogs `e2fsck`, `gzip`, `bzip2`, betaflight STM32F405, cleanflight DALRCF405 — 14,720 functions common to both panes) nonzero-offset raw `SUBnn` operators go **1,912 → 95** (95.0%), the functions carrying one go 733 → 20, and the blast radius is 728 functions (4.95%) with emitted lines 443,737 → 443,785 (+0.011%). Every one of the 95 residuals is a case upstream's own guards decline: 42 address-tied global-array stores (round 1's separately-filed `rodata-phantom-store`), 47 address-tied local partial-symbol writes (the P6 thread that renders `sym._2_2_` upstream) and 6 piece-structured field extractions on the special-print branch. Measured cost: exactly two functions in 14,720 gain a dead partial-register shift statement — `(undefined7)((uint8)a2 >> 8)` in bzip2 `sub_da00` and one `(undefined3)((uint4)v11 >> 8)` on ARM — the `SUB87`/`SUB43` form upstream lowers the same way (corpus-wide count of that shape 0 → 2). GED is unchanged by construction (`O2-noinline-iproute2-ip-netns_add` 8.0 → 8.0): the rewrite changes expressions inside blocks and moves no block. Interleaved medians, `decompile-all --mode reliable`: openssh `ssh` @0x4cd60 2,307 → 2,165 ms (-6.16%, 9 pairs), gzip -O0 3,387 → 3,325 ms (-1.84%, 7), bzip2 -O2 12,016 → 11,512 ms (-4.20%, 7), e2fsck -O2 21,378 → 21,430 ms (+0.24%, 5) — worst case +0.24%, inside the +5% budget. `tests/stages/ghdec-subright.xml` (1 of 8 assertions passes pre-fix, 8/8 post-fix) |
| `cspecprotos` (P4 prototype-model registry, the cspec's named models) | `Architecture::build_default_proto` read exactly one cspec element, `<default_proto><prototype>`, where the C++ `parseCompilerConfig` (`architecture.cc:1253/1280-1284/1310`) also dispatches `<prototype>`, `<resolveprototype>` and `<modelalias>` (`ProtoModel::decode`, the marshaling-decoder entry point, is a hard `Err` stub). The per-program model registry therefore held **exactly one** model, so `getModel("__thiscall")` could never resolve, `option defaultprototype __thiscall` failed with "Unknown prototype model" on every target, and kuna's fully-ported `this`-pointer machinery (`ProtoModel::hasThisPointer`, `FuncProto::hasThisPointer`/`updateThisPointer`/`getThisPointerStorage`, `ParameterPieces::isThisPointer` — driven from `ActionInputPrototype`, consumed by P5 type inference) was permanently dead: nothing could set `hasThis`. `ProtoModelMerged` was likewise ported and unit-tested with no live producer, because its only producer is `<resolveprototype>`. The named models are now decoded through the *same* body as the default one (so they carry identical `<pentry>` storage and `<unaffected>`/`<killedbycall>`/`<returnaddress>`/`<internal_storage>` fidelity) plus the `hasthis`/`constructor` attributes and upstream's `if (name == "__thiscall") hasThis = true` override (`fspec.cc:2595`); a `<resolveprototype>` folds its constituents into a `ProtoModelMerged` and finalizes the merged input list, and a `<modelalias>` registers an `isCompatible` named copy. Upstream's post-parse invariant "we must have a `__thiscall` calling convention" (architecture.cc:1342) is honored too, cloning one off the default on the many languages whose spec declares none, so `getModel("__thiscall")` resolves everywhere. Deviation from upstream, deliberate: a named model that fails to decode is **skipped** rather than aborting the spec, because kuna's vendored cspec corpus spans every processor it ships and one undecodable named model must not cost the architecture its default one. **Registration only** — `<eval_current_prototype>`/`<eval_called_prototype>` are still ignored (honoring them changes which model every function on x86win/x86gcc is evaluated with, which is a default change and not part of registration), and nothing assigns `__thiscall` automatically | **No output shift at all**, which is the point: 675/675 PARITY OK with no re-pin, stages 400/400 (delta is purely the 6 new keys), and 12 whole-binary `decompile-all` panes (73,173 lines) byte-identical before vs after across four compiler specs, both arms captured on the same rebased tree (x86-64-gcc ELF `ls`/`grep`/`gzip`/`du`/`xxd`/a g++ vtable demo/the DIV-60 `cppproto_x86_64` fixture; x86win PE32 `msvc_rtti_x86.exe`/`pe_imports.exe`/`pe_dwarf.exe`/`pdb_prog.exe`; x86-64-win `msvc_rtti_x64.exe`). Registry census over every `language:compiler` pair kuna ships: 294/294 bootstrap, pairs holding more than one model 0 -> 294, pairs where `getModel("__thiscall")` resolves 0 -> 294, models registered 294 -> 790 (110 cspecs declare 186 `<prototype>` elements; only the 110 inside `<default_proto>` were ever decoded, so 76 named models + 6 `<resolveprototype>` + 3 `<modelalias>` were dead spec text). Load time, the metric that matters since the extra elements are parsed once per program load: interleaved `kuna functions --json` minima at n=31 move +0.42% / +0.55% / +0.15% / -0.05% (`msvc_rtti_x86.exe`, `msvc_rtti_x64.exe`, `pe_imports.exe`, gzip) after the three separate cspec parses were folded into one; `decompile-all` worst case +0.89%. What changed is what is now *reachable*: on `x86:LE:32:default:windows` the registry holds `__stdcall` + `__cdecl` + `__fastcall` + `__thiscall` + the merged `__fastcall/__thiscall/__stdcall`, and `option defaultprototype __thiscall` turns the MSVC member function `int4 Box::vftable_0(void) { int4 v1; // ecx ... }` (a read-before-write of ECX — not valid C) into `int4 Box::vftable_0(int4 a0) { return *(int4 *)(a0 + 4) * *(int4 *)(a0 + 4); }` on the real `msvc_rtti_x86.exe` fixture. `tests/stages/ghdec-cspecprotos.xml` (two-pass, 6 assertions) + `decompiler/crates/kuna-decomp/tests/verify_cspecprotos.rs` (5 registry assertions over four real vendored cspecs) |

## Upstream provenance & sync

- Upstream: https://github.com/NationalSecurityAgency/ghidra.git — pinned port anchor:
  GHIDRA_REV=cef869af04c4740a71ad31a55704045b1b0d1644 (2026-06-01). Updated automatically
  by `tools/sync_upstream.py` on a successful sync; keep the `GHIDRA_REV=<hex>` token on
  one line — the script greps for it.
- Still vendored (the only paths the sync script maps):
  `Ghidra/Features/Decompiler/src/decompile/datatests/` → `tests/datatests/` and
  `Ghidra/Processors/<P>/data/languages/` → `specs/Ghidra/Processors/<P>/data/languages/`.
  The C++ source (`decompiler/cpp/`, `unittests/`) was removed after the port was proven.
- Sync: `python3 tools/sync_upstream.py --ghidra <checkout> --to origin/master [--dry-run]`,
  then `make test` (675/675 PARITY OK), review, commit. Plain `git apply` first, `--3way`
  fallback; hard-fails on renames/binary diffs (handle manually). The DIV-2 re-pinned
  datatest files (15 files / 22 assertions) no longer clean-apply — expect `--3way` there.
  If a sync intentionally changes the corpus, re-pin with
  `kuna test --datatests --save-baseline docs/baseline.json` and say so in the commit;
  never re-pin to absorb an unexplained regression.
- Local-modification policy: byte-identical to upstream until 2026-06-06 (GH-558); since
  then kuna intentionally diverges — it is its own decompiler, not a tracking mirror. New
  logic goes in `kuna_*` modules; edits to ported-core files stay minimal and carry a
  `// (kuna)` marker. The per-file divergence table for the retired C++ tree (71 rows) is
  in git history of `docs/UPSTREAM.md`.

## Retired campaign notes (what the deleted docs recorded)

- **Stage-model study** (`docs/history/stage-model.md` + `evidence/`): the P0+S1–S9 model
  was synthesized from 10 structured readers over Ghidra/angr/Reko (~168 pipeline steps,
  ~80 feedback loops, ~98 decision points), 3 competing taxonomies, and 2 adversarial
  critiques; it killed 4 "phantom stages" of the older 19-stage guess. Empirical critique
  over the first 10 issue fixes: symptom→sub-stage navigation 7/10 clean; ablations sized
  each option's blast radius and justified the DIV-2/DIV-3 default flips.
- **Issue campaign** (`fixed-public-bugs.md`, `issue-coverage.md`): 58 public Ghidra
  issues reproduced, 37 fixed (17 bug-fixes / 20 decision points); 5 options deliberately
  left opt-in as destructive-by-default (`returnpair`, `v850indirectbranch`,
  `switchmodbound`, `stackalias`, `sparcstructret`).
- **Analysis-tier port** (`analysis-port-*.md`): plan generated 2026-06-22 by a 13-agent
  workflow, executed as 62 logged increments; Ghidra ships 142 analyzers, ~57
  decompiler-relevant — every feasible one ported, plus the Listing tier, multi-format
  loaders (6 ELF chokepoints → `ObjectFormat` trait), kuna-native FID (`.fid`, not
  Ghidra's non-redistributable `.fidb`), and RTTI/Obj-C/PDB metadata (Swift deferred:
  no Rust demangler existed).
- **Region structurer** (`region-structurer-roadmap.md`, `regions.md`): the angr
  Phoenix/SAILR structurer roadmap that became DIV-12/DIV-14; deep goto-win schemas
  remain deferred.
