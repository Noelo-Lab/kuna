{
  "schema": "re-probe/1",
  "kind": "cli",
  "cmd": [
    "{{KUNA}}",
    "decompile-all",
    "{{BIN}}",
    "--option",
    "callbacktype",
    "on"
  ],
  "cwd": "{{WORK}}",
  "env": {
    "SLEIGHHOME": "{{SPECS}}"
  },
  "stdin": null,
  "timeout_s": 120,
  "repeat": 1,
  "target": {
    "binary_rel": "decompiler/crates/kuna-analysis/tests/fixtures/callbacktype_width_x86_64",
    "binary_sha256": "d6ff4a586c73366bd46fbb981c17e0265ca6455f1fe6d2c575c1604e4ee77c25",
    "binary_size": 16496,
    "binary_source": "in-repo",
    "in_repo_path": "decompiler/crates/kuna-analysis/tests/fixtures/callbacktype_width_x86_64",
    "selector": "cmp_outer",
    "selector_kind": "name"
  },
  "expect": {
    "exit_code": {
      "eq": 0
    },
    "stdout_matches": [
      "(v\\d+) = cmp_inner\\(a0,a1\\);(?:(?!\\n\\}\\n)[\\s\\S])*\\n  return \\1;\\n\\}",
      "\\\"%ld\\\\n\\\",zcmp\\("
    ],
    "stdout_absent": [
      "CONCAT44\\(dat_",
      "\\bint zcmp\\(",
      "\\bint cmp_inner\\("
    ]
  },
  "notes": "Each function here fits its `int` qsort slot on its own; a direct caller reads the whole `rax`. main hands `zcmp`'s result to printf(\"%ld\") and got `CONCAT44(dat_4,zcmp(..))`. `cmp_outer` passes `cmp_inner`'s whole `rax` out of its own `ret`, and declaring `cmp_inner` alone left `cmp_outer` returning a variable no path through the call sets.",
  "probe_id": "p-691289a20b9b"
}
