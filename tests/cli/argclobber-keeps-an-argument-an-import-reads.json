{
  "schema": "re-probe/1",
  "kind": "cli",
  "cmd": [
    "{{KUNA}}",
    "decompile-all",
    "{{BIN}}"
  ],
  "cwd": "{{WORK}}",
  "env": {
    "SLEIGHHOME": "{{SPECS}}"
  },
  "stdin": null,
  "timeout_s": 120,
  "repeat": 1,
  "target": {
    "binary_rel": "decompiler/crates/kuna-analysis/tests/fixtures/argclobber_import_x86_64",
    "binary_sha256": "ecd88cad55114959765266e9521ea066d6b162c7ef21a83bde0a8645a6836feb",
    "binary_size": 14472,
    "binary_source": "in-repo",
    "in_repo_path": "decompiler/crates/kuna-analysis/tests/fixtures/argclobber_import_x86_64",
    "selector": "0x11b0",
    "selector_kind": "addr"
  },
  "expect": {
    "exit_code": {
      "eq": 0
    },
    "stdout_matches": [
      "sub_1190\\(0x4018,v[0-9]+,a2\\) \\+ 1"
    ],
    "stdout_absent": [
      "sub_1190\\(0x4018,a2\\)"
    ]
  },
  "notes": "sub_1190 is `if (!a) return 0; return ext3(o,a,b);` in plain C, with no function pointer anywhere: ext3 is a dynamic import nothing states a signature for, so nothing at that call site reads rdx and kuna recovers two parameters for sub_1190 -- exactly the arguments a drop leaves. The direct call to a target nothing accounts for declines it; answering every direct call accounted deletes it.",
  "probe_id": "p-f1bada66dfff"
}
