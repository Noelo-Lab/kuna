{
  "schema": "re-probe/1",
  "kind": "cli",
  "cmd": [
    "{{KUNA}}",
    "decompile-all",
    "{{BIN}}"
  ],
  "cwd": "{{WORK}}",
  "env": {
    "SLEIGHHOME": "{{SPECS}}"
  },
  "stdin": null,
  "timeout_s": 120,
  "repeat": 1,
  "target": {
    "binary_rel": "decompiler/crates/kuna-analysis/tests/fixtures/calleevote_zero_x86_64",
    "binary_sha256": "92fcce67f29ec8ce46ba25d31e9fb51f3e3fe7f0d283b7adefa6b53177229a82",
    "binary_size": 5240,
    "binary_source": "in-repo",
    "in_repo_path": "decompiler/crates/kuna-analysis/tests/fixtures/calleevote_zero_x86_64",
    "selector": "get_c",
    "selector_kind": "name"
  },
  "expect": {
    "exit_code": {
      "eq": 0
    },
    "stdout_matches": [
      "unsigned long get_c\\(void \\*a0\\)\\n\\{\\n  return \\*\\(unsigned long \\*\\)\\(\\(long\\)a0 \\+ 0x10\\);",
      "unsigned long get_b\\(void \\*a0\\)"
    ],
    "stdout_absent": [
      "struct_\\d+"
    ]
  },
  "notes": "DEFAULT on x86-64, .text loaded at 0: get_c and get_b are called directly with a struct node * and also named by the const table ops inside .text, through which run calls them on a char buffer. Every SHF_ALLOC section's words are read whatever its address, so both keep void *.",
  "probe_id": "a-fb55b57565df"
}
