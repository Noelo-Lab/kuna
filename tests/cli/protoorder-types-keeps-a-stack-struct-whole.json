{
  "schema": "re-probe/1",
  "probe_id": "a-ca75c13c3e0d",
  "kind": "cli",
  "cmd": [
    "{{KUNA}}",
    "decompile-all",
    "{{BIN}}"
  ],
  "cwd": "{{WORK}}",
  "env": {
    "SLEIGHHOME": "{{SPECS}}"
  },
  "stdin": null,
  "timeout_s": 120,
  "repeat": 1,
  "target": {
    "binary_rel": "decompiler/crates/kuna-analysis/tests/fixtures/protoorder_stackstruct_x86_64",
    "binary_sha256": "82ee818a9bee936dd512a1b1ce9c3aea95a8f8d3930d477c79f5a76a8efebfa1",
    "binary_size": 14472,
    "binary_source": "in-repo",
    "in_repo_path": "decompiler/crates/kuna-analysis/tests/fixtures/protoorder_stackstruct_x86_64",
    "selector": "tail_bytes",
    "selector_kind": "name"
  },
  "expect": {
    "exit_code": {
      "eq": 0
    },
    "stdout_matches": [
      "stat v2; // stack - 0xa8",
      "if \\(fstat\\(a0,&v2\\)\\)",
      "v2\\._56_8_ = 0x200;"
    ],
    "stdout_absent": [
      "char v\\d+ \\[56\\]; // stack - 0xa8",
      "long v\\d+; // stack - 0x70",
      "fstat\\(a0,\\(stat \\*\\)v\\d+\\)"
    ]
  },
  "notes": "DEFAULT `types` on tail's `tail_bytes` shape. `dump` recovers its count as `void *`; voted onto `n_bytes` it reached the `st_size` read, `struct stat` split into `char [56]` + an `st_blksize` local at stack - 0x70, and the output read that local though nothing writes it. `n_bytes` is integer-used (`-n_bytes`, lseek's `off_t`), so the vote is refused and `stat` stays whole."
}
