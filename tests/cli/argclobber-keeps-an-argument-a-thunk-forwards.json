{
  "schema": "re-probe/1",
  "kind": "cli",
  "cmd": [
    "{{KUNA}}",
    "decompile-all",
    "{{BIN}}"
  ],
  "cwd": "{{WORK}}",
  "env": {
    "SLEIGHHOME": "{{SPECS}}"
  },
  "stdin": null,
  "timeout_s": 120,
  "repeat": 1,
  "target": {
    "binary_rel": "decompiler/crates/kuna-analysis/tests/fixtures/argclobber_thunk_x86_64",
    "binary_sha256": "e0626de36528599f62115e030422141042e9f5d94ab6beb4afd522e73d26412c",
    "binary_size": 12936,
    "binary_source": "in-repo",
    "in_repo_path": "decompiler/crates/kuna-analysis/tests/fixtures/argclobber_thunk_x86_64",
    "selector": "caller",
    "selector_kind": "name"
  },
  "expect": {
    "exit_code": {
      "eq": 0
    },
    "stdout_matches": [
      "sub_401030\\((?:\\(unsigned long \\*\\)0x404000|&dat_404000),v[0-9]+,a2\\) \\+ 1"
    ],
    "stdout_absent": [
      "sub_401030\\((?:\\(unsigned long \\*\\)0x404000|&dat_404000),a2\\)"
    ]
  },
  "notes": "sub_401030 is `test %rsi,%rsi; je; jmp *(%rdi)`: it names rdi and rsi, so kuna recovers two parameters while the function it tail-calls consumes three. Those two are exactly the arguments a drop would leave, so the prototype accounting admits it and the callee's BODY declines it -- rdx is unwritten at the indirect jump.",
  "probe_id": "p-fb4510197066"
}
