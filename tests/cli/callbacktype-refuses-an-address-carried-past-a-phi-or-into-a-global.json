{
  "schema": "re-probe/1",
  "kind": "cli",
  "cmd": [
    "{{KUNA}}",
    "decompile-all",
    "{{BIN}}",
    "--option",
    "callbacktype",
    "on"
  ],
  "cwd": "{{WORK}}",
  "env": {
    "SLEIGHHOME": "{{SPECS}}"
  },
  "stdin": null,
  "timeout_s": 120,
  "repeat": 1,
  "target": {
    "binary_rel": "decompiler/crates/kuna-analysis/tests/fixtures/callbacktype_refused_x86_64",
    "binary_sha256": "b9e3aeab05dd089e299f03cb6b0135592771fe49cc73120da22127b41610799a",
    "binary_size": 16800,
    "binary_source": "in-repo",
    "in_repo_path": "decompiler/crates/kuna-analysis/tests/fixtures/callbacktype_refused_x86_64",
    "selector": "quit_either",
    "selector_kind": "name"
  },
  "expect": {
    "exit_code": {
      "eq": 0
    },
    "stdout_matches": [
      "void quit_either\\(void\\)",
      "void quit_kept\\(void\\)",
      "signal\\(0xf,quit_either\\)",
      "signal\\(1,quit_kept\\)",
      "KEPT = quit_kept;"
    ],
    "stdout_absent": [
      "void quit_either\\(int a0\\)",
      "void quit_kept\\(int a0\\)"
    ]
  },
  "notes": "Each handler is registered with `signal` from ONE hoisted `lea` whose second use lies past a carrier: `run_either` calls `quit_either` through a phi that may also hold `other`, and `keep_and_register` writes the register into the global `KEPT`, which that body never reads back. The use walk follows the phi and counts the global write, so neither gets `void (*)(int)`.",
  "probe_id": "p-b799e90c6c28"
}
