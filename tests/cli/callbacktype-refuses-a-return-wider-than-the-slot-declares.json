{
  "schema": "re-probe/1",
  "kind": "cli",
  "cmd": [
    "{{KUNA}}",
    "decompile-all",
    "{{BIN}}",
    "--option",
    "callbacktype",
    "on"
  ],
  "cwd": "{{WORK}}",
  "env": {
    "SLEIGHHOME": "{{SPECS}}"
  },
  "stdin": null,
  "timeout_s": 120,
  "repeat": 1,
  "target": {
    "binary_rel": "decompiler/crates/kuna-analysis/tests/fixtures/callbacktype_width_x86_64",
    "binary_sha256": "d6ff4a586c73366bd46fbb981c17e0265ca6455f1fe6d2c575c1604e4ee77c25",
    "binary_size": 16496,
    "binary_source": "in-repo",
    "in_repo_path": "decompiler/crates/kuna-analysis/tests/fixtures/callbacktype_width_x86_64",
    "selector": "lcmp",
    "selector_kind": "name"
  },
  "expect": {
    "exit_code": {
      "eq": 0
    },
    "stdout_matches": [
      "long lcmp\\(long \\*a0,long \\*a1\\)",
      "\\\"%ld %ld %s\\\\n\\\",lcmp\\("
    ],
    "stdout_absent": [
      "\\bint lcmp\\(",
      "\\bint lcmp_tail\\(",
      "CONCAT44\\(dat_"
    ]
  },
  "notes": "`lcmp` returns a `long` it computes and `lcmp_tail` the `long` a tail-called strtol leaves; both are cast into qsort's `int` comparator slot, and main prints each whole result with %ld. Declaring `int` subtracted in the low half, and main's direct calls read the high half back as `CONCAT44(dat_4, ...)`, a global that is not in the image.",
  "probe_id": "p-62be4c9e88dd"
}
