{
  "schema": "re-probe/1",
  "kind": "cli",
  "timeout_s": 60,
  "cmd": [
    "{{KUNA}}",
    "decompile",
    "{{BIN}}",
    "cascade_leaf"
  ],
  "expect": {
    "exit_code": {
      "eq": 0
    },
    "stdout_absent": [
      "funcboundflow"
    ],
    "stdout_matches": [
      "a0\\s*==\\s*0x1000\\s*\\)\\s*return\\s+(?:10|0xa)\\s*;",
      "a0\\s*==\\s*0x3007"
    ]
  },
  "notes": "GH-468: ab_o2.exe's cascade_switch lost its 0xf043 case because aif planted entries inside a leaf only the PDB names. Reduced to a generated PE + sidecar .pdb: the default (auto -> aggressive) must not cut cascade_leaf at 0x1400011d0.",
  "target": {
    "binary_rel": "pe_pdbinterior_x86_64.exe",
    "binary_sha256": "9d572ee24504a476f0a343cba31b8f2579497f567a85afe25178c4c4c11af608",
    "binary_size": 2560,
    "binary_source": "in-repo",
    "in_repo_path": "decompiler/crates/kuna-analysis/tests/fixtures/pe_pdbinterior_x86_64.exe",
    "selector": "cascade_leaf",
    "selector_kind": "name"
  }
}
