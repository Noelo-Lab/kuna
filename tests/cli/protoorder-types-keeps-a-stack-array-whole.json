{
  "schema": "re-probe/1",
  "probe_id": "a-bec7620080ad",
  "kind": "cli",
  "cmd": [
    "{{KUNA}}",
    "decompile-all",
    "{{BIN}}"
  ],
  "cwd": "{{WORK}}",
  "env": {
    "SLEIGHHOME": "{{SPECS}}"
  },
  "stdin": null,
  "timeout_s": 120,
  "repeat": 1,
  "target": {
    "binary_rel": "decompiler/crates/kuna-analysis/tests/fixtures/protoorder_stackarray_x86_64",
    "binary_sha256": "9ba2a0f0261a6823bd060a70b9fa65405fa0c8425eeb8cf356a057460f5fa0a7",
    "binary_size": 14472,
    "binary_source": "in-repo",
    "in_repo_path": "decompiler/crates/kuna-analysis/tests/fixtures/protoorder_stackarray_x86_64",
    "selector": "main",
    "selector_kind": "name"
  },
  "expect": {
    "exit_code": {
      "eq": 0
    },
    "stdout_matches": [
      "char v3 \\[264\\];",
      "v3\\[v4\\] = \\(char\\)v4 \\* ",
      "sub_119f\\(&v3\\[\\(long\\)argc \\+ 0x14\\],&v3\\[1\\]\\)"
    ],
    "stdout_absent": [
      "char v\\d+ \\[20\\];",
      "unsigned int v\\d+ \\[61\\];",
      "\\(unsigned int \\*\\)\\(\\(long\\)v\\d+ \\+ \\(long\\)argc\\)"
    ]
  },
  "notes": "DEFAULT `types`: `geti`/`gets2` recover `int *`/`short *` parameters and main passes them addresses inside `char buf[256]`. A pointer vote at a frame address is a stack-layout hint; it split the buffer into `char [20]` + `unsigned int [61]` while the loop still wrote 256 bytes into the first. Votes at frame addresses are refused, so the buffer stays one `char [264]`."
}
