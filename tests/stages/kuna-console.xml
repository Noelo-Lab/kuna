<decompilertest>
<binaryimage arch="x86:LE:64:default:gcc">
<!--
    kuna infrastructure test: the stage registry + console capability.

    Proves that the kuna-owned IfaceKunaCapability self-registers its console
    commands inside the datatest harness (testfunction.cc ConsoleCommands ->
    IfaceCapability::registerAllCommands) with no upstream anchor edit, and
    that the stage registry (kuna_stages.cc) answers queries:

      phase list        - phases + Band B + sub-phase catalog (with LATENT)
      phase map <name>  - group / surface / sub-phase resolution
      phase status      - pipeline variant + kuna sub-phase option states

    Reuses the GH-558 bytes purely as a loadable function for `stage status`.
    Infrastructure only: nothing here perturbs decompilation output.
-->
<bytechunk space="ram" offset="0x100000" readonly="true">
83ff087f06b801000000c331c0c3
</bytechunk>
<symbol space="ram" offset="0x100000" name="gh558"/>
</binaryimage>
<script>
  <com>parse line extern int4 gh558(int4 x);</com>
  <com>lo fu gh558</com>
  <com>phase list</com>
  <com>phase map analysis</com>
  <com>phase map force goto</com>
  <com>phase map comparison-canonicalization</com>
  <com>phase status</com>
  <com>quit</com>
</script>
<stringmatch name="KUNA-CONSOLE #1: phase list shows Band B membership" min="4" max="4">\[Band B\]</stringmatch>
<stringmatch name="KUNA-CONSOLE #2: phase list includes the P3 dead-definition gate sub-phase" min="1" max="1">\[P3\] dead-definition-gate</stringmatch>
<stringmatch name="KUNA-CONSOLE #3: LATENT decisions are flagged in the catalog" min="5" max="40">\(LATENT\)</stringmatch>
<stringmatch name="KUNA-CONSOLE #4: phase map resolves the analysis rule group to P3" min="1" max="1">group analysis -&gt; P3</stringmatch>
<stringmatch name="KUNA-CONSOLE #5: phase map routes force goto to the P7 edge-virtualization sub-phase" min="1" max="1">surface "force goto" -&gt; P7 \(Region Hierarchy\) sub-phase edge-virtualization</stringmatch>
<stringmatch name="KUNA-CONSOLE #6: phase map resolves the GH-558 sub-phase" min="1" max="1">sub-phase comparison-canonicalization -&gt; P3</stringmatch>
<stringmatch name="KUNA-CONSOLE #7: phase status reports the active pipeline variant" min="1" max="1">pipeline variant: decompile</stringmatch>
<stringmatch name="KUNA-CONSOLE #8: phase status reports kuna sub-phase option defaults (DIV-2: original)" min="1" max="1">compareform: original</stringmatch>
</decompilertest>
